# 模型与供应商配置

> 为 Deep Student 接入你自己的 AI 模型：填一把 API 密钥，再告诉应用"哪个功能用哪个模型"，所有 AI 能力就都活了。

## 功能概览

Deep Student 本身不内置任何"免费云端 AI"，所有 AI 功能都通过你自己配置的模型服务运行。相关配置集中在两个设置页签：

- **模型服务**：管理"供应商"和"模型"。应用预置了 12 家供应商模板（SiliconFlow、DeepSeek、通义千问、智谱AI、字节豆包、MiniMax、月之暗面、OpenAI、Codex、NVIDIA、Xiaomi MiMo、Google Gemini），大多数只需填入 API 密钥即可使用；也可以自建供应商，接入 Anthropic、xAI (Grok)、Ollama、OpenRouter 等任意兼容端点。
- **模型分配**：为不同功能指派不同模型。对话、Anki 制卡、知识库嵌入、翻译、OCR、语音输入等各有独立的模型槽位，可以"对话用大模型、杂活用便宜小模型"。

所有 API 密钥都只保存在本机，并经过加密存储，不会上传到任何 Deep Student 服务器。

## 入口与界面布局

1. 点击侧边栏底部的「设置」，打开系统设置；在「学习桌面」模式下，设置以独立应用窗口打开，布局相同。
2. 在左侧设置导航中选择「模型服务」或「模型分配」页签。
3. 页签太多找不到某个设置时，用导航顶部的**设置搜索**（桌面窗口内按 `Cmd/Ctrl+F` 直接聚焦）：输入关键词（如"温度""嵌入"）即可跨页签搜索所有设置项，`↑` / `↓` 选择结果、`Enter` 跳转——应用会自动切到对应页签、滚动到该设置项并短暂高亮标出位置；`Esc` 清空搜索。没有匹配结果时会提示换个关键词，并提供「清空搜索」快捷出口。

「模型服务」页签为左右两栏：

- 左侧是**供应商列表**，每家供应商前有一个彩色图标；图标为灰色表示尚未配置密钥。桌面端可拖动排序。
- 右侧是选中供应商的详情：接口地址、API 密钥输入框、备注，以及该供应商下的**模型列表**。

「模型分配」页签是一组卡片，每张卡片对应一个功能（如"对话模型""嵌入模型"），从下拉框中选择模型即可，保存自动完成。

## 核心操作

### 为预置供应商填入 API 密钥

1. 打开「设置 → 模型服务」，在左侧供应商列表选择一家（例如 DeepSeek 或硅基流动）。
2. 点击「访问官网」前往该供应商官网注册并获取 API 密钥。
3. 回到应用，在「API 密钥」输入框粘贴密钥。看到"已安全保存"提示即表示保存成功，下次无需重新输入。
4. 建议随后点击模型行上的「测试连接」，确认密钥可用。

预置供应商已填好正确的接口地址，一般不需要修改；供应商本身不可删除，但其中的模型配置可以自由编辑和删除。

### 从 API 获取模型列表

如果供应商开通了新模型，或预置列表里没有你想用的模型：

1. 在供应商详情页点击「获取模型列表」。
2. 在弹出的「从 API 获取可用模型」对话框中，搜索并勾选想要的模型（已添加过的会标记"已添加"）。
3. 点击「添加 N 个模型」完成导入。

### 手动添加模型

1. 在供应商详情页点击「添加模型」。
2. 填写模型名称（必须与 API 调用时的真实模型名一致）。
3. 在「能力」标签页勾选模型能力：多模态模型（支持图片）、推理模型、嵌入模型、重排序模型、生图模型、支持工具调用。能力标记会影响该模型能出现在哪些分配槽位里。
4. 需要时在「参数」「推理」标签页调整温度、最大输出 Token、思维链等参数，然后保存。

### 添加自定义供应商（Anthropic / Grok / Ollama 等）

1. 在供应商列表右上角点击「添加供应商」（＋号）。
2. 填写供应商名称，选择「供应商类型」——除预置类型外，还提供 Anthropic、xAI (Grok)、百度文心、Mistral、OpenRouter、Ollama、通用适配器、自定义等选项。
3. 选择「API 协议」，四种可选：OpenAI Chat Completions、OpenAI Responses、Google GenerateContent、Anthropic Messages。
4. 填写「接口地址」和「API 密钥」。若是本机自建服务（Ollama / vLLM / llama.cpp 等），勾选「无需 API Key」，保存后会自动获取模型列表。
5. 需要时可展开「自定义请求头」填写额外 HTTP 头（JSON 格式）。

### 用 ChatGPT 账号登录 Codex

预置的「Codex」供应商不用 API 密钥，而是直接登录 ChatGPT 账号，使用账号套餐内的 Codex 额度：

1. 在供应商列表选择「Codex」。
2. 点击「使用浏览器登录」（或「使用设备码登录」），在浏览器中完成 ChatGPT 授权。
3. 登录成功后可看到套餐信息和「Codex 额度」用量，可随时「刷新额度」或「退出登录」。

### 为各功能分配模型

1. 打开「设置 → 模型分配」。
2. 至少设置「对话模型（解答 + 对话）」——这是所有 AI 对话的基础。
3. 按需设置其他槽位：
   - **Anki制卡模型**：用于自动生成记忆卡片（见 [12-Anki制卡与模板](12-Anki制卡与模板.md)）。
   - **嵌入模型（RAG知识库）** 与 **重排序模型（可选）**：让 AI 能检索你的资料库（见 [03-深度调研与智能记忆](03-深度调研与智能记忆.md)）。
   - **OCR引擎**：识别图片与扫描件文字，可添加多个引擎按优先级自动熔断切换。
   - **翻译专用模型**、**题库AI批改模型**、**语音输入 ASR 模型**、**生图模型**、**标题/标签生成模型**、**记忆决策模型**等：不设置时多数会回退使用对话模型或默认策略。
4. 每个下拉框选择后自动保存，无需额外操作。

### 硅基流动一键分配

如果你只有一把 SiliconFlow（硅基流动）密钥，想最快跑通全部功能：

1. 在「模型服务」中选择 SiliconFlow 供应商，填入 API 密钥。
2. 使用其详情页提供的"快速配置与一键分配"功能，应用会自动创建对话、嵌入、重排序、OCR 等一整套配置并完成模型分配。

## 进阶用法

### 模型分配预设

在「模型分配」页签底部的「模型分配预设」区域，可以把当前整套分配保存为预设，在多套方案（如"高质量档"与"省钱档"）之间一键切换：

1. 点击「保存当前配置为预设」，输入预设名称。
2. 之后随时点击预设上的「应用」切换；「更新」会用当前面板配置覆盖该预设。
3. 内置的「快速分配预设」由一键分配功能自动维护，不可更新或删除。

### 推理 / 思维链参数

编辑模型时，「推理」标签页可以统一设置思维链行为（如 OpenAI 的 reasoning.effort、Claude Extended Thinking、Gemini thinkingBudget 等），系统会自动映射到对应平台的参数格式。DeepSeek、通义千问、豆包、Kimi、智谱等国产模型也有各自的思维模式开关，界面会按所选供应商显示对应选项。

### 参数与上下文

「最大输出 Token」「上下文窗口大小」「温度参数」等可在模型编辑对话框的「参数」标签页调整；上下文窗口留空时会根据模型名称自动推断。

## 📱 移动端说明

- 移动端从抽屉菜单进入「设置」，设置为两级导航：先在分组列表首页选择分区，再进入具体设置页，功能与桌面端一致。
- 「模型服务」在小屏下改为两级导航：先看供应商列表，点击某一行进入该供应商的详情屏。
- 供应商列表的**拖拽排序在触屏上不可用**。
- Codex 的浏览器登录在移动端可能受系统限制，如遇问题可改用「使用设备码登录」。

## 常见问题

**Q：填了密钥还是发不出消息？**
A：先确认「模型分配」里的「对话模型」已经选了一个模型；再回到「模型服务」对该模型点「测试连接」。连接失败时依次检查接口地址、密钥和模型名称是否准确。

**Q：预置供应商可以删除吗？**
A：不可以，内置供应商入口无法删除（避免误删模板），但你可以不填密钥让它保持闲置；供应商下的模型配置可以自由删除。自建供应商删除时会连同其下所有模型一起移除。

**Q：一个功能不分配模型会怎样？**
A：多数可选槽位会自动回退：翻译、题库批改等未配置时使用「对话模型」；重排序模型不配置则跳过重排。只有「对话模型」和（若要用知识库）「嵌入模型」是建议必配的。

**Q：API 密钥存在哪里？安全吗？**
A：密钥只保存在本机并加密存储（AES-256-GCM），应用不会把密钥上传到任何第三方。注意：使用 AI 功能时，对话内容会发送给你所配置的模型服务商，受对方隐私政策约束。

**Q：想用本地模型（不联网）怎么办？**
A：先在本机部署 Ollama / vLLM 等服务，然后「添加供应商」→ 类型选 Ollama（或通用适配器）→ 填本机接口地址 → 勾选「无需 API Key」即可。

**Q：删除一个正在被使用的模型配置会怎样？**
A：如果该配置仍被某些功能分配使用，删除时会提示"该配置仍被 N 个功能使用"，继续删除会清空这些指派，相关功能需要重新分配模型。

<!-- 基于 v0.9.42 代码调研，2026-07-19；2026-08-24 补学习桌面窗口形态（设置搜索 ⌘F / 键盘选择 / 定位高亮 / 空态） -->
