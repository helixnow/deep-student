# 移动端指南

> Deep Student 的 Android 端不是"阉割版"，而是同一套应用换了一副适合单手操作的界面：核心学习功能基本都在，只有少数依赖桌面系统能力的功能除外。

## 移动端界面总览

移动端采用"**顶栏 + 抽屉 + 分屏**"的结构：

- **顶栏**：左侧 ☰ 按钮打开抽屉，中间是当前页面标题，右侧是当前页的快捷按钮（如聊天页的新建会话）。
- **抽屉（左侧滑出）**：一个统一的滚动面板，上半部分是**当前页面的内容区**（如聊天页的会话列表、待办页的清单列表），下半部分是**应用导航**，包含全部主入口：
  - 学习类：新会话 · 学习资源 · 待办
  - 管理类：技能管理 · 制卡任务 · 模板管理 · 设置
  - 以及「搜索与命令」（打开命令面板）
- **分屏**：主要页面是"左抽屉 – 中屏 – 右屏"三屏结构。中屏是主内容；右屏承载预览/编辑器等次级内容（如聊天页的资源库、学习资源的文件预览、技能页的编辑器），从右侧滑入、可整屏返回。

几个特别的入口：

- **设置**：移动端的设置是一个普通页面，采用**两级导航**——先是分组列表首页（带搜索框），点某个分区（模型服务、外观、数据治理等）进入具体设置；用顶栏返回箭头或系统返回键逐级退回。
- **数据统计、数据管理、PDF 阅读器**等页面没有抽屉入口，通过「搜索与命令」（命令面板）中的导航命令进入。

## 移动端特有交互

- **边缘滑动手势**：从屏幕左边缘向右滑打开抽屉；在有右屏内容的页面，从右边缘向左滑打开右屏。滑动需从屏幕边缘约 20px 内起手；PDF 阅读区、编辑器、导图画布内的手势不受影响。
- **Android 返回键**：按"浮层优先"处理——先关闭弹窗/命令面板/抽屉/图片查看器，再逐级返回页面，最后才把应用退到后台，不会直接杀掉应用。
- **触屏菜单**：桌面端靠鼠标悬停出现的操作，在移动端改为条目上的 **… 菜单**或常显按钮（如消息的复制按钮、会话条目的重命名/置顶/归档）。
- **长按拖拽**：待办任务等支持长按后拖动排序；供应商列表等桌面端的拖拽排序在触屏上关闭（避免和滚动冲突）。
- **软键盘防误触**：键盘弹出时抽屉导航会临时屏蔽，收起键盘后即可正常导航。
- **视图保活**：为省内存，后台最多保留 4 个页面的状态，更早的页面再次进入时会重新加载，个别未保存的界面状态（如筛选条件）可能重置。
- **界面缩放**：「设置 → 外观」中的「全局界面缩放（实验）」在移动端同样可用，适合临时放大整体界面。

## 各主要页面的移动形态

**新会话（聊天）**——三屏：

- 左抽屉：会话列表（置顶 / 课题 / 对话分区）、新对话按钮、已归档会话入口；会话条目的 … 菜单提供重命名、置顶、归档。
- 中屏：消息流与输入栏。输入栏保留了桌面端的全部能力：＋ 菜单（附件 / 资源库 / 相机）、技能面板、MCP 面板、对话参数、推理深度、语音输入；流式生成中可随时点停止。
- 右屏：学习资源库、代码沙箱、Anki 制卡面板等按需滑入。

**学习资源**——三屏：

- 左抽屉：桌面、全部文件、最近、收藏、各资源类型、回收站、向量化状态、记忆等快捷视图。
- 中屏：文件列表（列表/网格切换、排序、搜索、多选批量操作：添加到聊天 / 移动 / 删除）；进入子文件夹后顶栏出现面包屑逐级返回。
- 右屏：文件预览，多标签页管理，关闭最后一个标签自动回到列表。

**待办**——两屏：左抽屉是收件箱 / 今日 / 即将到期 / 四象限等智能视图和清单列表；主区是任务列表 + 快速添加框（支持 `明天交作业 !高 #标签` 这类速记语法）+ 番茄钟。点任务打开全屏详情编辑。

**技能管理 / 制卡任务 / 模板管理**：均为"列表 + 右屏编辑器"的形态，操作与桌面端一致；模板的代码编辑（正面/背面/样式）也在右屏完成。

## 功能可用性对照表

| 模块 | 桌面端 | 移动端 | 说明 |
|---|---|---|---|
| AI 对话（新会话） | ✅ | ✅ | 三屏布局；附件、技能、MCP、模型切换、语音输入均可用 |
| 深度调研 / 智能记忆 | ✅ | ✅ | 记忆管理入口在「学习资源」的记忆视图 |
| 学习资源中心 | ✅ | ✅ | 文件预览在右屏打开；部分文件导出操作在移动端受限 |
| PDF / 文档阅读 | ✅ | ✅ | 工具栏为紧凑模式，支持双指缩放 |
| 翻译 / 论文搜索 / 作文批改 | ✅ | ✅ | 从学习资源新建对应内容进入 |
| 笔记 / 知识导图 | ✅ | ✅ | 导图支持触屏编辑与捏合缩放 |
| 待办与番茄钟 | ✅ | ✅ | 两屏布局；支持沉浸专注模式 |
| 题库与练习 | ✅ | ✅ | |
| Anki 制卡 / 模板管理 | ✅ | ✅ | 模板代码编辑在右屏进行 |
| 闪卡复习 | ✅ | ✅ | |
| 设置：模型服务 / 模型分配 | ✅ | ✅ | 移动端为两级导航；列表拖拽排序不可用 |
| 技能系统（含社区市场安装） | ✅ | ✅ | 编辑器在右屏打开 |
| MCP：远程服务器（SSE/HTTP/WebSocket） | ✅ | ✅ | 预置 MCP 服务均为网络类型，可用 |
| MCP：Stdio 本地进程 | ✅ | ❌ | 移动端无法启动本地进程 |
| MCP：OAuth 浏览器授权 | ✅ | ❌ | OAuth 浏览器登录仅桌面端可用 |
| 数据治理（备份/恢复/审计） | ✅ | ✅ | 恢复后按提示重启应用 |
| 云同步（实验性） | ✅ | ✅ | Android 仅 WebDAV；S3 与 FTP 均不可用。与电脑互相同目录需走 WebDAV，或桌面导出 ZIP 再导入 |
| 自动化（定时任务） | ✅ | ⚠️ | 管理界面需要桌面应用；移动端后台调度受系统限制 |
| 学习桌面（多窗口工作台） | ✅ | ❌ | 仅大屏桌面端启用 |
| 快速学习小窗（系统全局快捷键） | ✅ | ❌ | 依赖桌面系统快捷键 |
| 本地终端命令 / 本地文件夹授权 | ✅ | ❌ | 依赖桌面文件系统与终端 |

✅ 可用 ⚠️ 部分可用/形态不同 ❌ 不可用

## 常用操作速查

### 开始一次对话

1. 打开抽屉，点「新会话」（或点顶栏 ☰ 后在会话列表点「新对话」）。
2. 输入栏的 ＋ 菜单可添加附件、打开学习资源库或调用相机拍照。
3. 需要资料时从右屏资源库选择文件加入对话（详见 [02-AI对话与会话管理](02-AI对话与会话管理.md)）。

### 阅读一份 PDF

1. 抽屉 → 「学习资源」，在文件列表点击 PDF。
2. 右屏打开预览，双指缩放、翻页、搜索、高亮均可用；顶栏返回或手势滑回退出。

### 改一个设置

1. 抽屉滚动到底部，点「设置」。
2. 在设置首页的分组列表中点目标分区（模型服务、外观、数据治理等），也可以用顶部搜索框直接搜设置项。
3. 改完用顶栏返回箭头或系统返回键逐级退回。

## 常见移动端问题

**Q：进了某个页面找不到返回入口？**
A：优先用 Android 返回键（或返回手势），它会逐层关闭浮层和页面。通过命令面板进入的页面（如数据统计）同样可以用返回键退出。

**Q：按返回键应用直接退到后台了？**
A：这是预期行为：当前没有任何浮层和历史页面时，返回键会把应用退到后台（不会丢数据），再次打开即恢复。

**Q：切回之前的页面后状态没了（列表回到顶部、筛选被清空）？**
A：移动端只保活最近 4 个页面，更早的页面会被回收以节省内存。重要内容（笔记、对话）都是实时保存的，不会因此丢失。

**Q：为什么有些按钮要点两次？**
A：删除等破坏性操作在移动端普遍采用"二次确认"（再点一次确认），防止误触。

**Q：顶部界面被系统状态栏遮住了一点？**
A：可在「设置 → 开发者选项」中调整"顶部栏顶部边距高度"，为状态栏预留空间（个别机型需要手动微调）。

**Q：移动端可以和电脑共用数据吗？**
A：可以。未配置云端端到端加密密码时，「立即备份到云端」再「从云端恢复」的包是便携归档，校验会拒绝；已配置同一加密密码时，云端整包是加密全保真 ZIP，解包后应能整槽恢复。已配置却读不到已存密码时会拒绝导出，不会偷偷打成便携包。此接线前上传的便携云端包仍可导入，不会套用已存密码。Android 目前只能配置 **WebDAV**（S3 / FTP 会在保存时被拒绝）。要和电脑共用学习数据：两端都配同一个 WebDAV 和同一个加密密码，使用实验性的记录级双向同步；或在电脑上「导出备份」并设置备份密码，把含受保护敏感材料的全保真换机 ZIP 导入手机（归档内容本身未加密，请勿通过不可信渠道传播）。桌面写入的 S3 / FTP 配置在 Android 上不会被加载。每次云备份仍是完整 ZIP 单对象上传，没有增量传输。详见 [16-数据管理与云同步](16-数据管理与云同步.md)。

<!-- 基于 v0.9.42 代码调研，2026-07-19 -->
