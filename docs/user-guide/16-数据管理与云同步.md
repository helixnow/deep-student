# 数据管理与云同步

> 你的学习数据全部存在本机，由你完全掌控——这一章教你备份、恢复、迁移数据，以及把备份同步到自己的网盘。

## 功能概览

Deep Student 是**本地优先**的应用：对话记录、笔记、文件、卡片、错题、设置等全部保存在你设备上的本地数据库和文件里，不会在你不知情的情况下上传到任何服务器。围绕这些数据，应用提供**三套彼此独立的机制**，请勿混淆：

1. **本地备份**：在本机一键创建全量 ZIP 备份，支持自动定时备份、分层备份（按数据重要程度选择范围）、备份验证与恢复。这是最可靠的一层，恢复走 A/B 槽位完整恢复。
2. **云端整包备份（实验性）**：把整包备份上传到你自己的 WebDAV / S3 兼容存储 / FTP（平台差异见下文），按版本保留，换机或灾备时从云端下载。⚠️ 当前版本有恢复限制，见[「云端整包备份 / 从云端恢复」](#云端整包备份--从云端恢复)。
3. **记录级双向同步（实验性）**：基于变更记录的多设备同步，带冲突检测、合并策略与隔离区。它偏"备份式同步"，不是实时协作。

此外还有：

- **数据导入导出**：ZIP 备份可导出到任意位置、在另一台设备导入；错题等数据还支持 JSON/CSV 导出。
- **隐私控制**：API 密钥等敏感数据 AES-256-GCM 加密存储；无行为追踪；匿名错误报告默认关闭；记忆功能可随时关闭（关闭后不再提取、检索或注入用户记忆，记忆内容不会发送给外部 AI 服务）。

## 入口与界面布局

主要入口是「设置 → 数据治理」，页面顶部分为几个标签：

- **概览**：全局版本、健康状态、各数据库状态，可「运行健康检查」。
- **归档**：管理已归档的聊天会话（恢复或永久删除）。
- **备份**：本地备份的创建、备份列表、备份设置（自动备份策略）。
- **同步**：云端整包备份与记录级双向同步（均标注"实验版"）、云存储配置、冲突检测与解决、同步隔离区。
- **审计**：所有迁移/备份/恢复/同步操作的日志，可追溯。
- **缓存**：媒体缓存管理（PDF 预览图片、压缩图片、OCR 文本、向量索引）。
- **调试**：诊断信息，底部是「危险操作」区（含「清空所有数据」）。

另外，「设置」中还有「隐私与数据」区块（错误报告开关、数据流向说明）和「记忆」区块（记忆开关与存储位置）。

## 核心操作

### 创建一次本地备份

1. 打开「设置 → 数据治理 → 备份」。
2. 点击「完整备份」（或「快速备份」）。想直接得到 ZIP 文件，用「导出备份」并选择保存目录。
3. 备份过程有进度条和预计剩余时间；完成后新备份出现在「备份列表」，可对它执行「验证」「导出为 ZIP」「恢复」「删除」。

备份按层级组织（在「分层备份」中可按需勾选，默认勾选**核心数据 + 重要数据**并开启「包含资产文件」，从而覆盖文件库原始文件等重要资产；可自由增减）：

- **核心数据 (P0)**：聊天记录、虚拟文件系统、主数据库（卡片/设置等）
- **重要数据 (P1)**：P0 + 使用统计、笔记附件
- **可重建数据 (P2)**：P0-P1 + 向量索引（可通过重新索引恢复）
- **大型资产 (P3)**：P0-P2 + 图片/文档/视频等媒体文件

### 开启自动备份

1. 在「备份」标签找到「备份设置」。
2. 打开「自动备份」，选择「备份间隔」（6 到 72 小时可选）。
3. 设置「最大备份保留数」，超过上限自动删除最旧的备份。
4. 空间紧张时可开启「精简备份模式」（仅备份数据库和设置，跳过图片、知识库等大文件），或改用分级备份自选范围。
5. 备份目录可自定义：用命令面板打开「数据管理」页面，在「本地备份」标签中设置「备份目录」。

### 从本地备份恢复数据

1. 在「备份列表」中选择一个备份，点击「恢复」；或先「导入」外部 ZIP 备份文件。
2. 确认提示：恢复将替换当前数据，系统会在恢复前**自动创建预恢复备份**。
3. 恢复过程中请勿关闭应用；完成后按提示「立即重启」以确保数据完整加载。

应用采用双数据空间设计（A/B 槽位）：导入的备份先写入非活动空间，重启后再切换，避免恢复过程破坏现有数据。

### 配置云存储（WebDAV 为例）

1. 打开「设置 → 数据治理 → 同步」，在「云端同步」区域点「去配置云存储」。
2. 「存储类型」选择 **WebDAV**（适用于坚果云、Nextcloud、自建 WebDAV 等）。
3. 填写「服务器地址」（如坚果云为 `https://dav.jianguoyun.com/dav/`）、「用户名」和「密码」——建议使用**应用专用密码**而非登录密码。
4. 可修改「根目录」（云端存放备份的目录，默认 `deep-student-sync`）。
5. **强烈推荐**设置「端到端加密密码」：设置后，**整包 ZIP 备份**、**记录级同步的变更记录 / 同步清单 / 删除标记**，以及 **workspace 数据库、VFS blob 与资产文件**都会用 AES-256-GCM 端到端加密，云服务商无法读取内容。对象地址仍用明文内容哈希做去重。启用加密前已上传的明文遗留对象会被拒绝下载，需用同一密码重新同步以完成迁移。⚠️ 密码只以加密形式保存在本机，不会上传，**遗忘则无法恢复任何已加密的云端备份**，且所有设备必须用同一密码。
6. 点「测试连接」确认可用，再点「保存配置」。

桌面端（Windows / macOS / Linux）还支持 **S3 兼容存储**（AWS S3、Cloudflare R2、阿里云 OSS、MinIO 等，需填 Endpoint、存储桶、Access Key）和 **FTP/FTPS**（实验性，存在已知数据一致性风险）。**Android 发行版目前只有 WebDAV 可用，S3 与 FTP 均不可用**——在 Android 上保存 FTP 配置会被直接拒绝并给出明确错误提示（详见下方[移动端说明](#-移动端说明)）。使用明文 `http://` / `ftp://` 连接时会收到安全警告，建议始终用加密连接。

### 云端整包备份 / 从云端恢复

1. 配置好云存储后，点「立即备份到云端」，可附一条备注（如"重大更新前备份"）；每次上传生成一个新版本，超出保留数量的旧版本会自动清理。
2. 在其他设备上配置同一云存储（和同一加密密码）后，点「从云端恢复最新版本」，或「查看历史版本」选择某个版本恢复。
3. ⚠️ 恢复会覆盖本地数据，操作前建议先给本机做一次备份。

> ⚠️ **当前版本限制（便携 ZIP / 云端整包）**：通过「导出备份」得到的**未加密**便携 ZIP，以及上传到云端的整包备份，出于安全会剥离本地加密密钥与审计数据，其清单被如实标记为"部分归档"——导入后**不能走本地备份那样的 A/B 槽完整恢复**（恢复校验会明确拒绝，界面上也会如实标注），API 密钥等凭据需要在新设备重新录入。这是刻意的安全取舍，不是数据丢失。当前建议：
>
> - 把**本地备份列表内的备份**当作主要灾备手段，它支持完整的 A/B 槽恢复；
> - 未加密便携 ZIP / 云端整包适合迁移学习数据本身（对话、笔记、错题等），迁移后在新设备重新录入 API 密钥等凭据；
> - 不要把未加密便携 ZIP 或云端整包当作唯一的数据保险。
>
> 需要**全保真换机**时，请使用**加密全保真导出**：在「导出备份」时填写「备份密码（端到端加密，可选）」，敏感数据（本地加密密钥、审计记录等）会以 Argon2id + AES-256-GCM 密封进 ZIP；在新设备「导入」该 ZIP 时输入同一密码，即可解封为可整槽恢复的完整快照，密码缺失或错误会得到明确提示，不会静默损坏数据。⚠️ 备份密码不会被保存或上传，丢失后该 ZIP 将永远无法解密。

### 记录级双向同步与冲突解决（实验性）

除了云端整包备份，「同步」标签还提供基于变更记录的**记录级双向同步**——注意它和上面的整包备份是两套独立机制，互不替代：

1. 选择「合并策略」：保留最新（按时间自动选）/ 保留本地 / 使用云端 / 手动处理。
2. 点「双向同步」（或单向「上传」「下载」）。
3. 若检测到冲突，会区分数据库级与记录级冲突，可按策略批量解决，也可逐条「手动合并」。
4. 无法安全应用的云端变更会进入「同步隔离区」，不阻塞后续同步，排查后可重试或丢弃。

这套同步为实验性功能，定位是"多设备备份与换机"，不适合两台设备同时高频编辑同一份数据。

> 端到端加密开启后，记录级同步的变更记录、清单、删除标记，以及 workspace 数据库、VFS blob 与资产文件都会加密上传。对象 key 仍是明文内容哈希（用于去重，不是秘密）。启用加密前留在云端的明文对象会被拒绝下载，用同一密码再同步一次即可迁移。云端整包 ZIP 同样整包加密。

### 关闭记忆 / 控制隐私

1. 「设置」中找到「记忆」区块，关闭「启用」开关：AI 将不再提取、检索或注入你的用户记忆。
2. 「隐私与数据」区块中，「匿名错误报告」默认关闭，可自行决定是否开启；这里还有完整的「数据流向说明」和隐私政策入口。
3. 请记住：使用任何 AI 功能时，相关对话内容都会发送给**你自己配置的**模型服务商（见 [14-模型与供应商配置](14-模型与供应商配置.md)）；不使用云同步时，数据不出本机。

## 进阶用法

- **审计日志**：「审计」标签记录每次迁移/备份/恢复/同步的时间、目标、耗时与结果，出问题时先来这里看。
- **清理缓存**：「缓存」标签可分类清理 PDF 预览图、压缩图片、OCR 结果和向量索引。⚠️ 清理向量索引后，智能搜索与知识库召回会失效，需重新建立索引。
- **归档会话**：从聊天侧栏归档的会话集中在「归档」标签管理，可恢复回主列表或永久删除。
- **旧版数据迁移**：从很老的版本升级时，可在设置中把旧版对话数据迁移到新架构（「数据迁移」，支持进度查看与回滚）。
- **清空所有数据**：「调试」标签底部「危险操作」区中的「清空所有数据」会删除全部应用数据（备份文件保留），需多重确认且不可撤销——执行前务必先备份。

## 📱 移动端说明

- 「设置 → 数据治理」在移动端可用，备份/恢复/云同步流程与桌面端一致。
- **Android 云存储只有 WebDAV**：Android 发行版目前仅编译了 WebDAV 后端，**S3 兼容存储与 FTP 均不可用**。两者在 Android 上都会被配置层直接拒绝：尝试保存 FTP 配置会立刻收到「FTP/FTPS storage is not available on Android」的错误，尝试使用 S3 会收到「当前安装包不支持 S3 兼容存储，请改用 WebDAV。」；桌面端写入的 FTP / S3 配置在 Android 上也不会被加载，因此不会留下看似保存成功、实则永远连不上的僵尸配置。如果你在桌面端用 S3 做云备份、想在手机上恢复，请改用以下两种方式之一：
  - 桌面端把云存储改为 **WebDAV**，重新上传后在手机上从同一 WebDAV 恢复；
  - 桌面端「导出备份」得到 ZIP，把文件传到手机后在「数据治理 → 备份」中导入（受上文便携 ZIP 恢复限制约束，迁移前请先验证链路）。
- 备份与恢复涉及大量文件读写，移动设备上耗时更长，请保持应用在前台、电量充足。
- 恢复完成后按提示「立即重启」；「清空所有数据」等个别操作在移动端会提示手动退出并重新打开应用。
- 云存储配置与加密密码同样只以加密形式保存在本机；换手机后需重新配置云存储与密码。

## 常见问题

**Q：我的数据到底存在哪里？**
A：全部在本机的应用数据目录（本地数据库 + 文件），包括对话、笔记、文件、卡片和设置。备份 ZIP 的保存位置可在「数据管理」页面的「本地备份」标签中查看和修改。

**Q：换新电脑怎么迁移全部数据？**
A：老设备「导出备份」得到 ZIP → 拷贝到新设备 → 新设备安装应用后在「数据治理 → 备份」导入该 ZIP 并恢复 → 重启应用。也可以走云端：老设备「立即备份到云端」，新设备配置同一云存储后「从云端恢复最新版本」。想连同 API 密钥等敏感数据一起完整迁移，导出时请设置**备份密码**得到加密全保真换机包，新设备导入时输入同一密码即可整槽恢复；**未加密**便携 ZIP 与云端整包仍受恢复限制（见上文「当前版本限制」），迁移前请先验证恢复能走通，且新设备是 Android 时云端只能走 WebDAV。

**Q：云备份、本地备份、双向同步有什么区别？**
A：三套独立机制。**本地备份**是本机的 ZIP 快照，支持 A/B 槽完整恢复，最可靠；**云端整包备份**是把整包上传到你的网盘按版本保留，用于换机/灾备（当前有恢复限制）；**记录级双向同步**按变更记录逐条合并多设备数据，带冲突解决，均为实验性。日常请至少保持本地备份开启。

**Q：云同步和坚果云等网盘的同步一样吗？**
A：不一样。Deep Student 的云同步是"版本化备份 + 变更合并"：每次上传是一个完整版本，多设备通过冲突策略合并，不是文件级实时同步。不要用网盘客户端直接同步应用数据目录，那可能损坏数据库。

**Q：忘记了端到端加密密码怎么办？**
A：无法找回。已加密的云端备份将永远无法解密。你可以清除云存储配置、设置新密码后重新上传（本机数据不受影响）。

**Q：明明输入的是正确密码，上传却一直提示"与该云端目录已登记的密码不一致"？**
A：这多半是**旧版加密标记被配错密码的设备抢先升级**导致的。旧版本创建的云端目录里有一个不含密码校验子的旧标记文件（`.encryption-marker` v1）；升级到新版本后，**第一台带密码上传的设备**会把自己的密码登记为该目录的校验子。如果那台设备恰好配错了密码，正确密码的设备反而会被上传拦截（这是刻意的 fail-closed 保护，**下载与解密不受影响**——数据本身仍由你最初加密时的密码保护）。解锁方法：
1. 先在所有设备上核对加密密码，确认哪一个才是最初加密数据时用的正确密码，并把配错的设备改正；
2. 用网盘自带的客户端 / 网页端（如坚果云、S3 控制台）进入该云端同步目录，**删除根目录下的 `.encryption-marker` 文件**（或用文本内容确认后重写它）；
3. 回到应用，用**正确密码**的设备执行一次「立即备份到云端」或同步——它会用正确密码重新登记标记，之后其他持相同密码的设备即可恢复上传。

⚠️ 删除 `.encryption-marker` 只是重置"该目录用哪个密码"的登记，**不会解密任何数据**，也救不回忘记的密码；请在确认所有设备已统一为正确密码后再删除，否则下一台上传的设备又会把错误密码登记进去。

**Q：备份失败或恢复中断怎么办？**
A：应用会记住未完成的任务，重新打开「数据治理」时会提示"发现可恢复的备份任务"，点「恢复」续跑。反复失败时到「审计」标签查看错误详情，常见原因是磁盘空间不足。

**Q：卸载应用会删除数据吗？**
A:卸载行为因操作系统而异，部分平台会清除应用数据目录。卸载前请务必「导出备份」到应用目录以外的位置（或云端）。

<!-- 基于 v0.9.42 代码调研，2026-07-19；2026-08-23 依据 cloud-sync-sota-b343 R01 调研更新云同步事实（Android 仅 WebDAV、三套机制区分、便携/云 ZIP 恢复限制）；同日依据 R02-backup 更新限制块（未加密便携 ZIP 诚实标记为部分归档；引擎层加密全保真导出/导入/整槽恢复闭环已实现）；同日依据 R06-guide 回写：导出/导入界面备份密码入口已开放、分层备份默认勾选核心+重要数据并含资产文件、Android 配置 SSOT 在保存与加载路径均拒绝 FTP；2026-08-24 依据 R06-e2ee-honest 如实标注 E2EE 覆盖范围（覆盖整包 ZIP 与记录级变更/清单/tombstone，不覆盖文件级 workspace db / VFS blob / 资产文件）；同日依据 R09-e2ee 回写 RESTORE-MATRIX P2-1 运维指引：新增"旧标记被错密码设备抢先升级"的解锁问答（删/重写云端 `.encryption-marker` 后由正确密码设备重新登记）；同日依据 R09-android 更新移动端段：Android 上 S3 的拒绝文案改为面向用户（"当前安装包不支持 S3 兼容存储，请改用 WebDAV。"），且 S3 配置与 FTP 一样在保存与加载路径均被拒绝、不产生僵尸配置 -->
