# 数据管理与云同步

> 你的学习数据全部存在本机，由你完全掌控——这一章教你备份、恢复、迁移数据，以及把备份同步到自己的网盘。

## 功能概览

Deep Student 是**本地优先**的应用：对话记录、笔记、文件、卡片、错题、设置等全部保存在你设备上的本地数据库和文件里，不会在你不知情的情况下上传到任何服务器。**本地备份**是基础安全层：可在本机创建完整恢复点，支持自动定时备份、验证与 A/B 槽位恢复。

需要使用自己的云存储时，应用提供**两套彼此独立的云端机制**，请勿混淆：

1. **云端整包备份（实验性）**：把整包备份上传到你自己的 WebDAV / S3 兼容存储 / FTP（平台差异见下文），按版本保留，换机或灾备时从云端下载。⚠️ 当前版本有恢复限制，见[「云端整包备份 / 从云端恢复」](#云端整包备份--从云端恢复)。
2. **记录级双向同步（实验性）**：基于变更记录的多设备同步，带冲突检测、合并策略与隔离区。它偏"备份式同步"，不是实时协作。

此外还有：

- **数据导入导出**：ZIP 备份可导出到任意位置、在另一台设备导入；错题等数据还支持 JSON/CSV 导出。
- **隐私控制**：API 密钥等敏感数据 AES-256-GCM 加密存储；无行为追踪；匿名错误报告默认关闭；记忆功能可随时关闭（关闭后不再提取、检索或注入用户记忆，记忆内容不会发送给外部 AI 服务）。

## 入口与界面布局

主要入口是「设置 → 数据治理」，页面顶部分为几个标签：

- **概览**：全局版本、健康状态、各数据库状态，可「运行健康检查」。
- **归档**：管理已归档的聊天会话（恢复或永久删除）。
- **备份**：本地备份的创建、备份列表、备份设置（自动备份策略）。
- **同步**：云端整包备份与记录级双向同步（均标注"实验版"）、云存储配置、冲突检测与解决、同步隔离区。
- **审计**：所有迁移/备份/恢复/同步操作的日志，可追溯。
- **缓存**：媒体缓存管理（PDF 预览图片、压缩图片、OCR 文本、向量索引）。
- **调试**：诊断信息，底部是「危险操作」区（含「清空所有数据」）。

另外，「设置」中还有「隐私与数据」区块（错误报告开关、数据流向说明）和「记忆」区块（记忆开关与存储位置）。

## 核心操作

### 创建一次本地备份

1. 打开「设置 → 数据治理 → 备份」。
2. 点击「完整备份」（或「快速备份」）。想直接得到 ZIP 文件，用「导出备份」并选择保存目录。
3. 备份过程有进度条和预计剩余时间；完成后新备份出现在「备份列表」，可对它执行「验证」「导出为 ZIP」「恢复」「删除」。

备份按层级组织（在「分层备份」中可按需勾选，默认勾选**核心数据 + 重要数据**并开启「包含资产文件」，从而覆盖文件库原始文件等重要资产；可自由增减）：

- **核心数据 (P0)**：聊天记录、虚拟文件系统、主数据库（卡片/设置等）
- **重要数据 (P1)**：P0 + 使用统计、笔记附件
- **可重建数据 (P2)**：P0-P1 + 向量索引（可通过重新索引恢复）
- **大型资产 (P3)**：P0-P2 + 图片/文档/视频等媒体文件

### 开启自动备份

1. 在「备份」标签找到「备份设置」。
2. 打开「自动备份」，选择「备份间隔」（6 到 72 小时可选）。
3. 设置「最大备份保留数」，超过上限自动删除最旧的备份。
4. 自动备份始终创建包含受治理数据库与资产的完整恢复点；旧版「精简备份」选项已经停用。需要只保存部分内容时可使用分层备份，但分层产物是部分归档，不能替换整个数据槽。
5. 备份目录可在当前「备份」标签的「本地备份」设置中自定义；留空则使用默认目录。

### 从本地备份恢复数据

1. 在「备份列表」中选择一个备份，点击「恢复」；或先「导入」外部 ZIP 备份文件。
2. 确认提示：恢复将替换当前数据，系统会在恢复前**自动创建预恢复备份**。
3. 恢复过程中请勿关闭应用；完成后按提示「立即重启」以确保数据完整加载。

应用采用双数据空间设计（A/B 槽位）：导入的备份先写入非活动空间，重启后再切换，避免恢复过程破坏现有数据。

### 配置云存储（WebDAV 为例）

1. 打开「设置 → 数据治理 → 同步」，在「云端同步」区域点「去配置云存储」。
2. 「存储类型」选择 **WebDAV**（适用于坚果云、Nextcloud、自建 WebDAV 等）。
3. 填写「服务器地址」（如坚果云为 `https://dav.jianguoyun.com/dav/`；若同步目录在「我的坚果云」等中文/空格文件夹下，请把该路径写进地址，例如 `https://dav.jianguoyun.com/dav/我的坚果云/`。应用会统一解码列举结果，不会再因编码不一致而静默列空）、「用户名」和「密码」——建议使用**应用专用密码**而非登录密码。
4. 可修改「根目录」（云端存放备份的目录，默认 `deep-student-sync`）。
5. **强烈推荐**设置「端到端加密密码」（至少 **8** 个字符，按 Unicode 码点计数，不能为空白；例如 4 个 emoji 只算 4 个字符。短于此长度会拒绝保存，不会出现「已配置」徽章）：设置后，**整包 ZIP 备份**、**记录级同步的变更记录 / 同步清单 / 删除标记**，以及 **workspace 数据库、VFS blob 与资产文件**都会用 AES-256-GCM 端到端加密，云服务商无法读取内容。对象地址仍用明文内容哈希做去重。启用加密前已上传的明文遗留对象会被拒绝下载，需用同一密码重新同步以完成迁移。⚠️ 密码只以加密形式保存在本机，不会上传，**遗忘则无法恢复任何已加密的云端备份**，且所有设备必须用同一密码。
6. 点「测试连接」确认可用，再点「保存配置」。

加密容器与云端密码校验子携带的 Argon2id 参数来自云端，应用会在开始密钥派生前执行硬上限检查：内存成本最多 **1 GiB**、迭代次数最多 **16**、并行度最多 **8**。参数超限会被当作损坏或不兼容数据直接拒绝，不会尝试分配异常内存或长时间计算；应用自己写出的默认参数为 64 MiB / 3 次 / 并行度 4。

桌面端（Windows / macOS / Linux）还支持 **S3 兼容存储**（AWS S3、Cloudflare R2、阿里云 OSS、MinIO 等，需填 Endpoint、存储桶、Access Key。控制台展示的带 bucket 前缀域名如 `https://mybucket.oss-cn-hangzhou.aliyuncs.com` 可原样粘贴，应用会剥掉前缀，避免拼成 `bucket.bucket`）和 **FTP/FTPS**（实验性，存在已知数据一致性风险）。**Android 发行版目前只有 WebDAV 可用，S3 与 FTP 均不可用**——在 Android 上保存 FTP 配置会被直接拒绝并给出明确错误提示（详见下方[移动端说明](#-移动端说明)）。使用明文 `http://` / `ftp://` 连接时会收到安全警告，建议始终用加密连接。

### 云端整包备份 / 从云端恢复

1. 配置好云存储后，点「立即备份到云端」，可附一条备注（如"重大更新前备份"）。每次成功备份都会把当前数据打成**完整 ZIP**，再作为**单个对象**上传并生成一个新版本；未变化的文件也不会少传。当前版本**没有**增量传输、内容去重或 CDC，不要按「只上传变化」来估算流量与时间。超出保留数量的旧版本会自动清理。新备份的版本编号是随机 ID，**不再包含备份时间或设备名**；恢复按版本列表里的条目进行，不必从网盘文件名解读。未配置云端端到端加密密码时，这条路径导出 ZIP **不会**带备份密码，产物仍是便携归档；已配置时会用已存密码导出加密全保真 ZIP（外层 DSBK 与内层备份密码用同一已存密码），解包后应能整槽恢复。已配置却读不到已存密码时会**拒绝导出**，不会偷偷打成便携包。
2. 「从云端恢复」会先下载并导入该 ZIP，再走与本地备份相同的整槽恢复校验。未配置云端端到端加密密码时，云端整包是便携/部分归档，**校验会明确拒绝，不会覆盖当前数据**。已配置时，导入只对带密封载荷的加密全保真 ZIP 使用同一已存密码；此接线前上传的便携云端包仍可导入，不会套用已存密码。全保真包解封并通过校验后可整槽恢复。历史版本和状态卡上的「最新版本」若已标记为便携归档，恢复按钮会直接禁用，不会开始下载；「从云端恢复最新版本」仍先走确认框。
3. 操作前仍建议先给本机做一次本地完整备份。只有校验通过的备份才会写入非活动槽并重启。未配置加密密码时的换机，请用「导出备份」并设置备份密码（见下方限制说明）。

从云端下载大备份包时支持**断点续传**（WebDAV 与桌面 S3 兼容存储）：网络中断后重新对同一版本执行恢复/下载，会从上次中断的字节处继续，不必整包重下；服务端忽略 Range 时会诚实从零重下，错位续传会明确失败。下载完成后会对整个文件做 SHA256 校验，断点损坏或云端对象被改动都会被明确拒绝并提示重试，不会把损坏文件交给恢复流程。记录级同步里的**工作区 / blob / 资产**大文件同样走这条续传路径：旁路 `.ds-dl.part` **按内容哈希区分**，写完并校验后才替换本地文件，不会往已有业务文件上追加；云端换成新版本时不会续上旧前缀。加密对象的密文旁路也会按密文哈希保留，中断后再次同步可继续，不必整包重下。**FTP 仍不支持**下载续传，中断后需整包重下（应用会明确按失败处理，不会把下载了一半的文件当作成功）。Android 发行版只有 WebDAV，S3 续传只在桌面生效。此外，所有云端下载（包括同步内部的文件级对象下载）都会核对实际收到的字节数与云端声明的大小，不一致时立即报错，绝不把半包或错版本文件保存为成功结果。

> ⚠️ **当前版本限制（便携 ZIP / 云端整包）**：「导出备份」未填备份密码时，产物是便携归档：剥离本地加密密钥与审计数据，清单如实标记为"部分归档"。导入后**不能**走本地备份那样的 A/B 槽完整恢复（恢复校验会明确拒绝，当前数据不会被覆盖）。「立即备份到云端」按是否配置了云端端到端加密密码分两种：
>
> - **未配置**云端加密密码：云端整包仍是便携归档，从云端恢复时校验会明确拒绝，不会覆盖当前数据；
> - **已配置**云端加密密码：会导出加密全保真 ZIP（外层 DSBK 与内层备份密码用同一已存密码），解包后应能整槽恢复；已配置却读不到已存密码时会拒绝导出，不会偷偷打成便携包。此接线前上传的云端包仍可能是便携归档，恢复时不会套用已存密码；
> - 把**本地备份列表内的备份**当作主要灾备手段，它支持完整的 A/B 槽恢复；
> - 多设备日常共用数据可走实验性的**记录级双向同步**（见下文），不要把未加密便携 ZIP 当作唯一保险。
>
> 需要**全保真换机**时，请使用**加密全保真导出**：在「导出备份」时填写「备份密码（保护凭据等敏感材料，可选）」，敏感数据（本地加密密钥、审计记录等）会以 Argon2id + AES-256-GCM 密封进 ZIP；在新设备「导入」该 ZIP 时输入同一密码，即可解封为可整槽恢复的完整快照，密码缺失或错误会得到明确提示，不会静默损坏数据。⚠️ 备份密码只保护密封的敏感材料：ZIP 里的聊天记录、错题、文件等归档内容本身未加密，任何拿到文件的人无需密码即可读取，请勿通过不可信渠道传播。备份密码不会被保存或上传，丢失后密封的敏感数据无法恢复、不能整槽恢复，但业务数据仍可读取。

### 云端仓库巡检（只读体检）

云端备份放得越久，越需要定期确认它**真的还能恢复**——网盘静默损坏、误删文件、上传中断残留都可能让某个版本悄悄失效。「云存储」面板底部提供**云端仓库巡检**：

1. 先「测试连接」，然后在「云端仓库巡检」区域点「开始巡检」。
2. 巡检会逐个下载云端清单引用的备份对象，核对**存在性**、**SHA256 校验和**与（启用端到端加密时）**加密头是否完好**，并报告**孤儿对象**（云端存在但没有任何清单引用、只占空间的文件）。因为要完整下载每个对象做哈希，耗时和流量与云端仓库总大小相当，建议在 Wi-Fi 下进行。WebDAV 与桌面 S3 巡检同一对象时支持**断点续传**（瞬时断线从已下载前缀继续）；**FTP 仍整包重下**。
3. 巡检是**只读**的：它绝不修改、覆盖或删除任何云端文件；发现问题后怎么处理由你决定，报告下方会给出对应的处置指引。

三种结论：

- **全绿**：引用对象齐全、校验和一致、加密头可解、无孤儿——该云端目录当前可以作为可靠灾备。
- **发现问题**：报告会按类别列出问题清单。「对象缺失 / SHA256 不匹配 / 加密头不可解 / 明文混布」意味着对应版本已无法完整恢复：不要动其他仍健康的版本，先在数据完好的设备上重新「立即备份到云端」产生新的云端版本（未配置加密密码时仍是便携归档，不能整槽恢复），确认巡检通过后再删除坏版本；「孤儿对象」不影响恢复，确认后可用网盘自带工具手动清掉。
- **巡检不完整**：云端文件列表被截断或个别对象读取失败时，应用会**如实拒绝给出「全绿」结论**（即使没发现任何问题）——请重试；在结果变为全绿之前，不要把该目录当作唯一灾备。

### 未同步文件清单（只读）

「同步」标签底部的「未同步文件」面板会只读对照云端清单与本机文件，集中列出云端存在、但尚未安全落到本机的附件或资产。常见类别包括下载未完成、启用加密前的明文遗留被拒收、文件名大小写冲突、净化后重名和非法路径。

- 「重新检查」只刷新清单，不写入或删除本机与云端数据，也不推进同步游标。
- 对可重试的下载可点「重试下载同步」；文件名冲突或非法路径需要先在源设备重命名，再重新同步。
- 条目过多时界面只显示前一部分并明确标注总数；空清单只表示云端清单中的文件已落地本机，不等同于云端仓库巡检全绿。

### 记录级双向同步与冲突解决（实验性）

除了云端整包备份，「同步」标签还提供基于变更记录的**记录级双向同步**——注意它和上面的整包备份是两套独立机制，互不替代：

1. 选择「合并策略」：保留最新（按时间自动选）/ 保留本地 / 使用云端 / 手动处理。
2. 点「双向同步」（或单向「上传」「下载」）。
3. 若检测到冲突，会区分数据库级与记录级冲突，可按策略批量解决，也可逐条「手动合并」。
4. 无法安全应用的云端变更会进入「同步隔离区」，不阻塞后续同步，排查后可重试或丢弃。

这套同步为实验性功能，定位是"多设备备份与换机"，不适合两台设备同时高频编辑同一份数据。

> 端到端加密开启后，记录级同步的变更记录、清单、删除标记，以及 workspace 数据库、VFS blob 与资产文件都会加密上传。对象 key 仍是明文内容哈希（用于去重，不是秘密）。新写入的变更/清单/删除标记路径不再包含完整设备名；新写入的文件级清单与快照对象名也不再包含时间或设备名（旧路径仍可被新版本读取）。启用加密前留在云端的明文对象会被拒绝下载，用同一密码再同步一次即可迁移。云端整包 ZIP 同样整包加密。

### 记录级时点恢复（本机自动快照）

批量解决记录冲突或执行库级策略覆盖前，应用会自动保存受影响记录的本机快照。可在记录冲突面板的「自动快照（可撤销）」区域找到对应批次，点「回退此批」恢复到操作前状态；回退前还会再创建一个撤销点，因此回退本身也可撤销。

这不是持续版本历史，而是危险操作前的最小恢复点：

- 快照**只保存在本机，不作为记录级同步数据上传云端**；它会随所在数据库进入覆盖该数据库的本地备份。
- 每个数据库最多保留最近 50 个快照批次，超出后自动清理最旧批次。
- 回退结果会作为新的本地变更等待上传，下一次同步可把回退后的状态传播给其他设备。

### 关闭记忆 / 控制隐私

1. 「设置」中找到「记忆」区块，关闭「启用」开关：AI 将不再提取、检索或注入你的用户记忆。
2. 「隐私与数据」区块中，「匿名错误报告」默认关闭，可自行决定是否开启；这里还有完整的「数据流向说明」和隐私政策入口。
3. 请记住：使用任何 AI 功能时，相关对话内容都会发送给**你自己配置的**模型服务商（见 [14-模型与供应商配置](14-模型与供应商配置.md)）；不使用云同步时，数据不出本机。

### 云端目录的加密标记与「密码不一致」解锁

开启端到端加密后，云端根目录下会有一个小文件 `.encryption-marker`（加密标记）：它记录"这个云端目录已经存在加密备份"，并（在较新版本中）携带一个**不可逆的密码校验子**。上传前应用会用它做两件事：拒绝向已加密的目录上传明文备份；拦截**配错密码**的设备，避免同一目录里混入两套无法互解的密文。标记里没有密码本身，也解不出密码。

此外，本机会保存一份「该云端目录曾经加密」的**本机记忆**作为第二道门禁：即使云端标记被误删，本机在未填写加密密码时仍会拒绝向该目录上传明文备份。它不是上文的 AI 用户记忆，不会上传到云端，落盘内容只含云端实例的单向指纹和首次观察时间，不含服务器地址或根目录明文；它只影响本机的明文上传判断。填写原密码进行加密上传不受影响，并会自动重新登记云端标记；确需改用未加密备份，请换一个新的云端目录。

**已知的一种被锁场景（旧标记被错密码设备抢先升级）**：旧版本写出的标记不含密码校验子；升级到新版本后，**第一台**带密码上传的设备会把自己的密码"登记"为该目录的校验子。如果这台设备恰好配错了密码，它会用错误密码完成登记——此后**持有正确密码的原设备**反而会在上传时被拦下，提示「加密密码与该云端目录既有加密备份使用的密码不一致」。注意：这只影响**上传**；下载与恢复不受标记影响，密码对不对由备份数据本身的解密校验判定。

按以下步骤解锁（在持有**正确**密码的设备上操作）：

1. **先自证密码正确**：在本机对同一云端目录执行「从云端恢复最新版本」（或任选一个历史版本）。云端端到端加密密码正确时，整包 ZIP 能解密并导入；密码错误会在下载/解密阶段失败。未配置加密密码时云端整包是便携归档，整槽恢复校验会拒绝——**这不能用来判断密码对错**，只要下载/解密成功即可自证。下载与恢复不受标记影响，密码对不对由备份数据本身的解密校验判定。
2. **删除云端标记**：用你的网盘自带工具（坚果云网页版 / Nextcloud 文件页 / S3 控制台等）打开同步根目录（默认 `deep-student-sync`），删除根目录下的 `.encryption-marker` 这一个文件。⚠️ 只删这一个文件，**不要**碰 `backups/`、`manifests/` 等目录——删除标记不会删除任何备份数据。
3. **用正确密码重新登记**：回到本机点「立即备份到云端」。上传前应用会发现标记缺失，用本机（正确的）密码重新写入带校验子的标记，此次上传正常完成。
4. **纠正配错密码的设备**：在那台设备的云存储配置里改成正确的加密密码。在改正之前，它的上传会被新标记再次拦截——这正是校验子的预期保护，不要通过反复删标记来"绕过"它。

如果两台设备**确实**各自用不同密码上传过备份（互相无法解密），说明该目录里已经存在两套密文，删标记解决不了互解问题：请让其中一台改用新的云端根目录，各自独立备份。

## 进阶用法

- **审计日志**：「审计」标签记录每次迁移/备份/恢复/同步的时间、目标、耗时与结果，出问题时先来这里看。
- **清理缓存**：「缓存」标签可分类清理 PDF 预览图、压缩图片、OCR 结果和向量索引。⚠️ 清理向量索引后，智能搜索与知识库召回会失效，需重新建立索引。
- **归档会话**：从聊天侧栏归档的会话集中在「归档」标签管理，可恢复回主列表或永久删除。
- **旧版数据迁移**：从很老的版本升级时，可在设置中把旧版对话数据迁移到新架构（「数据迁移」，支持进度查看与回滚）。
- **清空所有数据**：「调试」标签底部「危险操作」区中的「清空所有数据」会删除全部应用数据（备份文件保留），需多重确认且不可撤销——执行前务必先备份。

## 📱 移动端说明

- 「设置 → 数据治理」在移动端可用；主要备份与恢复步骤相同，但云存储提供商、文件选择器和重启行为存在下述平台差异。
- **Android 云存储只有 WebDAV**：Android 发行版目前仅编译了 WebDAV 后端，**S3 兼容存储与 FTP 均不可用**。两者在 Android 上都会被配置层直接拒绝：尝试保存 FTP 配置会立刻收到「FTP/FTPS storage is not available on Android」的错误，尝试使用 S3 会收到「当前安装包不支持 S3 兼容存储，请改用 WebDAV。」；桌面端写入的 FTP / S3 配置在 Android 上也不会被加载，因此不会留下看似保存成功、实则永远连不上的僵尸配置。如果你在桌面端用 S3 做云备份、想在手机上恢复，请改用以下两种方式之一：
  - 桌面端把云存储改为 **WebDAV**，重新上传后在手机上从同一 WebDAV 恢复；
  - 桌面端「导出备份」得到 ZIP，把文件传到手机后在「数据治理 → 备份」中导入（受上文便携 ZIP 恢复限制约束，迁移前请先验证链路）。
- 备份与恢复涉及大量文件读写，移动设备上耗时更长，请保持应用在前台、电量充足。
- **Android 上导入/导出 ZIP 会经过临时中转**：通过系统文件选择器选择的文件（`content://` 安全 URI）无法被直接随机读写，应用会先把 ZIP 复制到应用私有的临时目录再处理（导入），或先在临时目录生成 ZIP、完成后再写入你选择的位置（导出）。因此设备剩余空间需要**至少约为备份 ZIP 大小的两倍**；处理完成（含失败）后临时文件会自动清理，不会长期占用空间。
- 从「下载」等系统目录选择文件时，个别情况下文件名会显示为数字编号（系统文件选择器只给出了不透明 ID）——应用会自动通过文件内容识别类型，属正常现象，不影响导入。
- 恢复完成后按提示「立即重启」；「清空所有数据」等个别操作在移动端会提示手动退出并重新打开应用。若自动重启没有生效，手动结束应用再打开即可——数据空间切换固定发生在**下一次启动**时，重启前应用仍运行在旧数据上，不会出现「一半新一半旧」。
- 云存储配置与加密密码同样只以加密形式保存在本机；换手机后需重新配置云存储与密码。

## 常见问题

**Q：我的数据到底存在哪里？**
A：全部在本机的应用数据目录（本地数据库 + 文件），包括对话、笔记、文件、卡片和设置。备份 ZIP 的保存位置可在「数据管理」页面的「本地备份」标签中查看和修改。

**Q：换新电脑怎么迁移全部数据？**
A：可靠换机有两条路。一是老设备「导出备份」并设置**备份密码**，得到含受保护敏感材料的全保真换机包（归档内容本身未加密，请勿通过不可信渠道传播）→ 拷到新设备 → 在「数据治理 → 备份」导入并输入同一密码 → 整槽恢复后重启。二是两端都配置同一云端端到端加密密码，再「立即备份到云端」/「从云端恢复」——已配置时云端整包是加密全保真 ZIP，解包后应能整槽恢复；**未配置加密密码时**这条路径上传的是便携归档，整槽恢复校验会拒绝，当前数据不会被覆盖。多设备日常同步可走实验性记录级双向同步。新设备是 Android 时云端只能走 WebDAV。

**Q：云备份、本地备份、双向同步有什么区别？**
A：**本地备份**是基础安全层：本机 ZIP 快照支持 A/B 槽完整恢复，最可靠。云端则有两套独立机制：**云端整包备份**把整包上传到你的网盘按版本保留，用于换机/灾备（当前有恢复限制）；**记录级双向同步**按变更记录逐条合并多设备数据，带冲突解决。两套云端机制均为实验性，彼此不替代；日常请至少保持本地备份开启。

**Q：云同步和坚果云等网盘的同步一样吗？**
A：不一样。Deep Student 的云同步是"版本化备份 + 变更合并"：每次上传是一个完整版本，多设备通过冲突策略合并，不是文件级实时同步。不要用网盘客户端直接同步应用数据目录，那可能损坏数据库。

**Q：忘记了端到端加密密码怎么办？**
A：无法找回。已加密的云端备份将永远无法解密。你可以清除云存储配置、设置新密码后重新上传（本机数据不受影响）。

**Q：上传时提示「加密密码与该云端目录既有加密备份使用的密码不一致」，但我确定自己的密码没错？**
A：多半是旧版加密标记被一台配错密码的设备抢先"登记"了（下载恢复不受影响）。先在本机从云端恢复一次以自证密码正确，然后按上文[「云端目录的加密标记与『密码不一致』解锁」](#云端目录的加密标记与密码不一致解锁)的步骤删除并重新登记云端的 `.encryption-marker` 标记文件。

**Q：备份失败或恢复中断怎么办？**
A：应用会记住未完成的任务，重新打开「数据治理」时会提示"发现可恢复的备份任务"，点「恢复」续跑。反复失败时到「审计」标签查看错误详情，常见原因是磁盘空间不足。

**Q：卸载应用会删除数据吗？**
A:卸载行为因操作系统而异，部分平台会清除应用数据目录。卸载前请务必「导出备份」到应用目录以外的位置（或云端）。

<!-- 依据 v0.9.44 / cloud-sync-sota-b343 当前实现核对，2026-08-24：Android 仅 WebDAV；云端整包与记录级同步为两套机制；仓库巡检和未同步清单均只读；Argon2id 参数有应用级上限；加密目录本机记忆不上云；记录级危险操作前快照只在本机保留；WebDAV 与其他 provider 下载均校验实际字节数。 -->
