model=claude-fable-5-thinking-xhigh

# 27 — 09 号报告三处数字勘误（独立复算）

- 任务：对 `09-invariants-leftover.md` 中三处被引数字独立复算——
  ①第 11 项 token 预算护栏；②第 18 项 `pointer:coarse` 次数；
  ③第 18 项 v0.9.44 InputBar 单体行数。
- 方法：现树只读 read / `rg --count-matches` 直测，历史行数用只读
  `git show v0.9.44:<path> | wc -l` 复算；无任何 git 写操作、未用 gh。
  不改 09 原文，本文件为唯一产出。

## 1. token 预算护栏：09 引的是 R1 历史注释，现行护栏为 6800/51500/75500

- 09 第 11 项写：「token 预算锁 `tests/vitest/chat-v2/token-budget.test.ts:131`
  （现行基线：单组 7389 / schema 合计 54050 / 总计 75689，护栏
  9500/68000/95000……）」。
- 复算：`token-budget.test.ts:131` 是注释块里标明的 **R1 历史基线**；
  同一注释 `:132-136` 写明 R2–R4 三轮精简后实测为单组 6172（qbank-tools）/
  schema 合计 46671 / schema+content 合计 68310，且「R4 起护栏按新总量收紧为
  ≈10% 余量」。真正生效的护栏常量在 `:139-141`：
  `MAX_SINGLE_GROUP_SCHEMA_TOKENS = 6_800`、`MAX_TOTAL_SCHEMA_TOKENS = 51_500`、
  `MAX_TOTAL_TOKENS = 75_500`，由 `:196-203`（单组）与 `:205-211`（合计）
  两个断言强制执行。
- 勘误：09 的「现行……护栏 9500/68000/95000」应为
  「现行护栏 **6800/51500/75500**；9500/68000/95000 是 :131 注释保留的
  R1 旧护栏」。基线数字 7389/54050/75689 同为 R1 旧值，现行实测见 :133-134。
  与 15 号复核意见（15:184-185「实为 6800/51500/75500」）一致。
- 对裁决的影响：无。「预算锁在位且随实现演进」成立，且现行护栏比 09
  所引更紧，护栏方向只紧不松。

## 2. pointer:coarse 次数：4105 可精确复现，但口径是「src+tests」而非「全树」

- 09 第 18 项写：「全树 `pointer:coarse` 出现 4105 次（≥ Step 8 基线 3056）」。
- 复算（`rg` 按匹配次数计数，当前树，排除 `.git`）：
  - `src/`：**4101** 次（分布在 474 个文件）；
  - `tests/`：**4** 次（`chatV2SendButtonContract.test.ts` 2 +
    `data-governance/DataGovernanceDashboard.abg.source.test.ts` 2）；
  - `docs/`：**20** 次（14 次在审计系列自身文档 07/09/15/20 内，
    6 次在基座既有 dev 文档 `0824-MERGE-PLAN.md`、`dev/0824-g-i18n.md`、
    `dev/mobile-uiux-unify/ROUND-{81,82,90}-FIXES.md`）；
  - 字面全树合计：**4125** 次。
- 勘误：**4105 = src 4101 + tests 4**，数字本身可精确复现，但「全树」标签
  不准——字面全树今为 4125，且审计文档每提一次该串就会污染此口径。
  15 号提的「4105 应为 4101」（15:185）对应仅 `src/` 的口径，同样自洽；
  两个数都不算错，分歧只在计数范围。建议后续统一写明口径，
  如「src 4101 / src+tests 4105」。
- 对裁决的影响：无。4101 / 4105 / 4125 任一口径均 ≥ Step 8 基线 3056。

## 3. v0.9.44 InputBar 行数：实测 3919 行，09 的 3921 沿用了 Step 8 旧数

- 09 第 18 项写：「`InputBarUI.tsx` 现为 2661 行，3921 行单体未复活」。
- 复算：
  - v0.9.44：`git show v0.9.44:src/features/chat/components/input-bar/InputBarUI.tsx
    | wc -l` = **3919**；该版本文件以换行符结尾（`od -c` 核实末字节为 `\n`），
    `wc -l` 计数无末行歧义。
  - 现树：同路径文件 = **2661** 行（同法核实换行结尾）——09 此数正确。
- 勘误：「3921 行单体」应为「**3919** 行单体」。07 号（07:65「3919 行单体
  （git show v0.9.44 实测）」）与 15 号（15:109-111）此前均已实测 3919；
  09 复述了 Step 8 记录的 3921（差 2 行的旧口径数字）。
- 对裁决的影响：无。2661 < 3919，「单体未复活」成立。

## 结论

09 号报告三处数字勘误如下：①第 11 项护栏现行值为
**6800/51500/75500**（09 引用的 9500/68000/95000 是 `token-budget.test.ts:131`
注释中的 R1 旧护栏，且被误标为「现行」）；②第 18 项 **4105** 次可精确复现，
但口径实为 src+tests（src 4101 + tests 4），字面「全树」今为 4125（docs 内
另有 20 次）；③第 18 项 v0.9.44 单体实测 **3919** 行而非 3921，现树 2661 行
无误。三处均为数字或口径层面的勘误，均不动摇 09 对第 11、18 项 PASS 的裁决，
也不涉及任何产品代码缺陷。**本轮不改代码。**
