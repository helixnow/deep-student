# DOCS-SYNC — 用户文档与实现对齐记录（sota-subapp-polish 轮）

- 日期：2026-08-24
- 基线分支：`cursor/sota-subapp-polish-2399`
- 范围：`docs/user-guide/` 02 / 04 / 05 / 07 / 08 / 09 / 10 / 11 / 12 / 13，另含 `docs/dev/workbench-a11y-checklist.md`
- 原则：文档只对齐**已落地实现**；本轮未改代码的子应用（题库、制卡）不虚构功能，只标注复核结论或已知差异。

## 一、变更对照表

| 文档 | 变更类型 | 变更内容 | 实现依据（提交 / 代码） |
|------|----------|----------|--------------------------|
| 02 AI对话与会话管理 | 补新功能 + 新增章节 | ① 「从此处分支」补角标说明：原会话被分支消息显示「已从此处分支」，重启可见、点击跳转分支会话；② 引用预览 / PDF 打开失败给可见 toast；③ 新增「在学习桌面中使用对话」章节：单实例主窗、跨窗会话跳转握手（引用点击自动聚焦对话窗并切会话）、新会话指令挂起消费、命令面板对话命令、会话右键「在新窗口打开」（chat-session multi 实例轻窗） | `540c7a52`（branchSession + 角标）、`e8b8040a`（失败 toast）、`705833ba`（navigate-to-session 握手）、`208cb624`（命令面板对话命令）、`bf8fc6dc`（会话新窗口） |
| 04 学习资源中心 | 修正 + 补新功能 + 新增章节 | ① 删除行为修正：软删进回收站**无确认弹窗**，toast 带「撤销」（单个与批量一致），仅回收站内永久删除才二次确认；② 补「记忆徽标」（类型 + 用途 + 星标）与收藏计数徽标；③ 补 Finder 席新能力：空格 Quick Look 浮层（图片原图 / PDF 首页真预览）、`⌘Z` 撤销移动 / 重命名、复制 / 粘贴 / 制造副本（`⌘C/V/D`）、`⌘A` 全选、多选 `Enter` 打开全部（限流提示）、`Esc` 优先级；④ 新增「在学习桌面中使用资源库（Quick Look 式预览）」章节：双击开窗、悬停预览卡、拖出开新窗、预览窗查找（⌘F / ⌘G）、打印、另存为、引用到对话 | `LearningHubSidebar.tsx` 软删 + undo toast、`ea41e92c`（FinderQuickLook）+ `bfefd639`（真预览）、`e884df5f` / `923e5932`（撤销栈 + 键盘 + 徽标真数据）、`8e2a021f`（复制/粘贴/副本）、`FilesAppWindow.tsx` / `useFilesHoverPreview.tsx` / `FilePreviewAppWindow.tsx` |
| 05 文档阅读与翻译 | 补行为细节 + 补新功能 | ① 自定义提示词与领域预设联动：默认文案随领域切换，仅显式修改过才固定为覆盖项；② 离线口径修正：新翻译需网络，但编辑保存、质量评分、「保存并关闭」等本地操作离线可用；③ 划词动作菜单：复制 / 引用到对话 / 做笔记 / 生成题目 / 制卡；④ 高亮点击改色 / 删除（桌面锚定浮层、触屏底部操作条）；⑤ EPUB 章节进度随资源保存 | `b8f23462`（领域 override 门控 + 本地保存去离线拦截）、`73b55016`（保存并关闭）、`cb7181f6`（划词菜单 + 高亮操作）、`8d781f7d` / `8a0c9f9d`（引用 / 做笔记接线 + EPUB 进度落库）、`008eca0a`（菜单并存生成题目 / 制卡） |
| 07 笔记 | 补新功能 | ① 标签页循环 `Ctrl/⌘+Shift+]/[`；② 快速打开面板 `Ctrl/⌘+P`（文件名搜索 + 全文搜索）；③ 版本脚注补链接面板 / 重命名回写 `[[wikilink]]` / 资源分页加载。图谱 / 属性 / 搜索操作符第二波由笔记席随实现自带文档同步 | `dc3a2851`（重命名回写 + 分页）、`4fa3a735`（未链接提及转双链）、`NotesSearchOverlay.tsx`；`4a25b41b`（第二波，含 07 文档自同步） |
| 08 知识导图 | 补新功能 | 方向键按空间位置导航（`Alt+方向键` 移动节点本身）；多实例（分屏 / 多窗）下键盘导航只作用于聚焦画布；`Esc` 放行给背诵 / 演示等上层退出逻辑。sheet 切换 / 背诵 SRS / 大纲窗口化第二波由导图席随实现自带文档同步 | `6f75fcec`（空间导航限实例 + Esc 放行 + .mmap 导入 + 分支批量挖空）；`3f456e29` / `f0b876cf`（第二波，含 08 文档自同步） |
| 09 作文批改 | 版本脚注补全 | 上一波已同步图例颜色 / 多轮流程 / OCR 顺序回填 / 存为笔记；本次脚注补「轮次切换脏确认对话框」「建议采纳与撤销」为该分支实现 | `fd11a821`（DsAlertDialog 脏确认）、`01578cdf`（建议应用 / 撤销、OCR 顺序回填、存为笔记） |
| 10 效率工具 | 版本脚注补全 | 上一波已更新：番茄投射窗 / 菜单栏三态文案 / 休息期闪卡复习、待办快捷键窗口门禁、时长立即生效；本次脚注标明基于分支 | `05425509` / `ec949f01` / `f8e1b47a` / `c7145b1d` / `ad3731a4` |
| 11 题库与练习 | 标已知差异 + 随后续席位对齐 | ① 新增「已知差异（2026-08-24 走查）」章节：初版列 5 条（源自 R1-06 B1/B2/B3/B4/B5），题库席随后修复 B1/B2/B3/B6 后收敛为 2 条（限时 / 模拟考不可断点续考、多窗会话单槽互顶）；② 正文对齐修复后的行为：每日一练真实进度 + 庆祝 + 目标持久化 + 排除已答 + 再练一组、达标跟随目标、限时练习总结卡与「再来一轮」、组卷 PDF/Word 置灰徽标 | `R1-06-exam.md`、`eabd8fa0`（练习闭环真实进度 + 自评去重）、`8e2a47f9`（限时总结卡 + 导出置灰） |
| 12 Anki制卡与模板 | 复核 + 并行席位已同步 | 制卡核心链路（对话制卡 / 模板编辑器 / 导入导出）复核无漂移；任务看板空态双引导 / 重试常显 / 标题角标与模板库键盘操作等本轮改动，由并行席位随实现同步进文档（脚注合并保留其记录） | R1-09 审阅未发现 guide 12 漂移；`978310ed` / `6d76b876` + `9dd89867`（并行席位文档同步） |
| 13 闪卡复习 | **修正文档漂移** + 补新功能 | ① Q6 修正：调度设置**已上线**（统计页底部「调度设置」区：每日新卡上限 / 每日复习上限 0–9999 / 目标保持率 0.50–0.99；统计加载失败时仍可用），原文"暂时不能"与实现相反；② 统计章节补调度设置区说明；③ 入口补菜单栏到期数直达；④ 进阶用法补番茄钟休息期「去复习 N 张」入口；⑤ 撤销评分更新为会话内多级逐步撤销（他端评分的过期步骤自动跳过） | `SchedulerSettingsSection.tsx`（R1-09 B1 文档漂移项）、`StatusBarItems.tsx`（菜单栏入口）、`f8e1b47a`（番茄休息复习）、`d55c1b82`（多级撤销 + 设置可达性） |

## 二、随本轮 a11y 修补同步的开发文档

| 文档 | 变更 |
|------|------|
| `docs/dev/workbench-a11y-checklist.md` | 新增「§13 2026-08-24 补遗（sota-subapp-polish 轮）」：Dock tooltip 触屏等价（未运行应用长按钉住 tooltip；运行中应用长按列表加应用名头部）、Exposé 焦点环令牌化 + forced-colors 适配、`useFocusTrap` 扩展（initialFocus / restoreFocus）并接入 DsDialog / DsAlertDialog / 壁纸管理 / 系统窗抽屉 |

## 三、i18n 结论

本轮 UI 改动（Dock tooltip 钉住、窗口列表应用名头部、Exposé 焦点环、focus trap）均复用既有文案：

- Dock tooltip 与窗口列表头部显示的应用名来自应用注册表的 `nameKey`（已有中英翻译）；
- 焦点环 / focus trap 为纯行为与样式变更，无新增用户可见字符串。

**无新增 i18n key 需求。**（番茄休息复习入口的 `pomodoro.breakReview`、`menubar.pomodoroBreakRemaining` 已随 `f8e1b47a` 在 zh-CN / en-US 同步补齐，非本次改动。）

## 四、遗留（文档层面待下轮处理）

- 文档 11 的已知差异随修复逐条移除：B1/B2/B3/B6 已随 `eabd8fa0` 修复并同步正文，剩余 B4（断点续考）/ B5（多窗单槽）修复后删除「已知差异」章节。
- 文档 13 的调度设置目前位于统计页（`d55c1b82` 已解耦 stats 加载失败场景）；若后续迁出统计页，需同步改 13 的「查看学习统计」与 Q6。
- 组卷 PDF / DOCX 导出上线后，更新文档 11 Q6 与「组卷练习与导出」（当前为置灰 + 即将推出徽标）。
