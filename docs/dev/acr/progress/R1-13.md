# R1-13 — note 接线（建议模式 + dirty + 绑定）
- 状态：已完成（协调者回填；代码已落地，原进度报告缺失）
- 名下文件：
  - `src/features/learning-hub/apps/views/NoteContentView.tsx`
  - `src/features/notes/NotesCrepeEditor.tsx`
  - `src/features/workbench/agent/noteBinding.ts`
  - `src/features/workbench/agent/__tests__/noteDirtyRegistry.test.ts`
  - `src/features/workbench/agent/__tests__/noteBinding.test.ts`

## checklist
- [x] isDirty → contentDirtyRegistry
- [x] registerNoteEditor 挂载
- [x] noteBinding：活跃 note → canvasNoteId
- [x] suggestion 路径触发 canvas:ai-edit-request（driver 侧）

## 自验
- typecheck: PASS
- vitest noteDirtyRegistry + noteBinding: PASS

## 设计决策与偏差
1. canvasNoteId 写入路径以进度/代码为准（sessionManager 或 NotesContext）。

## 跨界申请
无。

## 遗留给 R2 的事项
1. dirty+replace → AIDiffPanel accept/reject 全链（R2-03）。
2. note_read 省略 noteId / watch 兜底（R2-03）。

## 新增 i18n keys
无
