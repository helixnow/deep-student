# R1-20 — 验收场景库 SCENARIOS.md

- 状态：已完成
- 名下文件：
  - 新建 `docs/dev/acr/SCENARIOS.md`
  - 新建 `docs/dev/acr/progress/R1-20.md`（本报告）
- **未改任何业务代码**（仅文档；发现的文档矛盾记于下文）

## checklist

- [x] 通读 `DESIGN.md` §2–§7、`ROUND1.md` 全部任务卡工具名/action、`STANDARDS.md` 验收约定
- [x] 核对已落地 skill：`workbench-tools.ts` 五工具 + app_command action 清单
- [x] 核对域工具名：`note_*` / `mindmap_edit_nodes` / `user_todo_*` / `qbank_*` / `chatanki_*` / `browser_*`
- [x] 核对 AgentOp kind（R1-03/R1-05/R1-11/R1-16 进度报告）
- [x] 产出 ≥35 条可执行场景（实产 **44** 条）
- [x] 覆盖：每应用主路径 / 跨应用≥3 / 仲裁 / 取消 / 降级 / 建议模式 / 撤销 / 性能
- [x] 每条含：编号 / 前置 / chat 指令 / 期望工具链 / 视觉 / 回执 / 判定
- [x] 禁改代码；矛盾写入本报告

## 自验

- typecheck: N/A（仅文档）
- vitest: N/A
- check:i18n: N/A
- 场景条数：44 ≥ 35
- 工具名抽检：与 ROUND1 R1-02/08 + `workbench-tools.ts` 一致

## 设计决策与偏差

1. **场景粒度**：主路径按「打开→定位→编辑→确认」拆条；跨应用给完整工具链而非单工具。
2. **exam `focusQuestion`**：DESIGN §5.4 有、R1-16 进度报告称可能未接线 → 单列 `S-APP-EXAM-02`，允许 `handled:false` + hint，禁止假成功。
3. **pomodoro**：桥/app_command action 用 `start|pause|resume|stop`；driver op kind 用 `pomodoro_*`（R1-16），场景中两者均写明。
4. **冒烟子集**：文末给出 R1 抽 5 / R3 抽 10，供协调者执行。

## 文档矛盾 / 对齐说明（未改代码）

| # | 矛盾点 | 处理 |
|---|--------|------|
| 1 | ROUND1 R1-16 写 content onActivation `scrollToAnchor`；DESIGN §5.2 / R1-12 / R1-08 skill / R1-16 进度报告对齐为 `scrollToHeading` | **SCENARIOS 采用 `scrollToHeading`**；在 §0.2 注明 |
| 2 | DESIGN §5.4 写 exam `focusQuestion`；ROUND1 R1-15 偏域事件刷新，R1-16 遗留「与 content onActivation 合并」 | 场景 `S-APP-EXAM-02` 标接线风险，回执允许 handled:false |
| 3 | DESIGN §3 表写 LLM 名 `builtin-workbench_*`；R1-02 can_handle 为 strip 后 `workbench_*` | 场景工具链写 LLM 可见全名；括号/叙述中可提短名 |
| 4 | DESIGN §2.3 `query_state` 行曾写「R1-19 provider」；实际 provider 归 R1-08 | 场景仍用 `builtin-workbench_query_state`，不依赖错误任务号 |
| 5 | mindmap dirty/hot 破坏性：R1-11 v1「拒绝 + suggestionPending、无 diff」vs 笔记 AIDiffPanel | 分列 `S-SUG-01/02`（笔记）与 `S-SUG-03`（导图 v1） |
| 6 | ROUND1 R1-04 文案 `source:"ai"`；DESIGN §5.6 冻结 `source: 'agent'\|'user'` | **未改代码**；场景叙述用 DESIGN 冻结值 `agent`；建议 R2 核对 executor emit |

## 跨界申请

无（纯文档卡）。若协调者要修 ROUND1 正文 `scrollToAnchor`→`scrollToHeading` 或 R1-04 `source` 字段，属文档勘误/R2，非本卡写权。

## 遗留给 R2 的事项

1. 按 SCENARIOS 冒烟抽检回填「代码可达 / 运行时失败」列（本卡只建基准表）。
2. `S-APP-EXAM-02` focusQuestion 接线确认。
3. R1-04 emit `source` 与 DESIGN §5.6 对齐核查。
4. 双窗第三路策略（排队 vs 拒绝）若实现与 `S-PERF-01` 文案不一致，以实现为准改场景判定句。

## 新增 i18n keys

无
