# R2-03 — 笔记链路区
- 状态：已完成
- 名下文件：
  - `src/features/workbench/agent/drivers/noteDriver.ts`（修改）
  - `src/features/workbench/agent/__tests__/noteDriver.test.ts`（修改）
  - `src/features/notes/hooks/useCanvasAIEditHandler.ts`（修改：ACR suggestion 回执 notify 降噪）
  - 走查确认未改（已就绪）：`agentHighlight.ts` / `CrepeEditor.agentInsert` / `NoteContentView` / `NotesCrepeEditor` / `noteBinding.ts` / `canvas_executor` 委托分支

## checklist
- [x] 打字机全链走查：委托→分批→decoration→渐隐→autosave→OCC
- [x] 批次间 `remapInsertPos`（读 agentHighlight caret）修用户他处打字 position mapping
- [x] 并发 insert 交错单测
- [x] 建议模式：dirty+replace/set → `canvas:ai-edit-request` → AIDiffPanel；clean 破坏类直写 `setMarkdown`
- [x] canvasNoteId 绑定 / note_read 省略 noteId / watch 兜底（代码走查确认）
- [x] typecheck + vitest 名下
- [x] 进度报告

## 自验
- typecheck: PASS
- vitest `noteDriver` + `noteBinding` + `noteDirtyRegistry`: **28 passed**
- check:i18n: 未改 i18n，跳过

## 设计决策与偏差
1. **破坏类门控**：按 DESIGN §1.1，仅 `isContentDirty('note', id)` 时走 suggestion；clean 窗 `note_replace`/`note_set` 直写 `setMarkdown`（触发 NotesCrepeEditor onChange→queueSave→OCC），账本记整篇还原。R1 曾对所有破坏类一律 suggestion，本轮纠正。
2. **打字机 remap**：每批 `agentInsert` 前 `remapInsertPos`；结束时用 highlight ranges 收紧账本区间，避免用户交错后 revert 删错段。
3. **Accept/Reject 回执**：ACR 路径工具已 `suggestionPending` 立即返回；`useCanvasAIEditHandler.sendResult` 仍 invoke `chat_v2_canvas_edit_result`（兼容旧 oneshot），失败降为 debug，不阻断面板。
4. **canvas_executor**：委托协商已落地（probe→apply_ops / closed→backend+dstu:change）；Rust 大改交 R2-01，本区未改 `.rs`。

## 走查结论（场景）
| 链路 | 结论 |
|------|------|
| S-APP-NOTE-02 打字机 | `agentInsert` addToHistory:false + insert meta；fadeRun→3s clearAll；onChange 非 programmatic → autosave |
| S-APP-NOTE-03 后端回落 | probe closed → Rust backend + `emit_note_updated_watch`；NoteContentView `dstu.watch` → `refreshFromDisk` |
| S-SUG-01/02 | dirty → CustomEvent → AIDiffPanel accept/reject |
| canvasNoteId | `setupNoteBinding` → modeState；skill 允许省略 note_id；executor `ctx.canvas_note_id` 兜底 |
| OCC | 前端委托不经后端写；后端回落走既有 `expected_updated_at`；watch 用 baseline 跳过自身保存 |

## 跨界申请
无（未改 mindmap/todo drivers、types.ts；未 cargo）。

## 遗留给 R2 / 协调者
1. 运行时冒烟：S-APP-NOTE-02/03、S-SUG-01/02（协调者）。
2. 若 R2-01 调整 apply_ops 超时/错误码，核对 note 回执 message 仍非静默降级。
3. hot 笔记追加暂停（S-SUG-04）属 R2-06 仲裁区；noteDriver probe 仍不报 hot。

## 新增 i18n keys
无
