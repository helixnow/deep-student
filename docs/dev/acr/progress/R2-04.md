# R2-04 — 列表应用区
- 状态：已完成
- 名下文件：
  - `src/features/workbench/agent/domainEvents.ts`（entityIds 归一 / dstu path 收集）
  - `src/features/workbench/agent/drivers/{todo,finder,fsrs,qbank}Driver.ts`
  - `src/features/workbench/core/workbenchBus.ts`（single activate 按 typeId；与 R2-10 ActivationResult 共存）
  - `src/features/chat/plugins/blocks/ankiCardsBlock.tsx`（收编双路径 → activate startReview）
  - `src/features/learning-hub/LearningHubSidebar.tsx`（inlineEdit 延迟 refresh）
  - `src/features/learning-hub/apps/views/ExamContentView.tsx`
  - `src/components/QuestionInlineEditor.tsx` / `QuestionBankEditor.tsx`（守卫 DOM 标记）
  - `src/features/chat/skills/builtin-tools/workbench-tools.ts`（action 清单补 focusQuestion）
  - `src/features/workbench/agent/__tests__/{domainEvents,todoFinderDriver,fsrsQbankDriver}.test.ts`
  - `docs/dev/acr/progress/R2-04.md`

## checklist
- [x] 域事件→flash：`entityIds` / `entity_ids` / dstu `path`/`node.id` 归一，与 `data-agent-entity="{typeId}:{id}"` 一致
- [x] 三守卫：todo 详情草稿延迟 reload；finder inlineEdit 延迟 silent refresh；qbank 答题/行内编辑延迟刷新并保 `currentQuestionId`
- [x] fsrs session `appendToQueue` 铁律保持；ankiCardsBlock 改走 `activate(startReview)` + fallbackLaunch
- [x] exam `focusQuestion`：R2-10 已在 `content/register.ts` 接线；本卡核对事件名与 ExamContentView 消费 + 技能清单
- [x] typecheck + vitest 名下测试

## 自验
- typecheck: BLOCKED（`noteDriver.ts` 中文串损坏，属 R2-03 名下；本卡未改该文件）
- vitest `domainEvents` + `todoFinderDriver` + `fsrsQbankDriver` + `agentFlash`: **27 passed / 4 files**

## 设计决策与偏差
1. `normalizeDomainPayload` + `collectDomainEntityIds` 统一 camelCase/`entity_ids`/dstu path，drivers 与 ExamContentView 共用。
2. finder 守卫由「永久跳过」改为「editingId 存在则 400ms 重试」。
3. `workbenchBus.activate`：single 按 `typeId` 匹配，保证 ankiCardsBlock `activate(startReview)` 命中已开窗。
4. ankiCardsBlock 不再直写 `fsrsReviewStore`；唯一写入口为 flashcards `onActivation(startReview)`。
5. exam `focusQuestion` 运行时由 R2-10 实现；本卡负责 flash/守卫/技能面与事件契约测试。

## 跨界申请
1. `noteDriver.ts` 编码损坏导致全仓 `tsc` 失败 → **R2-03** 修复。
2. `workbench-tools.ts` 补了 `focusQuestion`（若技能面写权在他区，可保留或回滚描述）。

## 遗留给 R2/R3 的事项
1. finder `reveal` 仍不进父目录（R1 遗留）。
2. 运行时 SCENARIOS 冒烟由协调者执行。

## 新增 i18n keys
无
