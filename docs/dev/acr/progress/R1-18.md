# R1-18 — 域事件订阅基建 + DevPanel 指示
- 状态：已完成（协调者回填；代码已落地，原进度报告缺失）
- 名下文件：
  - `src/features/workbench/agent/domainEvents.ts`
  - `src/features/workbench/components/WorkbenchDevPanel.tsx`（ACR 小节）
  - `src/features/workbench/agent/__tests__/domainEvents.test.ts`

## checklist
- [x] registerDomainListener via hubListen；dstu key extractor
- [x] 回执环形缓冲 / DevPanel 只读展示 presence + ledger 概要
- [x] 多 handler 注册/注销单测

## 自验
- typecheck: PASS
- vitest domainEvents: PASS（协调者修复 vi.hoisted mock TDZ）

## 设计决策与偏差
1. recordAcrReceiptSummary 供 StageManager 终态写入。

## 跨界申请
无。

## 遗留给 R2 的事项
1. DevPanel 指标与 DESIGN §7 性能场景读数对齐（R2-07）。

## 新增 i18n keys
无（DevPanel 开发向）
