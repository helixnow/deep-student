# R1-06 — StageManager + 仲裁 + 账本 + 挂载
- 状态：已完成（协调者回填；代码已落地，原进度报告缺失）
- 名下文件：
  - `src/features/workbench/agent/stageManager.ts`
  - `src/features/workbench/agent/arbitration.ts`
  - `src/features/workbench/agent/ledger.ts`
  - `src/features/workbench/agent/presenceStore.ts`
  - `src/features/workbench/components/WorkbenchDesktop.tsx`（AgentBridge 挂载 + stageManager.start/stop）
  - `src/features/workbench/index.ts`（末尾 export）
  - `src/features/workbench/agent/__tests__/stageManager.test.ts`

## checklist
- [x] StageManagerApi：probe / apply_ops / list_windows / open_app / app_command / close_window / query_state / revert_run
- [x] 租约互斥 `WINDOW_BUSY`；presence 写清
- [x] 仲裁：notifyUserInput → pausedByUser；checkPaused 2s resume / 15s abort
- [x] 账本 record/revertRun LRU 20
- [x] acr:bridge-cancel → abort
- [x] WorkbenchDesktop 挂载 AgentBridge + start/stop

## 自验
- typecheck: PASS（协调者 R1 统一验收）
- vitest agent 套件含 stageManager/arbitration/ledger：PASS
- check:i18n: N/A（本卡未改文案键，或仅 agent.core 占位）

## 设计决策与偏差
1. runId 贯穿 presence/ledger；与桥侧 `block_id` 对齐问题见 R1-01 遗留。
2. `disposeAllDrivers` 未在 stop 中调用（R1-16 遗留 → R2-06）。

## 跨界申请
无（回填时未发现未消化跨界）。

## 遗留给 R2 的事项
1. stop → disposeAllDrivers（R2-06）。
2. 全 driver checkPaused / userPatch 一致性（R2-06）。
3. 租约 TTL 心跳与 presence 泄漏自愈（R2-06）。

## 新增 i18n keys
- 见 workbench.json `agent.core.*`（与 R1-10 共用段）
