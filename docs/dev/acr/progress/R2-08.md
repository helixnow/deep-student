# R2-08 — 权限与降级区
- 状态：已完成
- 名下文件：
  - 新建 `docs/dev/acr/ERRORS.md`（与 R2-01 对齐；本卡定稿三档 + DISABLED/UNAVAILABLE）
  - 新建 `src/features/workbench/agent/gates.ts`
  - 新建 `src/features/workbench/agent/__tests__/gates.test.ts`
  - 修改 `src/features/workbench/agent/stageManager.ts`（off 闸、OS/control off abort、follow 聚焦、i18n 错误）
  - 修改 `src/features/workbench/agent/probe.ts`（control=off → disabled）
  - 修改 `src-tauri/src/chat_v2/tools/workbench_executor.rs`（FlagOff 硬闸 vs Off 只读）
  - 修改 `src/features/settings/components/WorkbenchSettingsSection.tsx`（offDesc 文案）
  - 修改 `src/locales/{zh-CN,en-US}/workbench.json`（`agent.errors.*` + offDesc）
  - 修改 `src/features/chat/skills/builtin-tools/workbench-tools.ts`（三档说明，面向 LLM）
  - 本报告 `docs/dev/acr/progress/R2-08.md`

## checklist
- [x] 三档定稿写入 ERRORS.md：off=list/query 只读；写与导航拒；background 不抢焦点；follow 自动聚焦
- [x] Feature flag 关 = 硬闸全拒（含 list/query）；与 setting off 区分
- [x] StageManager：off 拒 mutating 命令；open_app background 归还焦点 / follow focus
- [x] probe：OS 关或 control=off → `disabled`（域执行器后端直写）
- [x] OS `workbench:mode-changed` enabled=false → abort 活跃 run
- [x] control 热切到 off → abort 活跃 run
- [x] 用户可见闸门错误走 `workbench:agent.errors.*` i18n
- [x] 设置面 offDesc 与定稿一致
- [x] 禁 cargo / 未 commit

## 自验
- typecheck: PASS
- vitest `gates.test.ts` + `probe.test.ts` + `stageManager.test.ts`: 21 passed
- check:i18n: PASS（workbench.json 键一致 328；脚本对既有硬编码中文仅告警、exit 0）

## 设计决策与偏差
1. **ERRORS.md**：R2-01 若已写则对齐；本卡创建并定稿三档 + flag 硬闸语义（R2-01 曾把 flag 关等同 Off 只读——已改为 FlagOff 全拒）。
2. **桥侧 off**：`list_windows` / `query_state` / `probe` 允许；`apply_ops` 等在 StageManager 入口拒。Rust 工具面无 apply_ops，只覆盖 5 个导航工具。
3. **abort 不 clearActiveRun**：与 cancel 路径一致，等 apply finally 清租约，避免竞态。
4. **LLM 文案**：skill 描述不走 i18n（STANDARDS）；结构化 error message/hint 面向 LLM 可中文。

## 跨界申请
无阻塞项。R2-01 请确认 `ERRORS.md` 与 executor 的 FlagOff 分支一致（本卡已改 executor）。

## 遗留给 R3 / 协调者
- 运行时 SCENARIOS S-DEG-01/02 冒烟
- 全仓 vitest / lint（本卡未跑全量）

## 新增 i18n keys
- `workbench:agent.errors.controlOff|controlOffHint|osDisabled|osDisabledHint|launchFailed|launchFailedHint|abortedByControlOff|abortedByOsOff`
- 更新 `workbench:settings.agentControl.offDesc`（zh/en）
