# R3-02 — 性能压测与调优

- 状态：已完成（运行时 DevPanel 数值待协调者采样）
- 名下文件：
  - 新建 `docs/dev/acr/PERF-REPORT.md`
  - 新建 `docs/dev/acr/progress/R3-02.md`
  - `src/features/workbench/agent/visuals/agentFlash.ts`（与 R3-03 合流：`agentFlashMany` / scroll=auto）
  - `src/features/workbench/agent/visuals/agent-visuals.css`（flash → `::before`+opacity；保留 R3-03 token）
  - `src/features/workbench/agent/drivers/{todo,finder,fsrs,qbank}Driver.ts`（批量 flash）
  - `src/features/workbench/agent/drivers/mindmapDriver.ts`（`VIEWPORT_FOLLOW_EVERY=5`）
  - `src/features/workbench/components/WorkbenchDevPanel.tsx`（解包 `PerfSample.frame` + 挂载 start）
  - `src/features/workbench/agent/__tests__/{agentFlash,fsrsQbankDriver}.test.ts`

## checklist

- [x] 构造/走查四条压测路径（双窗、200 节点、5k 打字机、50 todo flash）
- [x] 代码审计 + 钩子填写 `PERF-REPORT.md`（运行时标「待协调者采样」+ 步骤）
- [x] 超预算项参数/实现级调优（禁架构重构；Channel 仍不实施）
- [x] typecheck + 相关 vitest
- [x] 本进度报告

## 自验

- typecheck: **PASS**
- vitest（相关）: **65 passed / 9 files**
  - `agentFlash.test.ts` (9)
  - `mindmapDriver.test.ts` (12)
  - `perfMonitor.test.ts` (10)
  - `pacing.spec.ts` (8)
  - `bridge.test.ts` (3) / `bridgeRouting.test.ts` (5)
  - `stageManager.test.ts` (6)
  - `todoFinderDriver.test.ts` (4)
  - `fsrsQbankDriver.test.ts` (8)
- cargo / tauri dev / commit: 未执行（按纪律）

## 设计决策与偏差

1. **运行时未测**：禁 `tauri dev`；PERF-REPORT §3 表格留给协调者按步骤填。
2. **与 R3-03 并行**：视觉层曾被 R3-03 改写（token / reduced-motion）；本卡保留其 token，仅把 flash 动画改回 §7 合规的 opacity，并接线 `agentFlashMany`。
3. **Channel**：承接 R2-07，触发条件未在审计中成立 → **仍不实施**（见 PERF-REPORT §5）。

## 调优摘要（问题→根因→修法）

| ID | 问题 | 根因 | 修法 |
|---|---|---|---|
| T1 | 50 todo flash 易掉帧 | N×smooth scroll + background 动画 | `agentFlashMany` 仅末项 `auto` scroll；`::before`+opacity |
| T2 | 200 节点 setCenter 过密 | 每 4 op 跟随 | `VIEWPORT_FOLLOW_EVERY` 4→5 |
| T3 | DevPanel 曲线可能空白 | 未解包 `sample.frame`；未 start | 解包嵌套字段；挂载 `startPerfMonitor` |

## 跨界申请

- 无额外跨界。`WorkbenchDevPanel.tsx` / drivers / visuals 为本卡压测调优必要范围；与 R3-03 共享 visuals 时以合流方式保留双方改动。

## 遗留给协调者

- 按 `PERF-REPORT.md` §2–§3 做 DevPanel 实测填表。
- INP 仪器化（P1）；笔记全文虚拟化（P2，既有）。

## 新增 i18n keys

- 无
