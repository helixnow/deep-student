# R1-11 — mindmap Driver（标杆）
- 状态：已完成（协调者回填；代码已落地，原进度报告缺失）
- 名下文件：
  - `src/features/workbench/agent/drivers/mindmapDriver.ts`
  - `src/features/workbench/apps/mindmap/register.ts`（onActivation）
  - mindmap 瞬态 `agentEnteringIds` / MindMapCanvas className 通道 / css
  - `src/features/workbench/agent/__tests__/mindmapDriver.test.ts`

## checklist
- [x] 逐 op：锚点 resolve → store mutation（skipHistory）→ ledger invert → focus → entering → pacer/checkPaused
- [x] destructive+dirty/hot → v1 拒绝式 suggestionPending
- [x] 单测真实 mindmapStore round-trip

## 自验
- typecheck: PASS
- vitest mindmapDriver: PASS

## 设计决策与偏差
1. dirty 场景 v1 直接拒绝并说明，无 diff 预览（R2-02 评估升级）。

## 跨界申请
无。

## 遗留给 R2 的事项
1. 视口跟随节流、大纲同步、双导图窗防御（R2-02）。
2. destructive+dirty 建议模式是否升级预览（R2-02）。

## 新增 i18n keys
- `workbench.agent.apps.mindmap.*`（若有）
