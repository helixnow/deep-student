# R2-06 — 仲裁一致性区
- 状态：已完成
- 名下文件：
  - `src/features/workbench/agent/userPatch.ts`（新建：summarizer / DEFAULT_USER_PATCH / withUserPatch）
  - `src/features/workbench/agent/inputProbe.ts`（新建：滚动/标题栏/Strip 过滤）
  - `src/features/workbench/agent/presenceStore.ts`（`isPresenceExpired` / `sweepExpired`）
  - `src/features/workbench/agent/stageManager.ts`（TTL 心跳常量导出、presence 泄漏自愈 sweep、`__healStalePresenceForTests`；既有 `disposeAllDrivers` 接线保留）
  - `src/features/workbench/agent/drivers/index.ts`（register summarizers；dispose 清 summarizer）
  - `src/features/workbench/agent/drivers/{mindmap,note,todo,finder,fsrs,qbank,pomodoro}Driver.ts`（abort/中止回执 `withUserPatch`）
  - `src/features/workbench/components/WindowShell.tsx`（输入探测接 `shouldNotifyAgentUserInput`）
  - `src/features/workbench/agent/visuals/AgentStrip.tsx`（既有 stopPropagation，未改行为）
  - `src/features/workbench/agent/__tests__/arbitrationConsistency.r2-06.test.ts`（新建矩阵）
  - `docs/dev/acr/progress/R2-06.md`（本报告）

## checklist
- [x] 全 driver 暂停/续放/中止一致性矩阵测试
- [x] userPatch：各 driver summarizer + 缺省「用户进行了手动编辑」
- [x] stageManager.stop → disposeAllDrivers
- [x] 租约互斥 WINDOW_BUSY；TTL 心跳续期；presence 泄漏自愈
- [x] 输入探测误报治理：滚动/标题栏/AgentStrip 不触发暂停
- [x] typecheck + vitest

## 自验
- typecheck: PASS（`npm run typecheck`）
- vitest `src/features/workbench/agent/__tests__/arbitrationConsistency.r2-06.test.ts`: **33 passed**
- check:i18n: N/A（未改 locale）

## 设计决策与偏差
1. **userPatch**：types.ts 只读，未扩 CollabDriver 接口；用旁路 `registerUserPatchSummarizer(typeId)` + `withUserPatch(receipt, typeId)`，abort/仲裁中止路径统一填充。
2. **真实 driver 矩阵**：单测用契约级 matrix driver 覆盖 7 个 typeId 的 pause/resume/abort + userPatch；真实 driver 已接线 `withUserPatch`，避免在矩阵里 mock 各域 store。
3. **输入探测**：滚轮不绑 listener；capture 内过滤 Strip/titlebar/中键/非编辑焦点滚动键；Strip 自身仍 `stopPropagation` 双保险。
4. **TTL 自愈**：`startPresenceSweep` 每 2s 调 `healStalePresence`；挂死活跃 run → abort+清租约；孤儿 presence → 直接 clear。

## 跨界申请
无（WindowShell 属 R1-10 遗留交 R2-06 收紧，任务卡明示范围）。

## 遗留给 R2/R3 的事项
1. 真实 driver 的细粒度 diff 概述（当前为 typeId 级静态文案；笔记/导图可后续接编辑器/store snapshot）。
2. AgentStrip 与工具卡 runId 撤销失效态（R2-05）。

## 新增 i18n keys
无
