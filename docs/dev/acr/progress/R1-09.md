# R1-09 — chat 侧 workbench_ops 工具卡
- 状态：已完成（协调者回填；代码已落地，原进度报告缺失）
- 名下文件：
  - `src/features/chat/plugins/events/toolCall.ts`（workbench_* → workbench_ops remap）
  - `src/features/chat/plugins/blocks/workbenchOpsBlock.tsx`
  - `src/features/chat/plugins/blocks/index.ts`
  - `src/features/chat/core/types/common.ts`（BlockType `'workbench_ops'`）
  - `src/locales/{zh-CN,en-US}/chatV2.json` `blocks.workbenchOps.*`

## checklist
- [x] remap：stripped toolName 以 `workbench_` 开头 → `workbench_ops`
- [x] 块：toolInput / content 步骤流 / AcrReceipt 展示；跳转 / 撤销
- [x] partial/cancelled 双列 done/undone
- [x] onAbort:`keep-content`；未动 TIMELINE_BLOCK_TYPES

## 自验
- typecheck: PASS
- vitest: 块渲染单测若未独立文件，由 R2-05 补齐
- check:i18n: 键已成对（协调者 R2-08 再跑全量 check:i18n）

## 设计决策与偏差
1. 撤销调 `stageManager.revertRun(block.toolCallId)`，依赖 runId===toolCallId（R1-01 遗留）。
2. 会话恢复后块渲染依赖 DB 持久化 `block_type=workbench_ops`（R2-05 定稿）。

## 跨界申请
无。

## 遗留给 R2 的事项
1. 重启恢复语义与 High 审批聚焦 chat（R2-05）。
2. 账本过期时撤销按钮失效态（R2-05）。
3. AgentTaskPanel 是否增列：评估后实施或否决（R2-05）。

## 新增 i18n keys
- `chatV2.blocks.workbenchOps.*`
