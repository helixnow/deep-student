# 合成学习笔记：能量与变化

第一段包含中文、English、数字 2026 和 emoji 🧪。标题元数据可另设为“F01 纯文本页面标题”，不与这个 H1 自动绑定。

**粗体**、*斜体*、~~删除线~~、`inline_code` 与 [普通链接](https://example.invalid/lesson?q=1&lang=zh)。

## 相同标题

这一段属于第一个章节。

## 相同标题

这一段属于第二个章节，不能因 heading 文本相同而混为同一块。

软换行从这里开始，
下一行仍属于同一段。

反斜杠硬换行在这里结束。\
这里应当换行。

字面转义：\*不是斜体\*，\[不是链接\]，路径文字 `a/b/c`。

---

F01-END：最后一段必须在保存和重新打开后保留。
