# O3 — 标题栏与三键微交互 · 进度

- 代理：O3
- 开始时间：2026-07-08
- 状态：部分完成（2026-07-09 由收尾核查回填）
- 写权文件：`components/WindowTitleBar.tsx`、新建 `components/WindowTitleBar.css`、`tests/vitest/workbench/window-titlebar.test.tsx`

## Checklist

- [ ] 三键 macOS 像素级对标：12px 直径 / 8px 间距（沿用 token）、真实符号 SVG（× / − / 展开-还原双三角）
  <!-- 未核实到：CSS 注释写明沿用 token 尺寸，但 WindowTitleBar.tsx 仍为空 button（无 SVG glyph / 无 wb-title-key / 未 import CSS） -->
- [ ] 逐键悬停微光（radial 高光 overlay，opacity 过渡）+ 各键 hover 色微调（color-mix 提亮）
  <!-- CSS 已写 `.wb-title-key::before` / color-mix hover；TSX 未挂 `wb-title-key` 类，样式未生效 -->
- [ ] 按压凹陷（scale 0.88 transform 过渡，叠加契约 brightness 变暗）
  <!-- 同上：仅 CSS，未接线 -->
- [ ] zoom 键符号随 displayMode 切换（floating=向外展开三角；maximized=向内还原三角）
  <!-- 未核实到：TSX 无 glyph / displayMode 仅传给 TileMenuPopover -->
- [ ] 标题栏双击 maximize 涟漪反馈（wb-title-ripple，transform+opacity keyframes，独立裁剪层）
  <!-- CSS 有 `.wb-title-ripple` / keyframes；TSX 无 ripple 层、无 import -->
- [ ] 长标题渐隐省略（溢出检测 → mask-image 渐隐替代硬截断）
  <!-- CSS 有 mask-image；TSX 仍用 `truncate`，未见溢出检测 / mask 类 -->
- [ ] 拖拽区 grab / grabbing 光标（覆盖契约 cursor:default，需声明）
  <!-- CSS 有 `[data-wb-title-draggable]` / `.wb-title-dragging`；TSX 未写这些属性/类 -->
- [ ] 焦点/非焦点标题色与权重过渡（wb-title-idle，opacity+font-weight 过渡）
  <!-- CSS 有相关规则；TSX 仍用 `opacity-90` 简陋降权，未见 wb-title-idle -->
- [ ] 尊重 prefers-reduced-motion 与 minimal 材质档（时长归零）
  <!-- 仅在未接线的 CSS 中存在 -->
- [ ] 现有 7 条 titlebar 测试保持全绿 + 新增微交互测试
  <!-- 未核实到新增微交互测试；`window-titlebar.test.tsx` 仍为 P3 基础行为 -->
- [ ] 自验：tsc 无新增错误；相关 vitest 全绿
  <!-- 本轮收尾未跑 tsc/vitest -->

## 已完成

- 新建 `components/WindowTitleBar.css`：含 grab/grabbing、按压 scale(0.88)、radial 悬停微光、
  color-mix hover、mask-image 渐隐、`wb-title-ripple` keyframes、reduced-motion / minimal 归零。
- `WindowTitleBar.tsx` 仍为 P3 基线（空 traffic 按钮 + truncate 标题 + 双击 onZoom），**未 import CSS、
  未挂 O3 类名/涟漪层/SVG 符号**——样式层交付、组件接线未完成。

## 视觉与交互决策（供人工复核）

- CSS 设计意图：用更高特异性 `.wb-titlebar.wb-title-bar…` 覆盖契约 cursor/background，
  新类一律 `wb-title-` 前缀；动画仅 transform/opacity。
- 运行时效果：因 TSX 未接线，当前桌面看不到 O3 微交互。

## 需 O20 reconcile 事项

1. 将 `WindowTitleBar.css` import 进 `WindowTitleBar.tsx`，并补齐 `wb-title-key` /
   glyph / ripple / mask / draggable 属性接线（或确认由后续代理完成）。
2. CSS 文件头已声明覆盖契约 cursor:default 与 traffic background —— 需 O20 确认与
   冻结 `workbench.css` 的共存策略。

## 新增 i18n keys

无新增（沿用既有 `workbench:window.close` / `minimize` / `zoom`）。
