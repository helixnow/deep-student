# O13 — 桌面画布 / 桌面手势 / 右键菜单

- 日期：2026-07-08
- 归属文件：`components/WorkbenchDesktop.tsx`（增量增强）、新建 `components/DesktopContextMenu.tsx` + `.css`
- 状态：已完成（2026-07-09 由收尾核查回填）

## Checklist

- [x] 桌面空白区右键菜单（新建 Chat / 资源库 / 整理窗口 / 平铺全部 / 显示桌面 / 俯瞰 / 壁纸 / 材质档 / 桌面设置）
- [x] 菜单键盘导航（↑↓ / ←→ 子菜单 / Enter / Esc）+ role/aria
- [x] 菜单玻璃材质 + 进场动画（仅 transform/opacity，尊重 reduced-motion / minimal 档）
- [x] 双击桌面空白 show desktop（最小化全部，再次双击恢复）
- [x] ResizeObserver rAF 合帧 + 尾随防抖 + 状态等值短路
- [x] 壁纸保持静止，不随鼠标或窗口移动
- [x] 自验：tsc 无新错误、相关 vitest 全绿、p11 冒烟保持绿
  <!-- 静态核实；本轮未重跑 vitest -->
- [x] 新增行为补测试
  <!-- `tests/vitest/workbench/o13-desktop-context-menu.test.tsx` 存在（菜单/平铺/双击/壁纸） -->

## 已完成

- `DesktopContextMenu.tsx` + `.css`：菜单项、子菜单、键盘导航。
- `WorkbenchDesktop.tsx`：挂载菜单、静态壁纸裁切层、ResizeObserver 合帧、双击/右键手势。
- o13 测试覆盖核心行为。

## 视觉与交互决策（供人工复核）

- 壁纸固定为静态背景，不提供视差设置。
- 菜单 z 介于 snap 与 DevPanel 之间（见 CSS 注释）。

## 需 O20 reconcile 事项

- 壁纸预设名走 `WALLPAPER_PRESETS[].nameKey`（O14 拥有预设清单）；材质档文案复用
  `workbench:settings.materialTier.*`。
- 确认桌面菜单 z 与 O1 刻度无冲突。

## 新增 i18n keys

| key | 兜底文案 |
|---|---|
| `workbench:desktopMenu.label` | 桌面菜单 |
| `workbench:desktopMenu.newChat` | 新建对话 |
| `workbench:desktopMenu.openFiles` | 打开资源库 |
| `workbench:desktopMenu.arrange` | 整理窗口 |
| `workbench:desktopMenu.tileAll` | 平铺全部窗口 |
| `workbench:desktopMenu.showDesktop` | 显示桌面 |
| `workbench:desktopMenu.restoreWindows` | 恢复窗口 |
| `workbench:desktopMenu.expose` | 窗口俯瞰 |
| `workbench:desktopMenu.wallpaper` | 桌面壁纸 |
| `workbench:desktopMenu.materialTier` | 视觉材质 |
| `workbench:desktopMenu.settings` | 桌面设置… |

另复用（非 O13 新增字面量来源，但菜单内调用）：
`workbench:settings.materialTier.auto/full/reduced/minimal`；
壁纸项 `t(preset.nameKey, preset.id)`（nameKey 属 O14 预设表）。
