# O5 — Dock 放大与动效（macOS Dock 灵魂）

- 归属文件：`components/Dock.tsx`、`components/DockItem.tsx`、新建 `components/Dock.css`、
  新建 `components/dockGeometry.ts`、相关测试
- 状态：已完成（2026-07-09 由收尾核查回填）

## Checklist

- [x] `dockGeometry.ts`：typeId → 图标屏幕坐标 get/subscribe provider（O9 genie 消费）
- [x] 邻近放大 magnification：高斯衰减 + 相邻联动 + 进出 strength 渐变（rAF 直写 DOM，不进 state）
- [x] launch bounce：窗口数 0→1 触发两段递减弹跳（transform-only keyframes）
- [x] 运行指示点：淡入 + 呼吸（opacity/transform keyframes）
- [x] hover tooltip：玻璃气泡带箭头（wb-glass + wb-dock-tip），hover/focus-within 显示
- [x] autohide：O1 overshoot 曲线滑入 / 标准曲线滑出，translate(-50%, y) 复合形式
- [x] reduced-motion / minimal 材质档全部动效降级
- [x] tsc 无新增错误；Dock 既有测试全绿；新增 O5 测试全绿
  <!-- 代码侧交付齐全；既有 `Dock.test.tsx` 覆盖 autohide 等。未找到独立 o5-*.test 文件；
       magnification / dockGeometry 专项断言未在本轮逐条跑通，仅静态核实实现存在 -->

## 已完成

- `dockGeometry.ts`：`getDockIconRect` / `getDockIconCenter` / `subscribeDockGeometry`。
- `Dock.tsx`：magnification 引擎（rAF 直写 `data-wb-dock-mag-item`）、geometry 发布、autohide。
- `DockItem.tsx`：launch bounce、`wb-dock-tip` tooltip、指示点层。
- `Dock.css`：mag / tip / bounce / breathe / slide + reduced-motion / minimal 降级。

## 视觉与交互决策（供人工复核）

- 放大不进 React state，避免高频 setState；bounce 与 mag 分层（`.wb-dock-bounce` vs mag 内层）。
- autohide 用 `wb-dock-slide` 复合 `translate(-50%, y)`，与 P4 居中契约对齐。

## 需 O20 reconcile 事项

- Dock 既有测试未覆盖 magnification 数值/geometry 发布；若门禁要求专项用例，需补测。
- genie（O9）消费 `subscribeDockGeometry` 的接线属 O9/O20 范围。

## 新增 i18n keys

无（Dock 文案沿用既有 app 标题 / aria；本轮未在 O5 名下文件发现新增 `t('workbench:…')`）。
