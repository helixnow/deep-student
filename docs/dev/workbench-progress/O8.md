# O8 — 窗口切换器（Ctrl+Tab）极致打磨

- 状态：已完成（2026-07-09 由收尾核查回填）
- 文件（唯一写权）：`components/WindowSwitcher.tsx`、新建 `components/WindowSwitcher.css`、
  新建 `components/__tests__/WindowSwitcher.test.tsx`

## Checklist

- [x] 中央玻璃条：wb-glass 契约类 + 图标 + 选中标题 + hint（复用现有 i18n key）
- [x] 玻璃焦点框在图标间平滑滑动（单元素 transform 过渡，直写 DOM 不进 state）
- [x] 选中项放大高亮（tile scale，transform-only）
- [x] 快速连按流畅（CSS transition 中途重定向 + 关闭中重开会话恢复）
- [x] 松开聚焦有反馈（commit 脉冲 + 区分 commit/cancel 的退出动画）
- [x] 进出动画（enter scale+fade / exit 两种变体，animationend + 超时兜底卸载）
- [x] 大量窗口：换行 + 三行封顶纵向滚动 + 选中项 scrollIntoView
- [x] 可选窗口内容缩略（`thumbnails` prop，占位式迷你窗口卡，默认关闭）
- [x] reduced-motion / minimal 材质档动效归零
- [x] z-index 走 O1 刻度变量（带回退），去除内联硬编码
- [x] tsc 无自己引入错误；vitest 全绿
  <!-- 静态核实实现与 `WindowSwitcher.test.tsx` 存在；本轮未重跑 vitest -->

## 已完成

- `WindowSwitcher.tsx` 已 `import './WindowSwitcher.css'`；`thumbnails` prop、frame 直写 transform、
  `scrollIntoView`、commit/cancel 退出相位。
- `WindowSwitcher.css`：`z-index: var(--wb-z-switcher, 2147483200)`；进出/commit keyframes；
  三行封顶；minimal / prefers-reduced-motion 时长归零。
- 组件测试覆盖 frame 滑动、thumbnails、退出动画等。

## 视觉与交互决策（供人工复核）

- 焦点框为单一 `.wb-switcher-frame` 元素，靠 transform 滑动而非逐项边框切换。
- 缩略模式默认关闭，开启后换迷你窗口卡尺寸变量。

## 需 O20 reconcile 事项

- `--wb-z-switcher` 回退值仍为历史巨数 `2147483200`，与 O1 刻度表对齐由 O20 确认。
- `WorkbenchDesktop` 已挂 `<WindowSwitcher />`（装配侧已接线）。

## 新增 i18n keys

无新增（标题/hint 复用既有 switcher 文案路径；本轮未在 O8 文件发现新 `t('workbench:…')` 字面量）。
