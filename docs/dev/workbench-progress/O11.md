# O11 — 状态机 / 快照体验 / 契约扩展

- 代理：O11（core/windowStore.ts、core/snapshot.ts、core/types.ts 仅追加、相关测试）
- 状态：已完成（2026-07-09 由收尾核查回填）
- 铁律自检：只写名下文件；types.ts 只追加可选字段/新导出；focusStack/zIndex 不变量与快照白名单纯净性保持；不启动 dev server；不 commit。

## Checklist

- [x] types.ts 追加：`WindowTransientPhase`、`WorkbenchStoreState.transientPhases?/setWindowTransient?`、`WorkbenchSnapshotV1.desktopSize?`
- [x] focusWindow：已聚焦窗口 no-op 短路（消除点击焦点窗时的 zIndex 跳变/重渲染闪烁）
- [x] zIndex 过渡分配：zTop 超阈值时同一次 set 内紧凑重排（相对序不变 → 无视觉跳变）
- [x] 瞬态生命周期标记（O9 动画消费）：open→'opening'、unminimize→'restoring'、显式 set/clear、close/minimize 自动清理、hydrate/reset 清空
- [x] cascade 落位感知已有窗分布：优先空槽位，避免与现有 floating 窗完全重叠；全满时轮转回卷
- [x] hydrate 分辨率自适应恢复：按快照 desktopSize→当前 desktopSize 比例缩放位置、超尺寸窗等比收缩（respect minSize）、钳回可视区（frame 与 restoreFrame 同步处理）
- [x] hydrate 首帧只完整渲染焦点窗（初始 lifecycles：栈顶 focused、其余 background）
- [x] 恢复逐帧唤醒：post-hydrate 钩子驱动 rAF 队列逐帧提升 background→visible，完成后 recomputeLifecycles 收敛占用/冻结真值；窗口集合变化即让位给 scheduler
- [x] 快照分层保存：saveSnapshot('layout'|'meta')，layout 2s 尾随防抖（现状不变），meta 10s 首次请求优先；layout 待写时 meta 搭车
- [x] 落盘去重：内容未变的防抖保存跳过写盘（flush 强制写）
- [x] 快照版本迁移健壮：version 缺失/未来版本 → 白名单 best-effort 迁移 + warn，仅结构性坏数据拒绝
- [x] 快照记录 desktopSize（白名单新字段，sanitizer 校验）
- [x] 更新受影响旧测试 + 补新行为测试
- [x] tsc 无自己引入错误；windowStore/snapshot/scheduler/occlusion 相关 vitest 全绿
  <!-- 静态核实测试覆盖 O11 行为；本轮未重跑 vitest -->

## 已完成

- `types.ts`：`WindowTransientPhase`、`transientPhases` / `setWindowTransient`、快照 `desktopSize?`。
- `windowStore.ts`：focus no-op、`maybeCompactZ`、cascade 空槽、瞬态标记、hydrate 自适应与分层初值。
- `snapshot.ts`：分层 `saveSnapshot`、内容去重、desktopSize 停放、post-hydrate 逐帧唤醒钩子。
- 测试：`windowStore.test.ts` / `snapshot.test.ts` 含 O11 专条。

## 视觉与交互决策（供人工复核）

- 瞬态相位不进快照白名单；动画结束由 O9 清标记，close/minimize/hydrate 兜底清理。
- 首帧只 focused，其余 background，再由 rAF 唤醒，避免恢复瞬间全量解冻。

## 需 O20 reconcile 事项

- O9 genie / 开窗动画需消费 `transientPhases` + `setWindowTransient`（若 O9 未完成则仍为半成品链路）。
- `WINDOW_Z_COMPACT_THRESHOLD` 与 O1 z 刻度并存，O20 确认无视觉跳变即可。

## 新增 i18n keys

无（纯内核模块）。
