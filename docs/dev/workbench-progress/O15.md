# O15 — 诊断 HUD / 性能可视化

- 状态：已完成（2026-07-09 由收尾核查回填）
- 开始时间：2026-07-08 22:15
- 写权文件：`src/features/workbench/components/WorkbenchDevPanel.tsx`、新建 `WorkbenchDevPanel.css`

## 任务 Checklist

- [x] 新建 `WorkbenchDevPanel.css`（全部新类 `wb-hud-` 前缀，引用 O1 token/曲线，reduced-motion / minimal 档降级）
- [x] 实时帧耗时曲线图（SVG polyline，右侧对齐滚动，16.7/33.3ms 参考线）
- [x] 掉帧标记（≥33.4ms 判定，曲线顶部红色 tick）+ FPS / 平均 / 峰值 / 掉帧数 / 长任务 chips
- [x] 生命周期分布（堆叠条 + 四档图例计数，沿用 P10 四色）
- [x] 内存预算占用条（scaleX 动画，超限变红）——保留 `X / Y` 文案契约
- [x] 焦点栈 / 快照保存时间 / 活动窗口列表（lifecycle 着色）——保留 P10 测试 DOM 契约
- [x] 玻璃 HUD 可拖动（标题栏 pointer capture，直写 DOM transform，rAF 合帧，不进 state，视口钳制，will-change 拖前加拖后移）
- [x] 可折叠（按钮 + 双击标题栏，折叠态显示 fps/窗口数 mini 摘要），位置/折叠态 localStorage 持久化
- [x] 不遮挡操作：外部 pointer 按住期间 HUD 自动降透明 + pointer-events:none（wb-hud-ghost）
- [x] perfMonitor 消费（O10 未就绪 → 防御性三级数据源：全局桥 / 运行时动态 import / 本地 rAF+longtask 兜底）
  <!-- O10 `perfMonitor.ts` 现已存在；O15 仍保留三级探测，可无缝接管 -->
- [x] 自验：tsc 无自己引入错误；`workbenchDevPanel.test.tsx` 等相关 vitest 全绿
  <!-- `tests/vitest/workbench/workbenchDevPanel.test.tsx` 存在；本轮未重跑 -->

## perfMonitor 依赖（编码时预告；现已落地）

O15 仍按三级探测消费（见组件注释）。O10 已提供 `subscribePerfMonitor` / `startPerfMonitor`。

## 已完成

- `WorkbenchDevPanel.css`：`wb-hud-*` 全套样式。
- `WorkbenchDevPanel.tsx`：曲线/掉帧/生命周期/预算/焦点栈/拖动/折叠/ghost/perf 三级源。

## 视觉与交互决策（供人工复核）

- 拖动直写 transform，不进 React state；折叠与位置写 localStorage。
- 外部 pointer 按下时 `wb-hud-ghost` 降透明并禁指针，避免挡操作。

## 需 O20 reconcile 事项

- 可评估去掉动态 import 兜底、改为静态依赖 O10（降低复杂度）。
- HUD z 与桌面菜单/cheatsheet 层序由 O20 统一。

## 新增 i18n keys

| key | 兜底文案 |
|---|---|
| `workbench:devPanel.title` | Workbench 诊断 |
| `workbench:devPanel.expand` | 展开面板 |
| `workbench:devPanel.collapse` | 折叠面板 |
| `workbench:devPanel.close` | 关闭面板 |
| `workbench:devPanel.frameTime` | 帧耗时 |
| `workbench:devPanel.avg` | 平均 |
| `workbench:devPanel.max` | 峰值 |
| `workbench:devPanel.sampling` | 采样中… |
| `workbench:devPanel.dropped` | 掉帧 |
| `workbench:devPanel.longTasks` | 长任务 |
| `workbench:devPanel.source` | 数据源 |
| `workbench:devPanel.distribution` | 生命周期分布 |
| `workbench:devPanel.windows` | 窗口 |
| `workbench:devPanel.noWindows` | 暂无窗口 |
| `workbench:devPanel.minimizedTag` | 最小化 |
| `workbench:devPanel.budget` | 内存预算 |
| `workbench:devPanel.focusStack` | 焦点栈 |
| `workbench:devPanel.emptyFocusStack` | （空） |
| `workbench:devPanel.lastSnapshot` | 快照最后保存 |
| `workbench:devPanel.never` | 尚未保存 |

动态 key（fallback 为英文枚举字面量）：
`workbench:devPanel.lifecycle.focused|visible|background|frozen`。
