# O10 — 调度器 / 遮挡 / 性能监控 进度

- 代理：O10
- 开始：2026-07-08 / 完成：已完成（2026-07-09 由收尾核查回填）
- 关联：`docs/dev/learning-os-20-agent-polish-plan.md` §2 O10、设计文档 §5
- 名下文件：`core/scheduler.ts`、`core/occlusion.ts`、新建 `core/perfMonitor.ts`、相关测试

## 任务 checklist

- [x] 1. `occlusion.ts` 增量化：`createOcclusionCache` + `computeOcclusionIncremental`（只重算受影响窗口）、`computeOcclusionDetail`（visibleRatio 可见比例）；`computeOcclusion` 冻结签名与语义不变
- [x] 2. `scheduler.ts` 生命周期降频精细化：visible 分完全可见/部分可见（render hints）、焦点切换平滑过渡（defocus 宽限）、滚动/流式动态降频（reportSchedulerActivity）
- [x] 3. `scheduler.ts` 预算冻结平滑：即将冻结宽限（FREEZE_GRACE_MS + freezeImminent 标记）、唤醒预取（requestWakePrefetch）；四档判定与 focused/visible 永不冻结不变量保持
- [x] 4. 新建 `perfMonitor.ts`：帧耗时/长任务/掉帧/生命周期窗口分布/内存预算占用采集，订阅接口供 O15，开发期开关（startPerfMonitor/stopPerfMonitor）
- [x] 5. 测试：occlusion 增量一致性（含随机化等价）、scheduler 新行为（宽限/预取/hints）、perfMonitor 采样；原有用例全数保留（预算用例统一 `setFreezeGraceOverride(0)` 保持既有语义）
- [x] 6. 自验：`npx tsc --noEmit` 无自引错误；相关 vitest 全绿
  <!-- 静态核实测试文件与实现齐全；本轮收尾未重跑 tsc/vitest -->

## 已完成

- `occlusion.ts`：`createOcclusionCache` / `computeOcclusionDetail` / `computeOcclusionIncremental`；
  `visibleRatio` 明细；既有 `computeOcclusion` 保留。
- `scheduler.ts`：`FREEZE_GRACE_MS`、`freezeImminent`、`reportSchedulerActivity`、`requestWakePrefetch`；
  内部走增量遮挡；render hints 含 `visibleRatio`。
- `perfMonitor.ts`：`startPerfMonitor` / `subscribePerfMonitor` 等。
- 测试：`occlusion.test.ts`（增量/随机等价）、`scheduler.test.ts`（宽限/预取/activity）、
  `perfMonitor.test.ts`。

## 视觉与交互决策（供人工复核）

- 无直接 UI；冻结宽限默认 2500ms，测试可 `setFreezeGraceOverride(0)` 保持旧语义。

## 需 O20 reconcile 事项

- O15 已防御性消费 perfMonitor（全局桥 / 动态 import / 本地兜底）；O20 可确认是否注册
  `globalThis.__WB_PERF_MONITOR__` 或改为静态 import。

## 新增 i18n keys

无（纯内核模块）。
