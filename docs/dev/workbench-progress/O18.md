# O18 — 系统 / 沙箱窗口体验 + 投射呈现 进度

- 代理：O18
- 开始：2026-07-08 ／ 完成：已完成（2026-07-09 由收尾核查回填）
- 范围：`src/features/workbench/apps/system/**`、`src/features/workbench/apps/sandbox/**` + 相关测试
- 铁律遵守：不改 legacy 页面组件；`projection.ts`/`eventHub.ts`/bus 只消费不改；新类全部 `wb-sys-` / `wb-sandbox-` 前缀；动画仅 transform/opacity；尊重 reduced-motion 与 minimal 材质档。

## Checklist

- [x] 1. 共享基建：`useWbSysSize.ts`（ResizeObserver 窗口尺寸分级 wide/medium/compact，jsdom 无 RO 时安全兜底）
- [x] 2. 共享基建：`SystemWindowShared.tsx` + `SystemWindowShared.css`（`WbSysSkeleton` 变体骨架屏、`WbSysSidebarLayout` 窄窗抽屉侧栏、`WbSysActivityStrip` 任务进行中顶部活动条、内容淡入）
  <!-- 核实到 skeleton 变体：sidebar / list / dashboard / surface（四变体；checklist 写「五变体」未在代码中见到第五种） -->
- [x] 3. `TodoAppWindow`：legacy 页面 lazy 化 + 骨架屏；窄窗（<640）侧栏收纳为玻璃抽屉
- [x] 4. `SettingsAppWindow`：lazy + 骨架屏；中窗侧栏收窄、极窄窗抽屉兜底
- [x] 5. `SkillsAppWindow` / `TemplatesAppWindow`：lazy + 骨架屏 + 尺寸分级 data 属性 + 内容淡入
- [x] 6. `TaskDashboardAppWindow`：lazy + 骨架屏；标题实时显示进行中任务数；任务进行中顶部活动条
- [x] 7. `ankiTaskSource.ts`：追加 `subscribeAnkiTaskCount()` 导出（复用现有 listeners/watcher 机制，不动投射源行为）
- [x] 8. `PomodoroAppWindow` + `PomodoroAppWindow.css`：精致计时视觉（SVG 进度环 + 大字计时 + 模式/任务/暂停徽章 + 运行光晕），legacy PomodoroPanel 作为底部控制坞；标题带模式语义
- [x] 9. `SandboxAppWindow` + `SandboxAppWindow.css`：lazy + 骨架屏；非焦点窗 iframe 焦点守卫层（首次点击先聚焦窗口，不落入 iframe）
- [x] 10. 测试：`tests/vitest/workbench/o18-system-windows.test.tsx`（骨架/抽屉/番茄钟视觉状态/任务角标标题/沙箱焦点守卫/尺寸分级）
- [x] 11. 自验：`npx tsc --noEmit` 无自己引入错误；o18 + p9 + i18n parity 测试全绿
  <!-- 静态核实 o18 测试存在；本轮未重跑 tsc/vitest -->

## 已完成

- 共享：`useWbSysSize`、`SystemWindowShared`（Skeleton/SidebarLayout/ActivityStrip/Fade）+ CSS。
- 系统窗：Todo/Settings/Skills/Templates/TaskDashboard/Pomodoro 均 lazy + 尺寸分级。
- `subscribeAnkiTaskCount`；沙箱 `wb-sandbox-focus-guard`。
- `o18-system-windows.test.tsx`。

## 视觉与交互决策（供人工复核）

- 窄窗侧栏改为抽屉（始终挂载、visibility 隐藏），避免 lazy 侧栏二次加载跳变。
- 番茄钟模式色沿用 legacy 映射；运行光晕尊重 reduced-motion / minimal。

## 需 O20 reconcile 事项

- Skeleton 实际四变体 vs 计划「五变体」——若需第五形态由 O20 补或改文档口径。
- Todo/Settings 等文件工作区可能仍有并行改动；以最终合并态再跑 o18 门禁。

## 新增 i18n keys

| key | 兜底文案 |
|---|---|
| `workbench:window.loading` | 加载中… |
| `workbench:apps.system.showNav` | 显示导航 |
| `workbench:apps.system.hideNav` | 收起导航 |
| `workbench:apps.system.todoNav` | 待办导航 |
| `workbench:apps.system.settingsNav` | 设置导航 |
| `workbench:apps.system.tasksRunning` | {{count}} 个制卡任务进行中 |
| `workbench:apps.system.paused` | 已暂停 |
| `workbench:apps.system.idleHint` | 在下方开始一段专注 |

沿用既有 app 标题 key（非本轮新造，但窗口内调用）：
`workbench:apps.todo/settings/skills/templates/taskDashboard/pomodoro/sandbox`。

番茄钟模式文案走 `todo:` namespace（非 workbench）：
`todo:pomodoro.modes.focusing/shortBreak/longBreak/idle`。
