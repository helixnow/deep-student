# O14 — 壁纸系统 / 空桌面（极致打磨轮）

- 日期：2026-07-08
- 范围（唯一写权）：`components/WallpaperLayer.tsx`、`components/EmptyDesktop.tsx`、
  新建 `components/WallpaperLayer.css`、`components/EmptyDesktop.css`、
  新建测试 `components/__tests__/WallpaperLayer.test.tsx`、`components/__tests__/EmptyDesktop.test.tsx`
- 只读依赖：`styles/workbench.css`（wb-wallpaper*/wb-empty* 基线，冻结）、
  `styles/workbench.tokens.css`（--wb-wallpaper-* 预设变量，O1 拥有，仅引用不改）
- 状态：已完成（2026-07-09 由收尾核查回填）

## Checklist

- [x] 壁纸预设扩充至 ≥6 套（明暗各调），新预设渐变承载在自有 `wb-wallpaper-` 前缀类
  <!-- WALLPAPER_PRESETS：aurora/horizon/graphite + meadow/lagoon/dusk/sakura/sand/nebula + 两套 flow ≥6 -->
- [x] 动态/渐变流动壁纸（纯 transform/opacity CSS 动画，reduced-motion / minimal 档关闭）
- [x] 自定义图片适配层：模糊（可配）/ 暗角 / 亮度压暗（可配），minimal 档关闭
- [x] 壁纸切换淡入淡出过渡（双 pane 交叉淡入，animationend + 超时兜底回收）
- [x] 空桌面引导升级：插画组合 + 光晕 + 浮动动画
- [x] 引导动作卡片（资源库 / 新建对话 / 待办，走 workbenchBus.launch）
- [x] 首次使用轻量 onboarding（技巧列表 + 「知道了」，localStorage 记忆）
- [x] tsc 无自己引入错误
  <!-- 静态核实；本轮未跑 tsc -->
- [x] 相关 vitest 全绿
  <!-- `WallpaperLayer.test.tsx` / `EmptyDesktop.test.tsx` 均存在；本轮未重跑 -->

## 已完成

- `WallpaperLayer.tsx` + `.css`：预设表、flow 层、图片 blur/dim/vignette、双 pane 交叉淡入。
- `EmptyDesktop.tsx` + `.css`：场景插画、动作卡、onboarding + `EMPTY_DESKTOP_ONBOARDING_KEY`。
- 两份组件测试覆盖预设/过渡/空桌面行为。

## 视觉与交互决策（供人工复核）

- 新静态预设渐变放在自有 CSS 类，不改 O1 token 文件。
- 壁纸 nameKey 无独立 nameFallback 常量；菜单侧用 `t(nameKey, preset.id)` 兜底为 id。

## 需 O20 reconcile 事项

- 确认 `wb-wallpaper-*` 新类与冻结 `workbench.css` 基线类叠加无冲突。
- O13 菜单消费同一 `WALLPAPER_PRESETS` 导出。

## 新增 i18n keys

EmptyDesktop 直接 `t(...)`：

| key | 兜底文案 |
|---|---|
| `workbench:emptyDesktop.title` | 你的学习桌面 |
| `workbench:emptyDesktop.hint` | 从下方 Dock 打开应用开始工作——窗口可以自由摆放、平铺与并排对比。 |
| `workbench:emptyDesktop.actionsLabel` | 快速开始 |
| `workbench:emptyDesktop.actionFiles` | 打开资源库 |
| `workbench:emptyDesktop.actionFilesDesc` | 浏览笔记、教材与全部资源 |
| `workbench:emptyDesktop.actionChat` | 新建对话 |
| `workbench:emptyDesktop.actionChatDesc` | 开一个 AI 对话窗口 |
| `workbench:emptyDesktop.actionTodo` | 查看待办 |
| `workbench:emptyDesktop.actionTodoDesc` | 安排今天的学习任务 |
| `workbench:emptyDesktop.tipsTitle` | 小技巧 |
| `workbench:emptyDesktop.tipsDismiss` | 知道了 |
| `workbench:emptyDesktop.tipTile` | 把窗口拖到屏幕边缘，松手即可平铺 |
| `workbench:emptyDesktop.tipSwitch` | 在窗口间快速切换 |
| `workbench:emptyDesktop.tipExpose` | 俯瞰所有打开的窗口 |

WallpaperLayer 预设 nameKey（无内联中文兜底，消费方用 id 或 locale）：
`workbench:wallpaper.aurora/horizon/graphite/meadow/lagoon/dusk/sakura/sand/nebula/auroraFlow/duskFlow`。
