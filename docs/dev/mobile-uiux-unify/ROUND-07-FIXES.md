# Round 7 落地（claude-fable-5-thinking-xhigh）

## 已修

- `shad/Popover` 打开时 overlay 返回键关闭（覆盖模型选择、笔记格式溢出、改期、倍速、题号跳转等）
- chat 右屏沙箱 `hideToolbar` + 顶栏刷新/检查器；资源预览「在学习中心打开」进顶栏
- PDF 保活浮层返回加可见性守卫；沙箱 Inspector 接返回
- 分组编辑器小屏不再双保存
- Settings Sheet 推迟 pointer capture，避免吞掉 header 按钮 click
- Todo/笔记标签建议 listbox 接返回

## 仍开（Round 8+）

- 导图子屏双 chrome / 工具条 40px
- 题库统计热力图触屏、图表 28px 钮
- Settings Sheet 安全区死区、view 级返回被 Radix 抢先
