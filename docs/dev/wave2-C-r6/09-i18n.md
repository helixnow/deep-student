# 0824 Wave2-C R6 复核报告 · 09 i18n

- 复核员：第 6 轮复核员-i18n（claude-fable-5-thinking-high）
- 复核对象：`tests/vitest/mobile-uiux/i18nKeyExtract.ts`、`tests/vitest/mobile-uiux/inputBarSplitI18nKeys.contract.test.ts`、`scripts/check-i18n.mjs`
- 工作目录：`/tmp/0824-wave2-c-r6-i18n`
- 约束遵守：未执行 node/vitest，未 git commit。验证全部通过静态阅读 + Python 复刻模拟 + tree-sitter/esprima 解析完成。

## 结论：PASS（无阻断问题；`check-i18n.mjs` 语法有效，未做修改）

---

## 1. 复核方法

因禁止执行 node/vitest，采用三种替代手段：

1. **Python 复刻提取器**：逐行对照 `i18nKeyExtract.ts` 的三条正则（LITERAL_NAMESPACED_KEY / LITERAL_BARE_KEY / TEMPLATE_KEY）、模板骨架规格化、枚举展开注册表与严格叶子解析 `resolveKeyToText`，在真实源码与 locale 上完整模拟契约测试全部断言。
2. **语法验证**：`check-i18n.mjs` 先用 esprima（失败——系 esprima 不支持 shebang 与 `import.meta`，为解析器年代问题而非脚本缺陷），改用 tree-sitter-javascript 全树解析，**无 ERROR / MISSING 节点**；两个 TS 文件用 tree-sitter-typescript 解析同样无错误节点。
3. **产品声明逐项比对**：drift 断言涉及的 5 组枚举与产品源码逐一核对。

## 2. 契约测试全断言模拟结果（全部通过）

| 断言 | 模拟结果 |
|---|---|
| unexpandedTemplates 为空 | ✅ 空（12 个扫描文件全部存在） |
| 键数 > 120（anti-rot） | ✅ 提取 **187** 个键，与测试注释「当前 187 个」精确一致 |
| 模板键归因宿主文件（6 组抽查） | ✅ 全部命中（uploadStage.creating→AttachmentPanelBody、shortHints.danger_full_access→ComposerPlusMenu、injectMode.pdf.ocr→useInputBarV2、thinkingDepth.max / compaction.reason.unknown→InputBarV2、common.clearSearch→ComposerPanel） |
| 187 键 × zh-CN/en-US 严格叶子解析 | ✅ **0 缺失**（叶子均为非空字符串） |
| KNOWN_UNRESOLVED_KEYS 自清理 | ✅ 表为空 `{}`，与「R5 已补 thinkingDepth.minimal」注释一致，断言空转通过 |

## 3. 专项核对

### 3.1 叶子必须非空字符串

`resolveKeyToText`（i18nKeyExtract.ts L155-170）逐段下钻，中途遇非对象/数组/缺段返回 false，终点要求 `typeof cursor === 'string' && cursor.trim().length > 0`。✅ 正确修复了旧实现「键指向子树误判可解析」的缺陷；空串、打到中间对象均判失败。

### 3.2 actions.more alias（官方裁决 · Step 21）

- 组件侧：`AttachmentPanelBody.tsx` L125 `aria-label={t('common:more', { defaultValue: 'More' })}`，L166/L204 `common:actions.close`；文件内**无** `actions.more` 引用。✅
- locale 侧：`common.json` 双语 `more`（更多/More）、`actions.more`（更多/More）、`actions.close`（关闭/Close）均为非空字符串。✅ alias 词条保留且双语可解析。
- 与 `src/__tests__/releaseUpgradeI18n.test.ts` 口径一致：该测试 L60-61 声明 `keys: ['common:more']` + `removedKeys: ['common:actions.more']`（锁组件不得引用），本契约锁 locale 词条不得删——两把锁互补无冲突。✅

### 3.3 thinkingDepth.minimal 已补

`chatV2.json` 双语 `inputBar.thinkingDepth.{minimal,low,medium,high,xhigh,max}` 六键齐全（zh 最低/低/中/高/超高/超高；en Minimal/Low/Medium/High/Ultra/Max）。✅

### 3.4 drift 断言 vs 产品声明（逐项吻合）

| 枚举 | 产品源码 | 注册表 |
|---|---|---|
| uploadStage | common.ts L302 `'reading'\|'uploading'\|'creating'` | ✅ 一致 |
| ImageInjectMode / PdfInjectMode | common.ts L207/L215 | ✅ 一致 |
| PERMISSION_PRESETS | ComposerPlusMenu.tsx L55-60（4 值） | ✅ 一致 |
| THINKING_DEPTH_LABEL_KEYS 值域 | InputBarV2.tsx L100-127，11 个 kind 的值域并集恰为 6 后缀；切片终点 `\n};`（L127）正确排除后方 FALLBACKS 中文值；值捕获正则 `:\s*'([A-Za-z]+)'` 不会误吞带引号的 kind 键（引号在冒号前） | ✅ 一致 |
| KNOWN_COMPACTION_REASONS | compactionFeedback.ts L58-67（8 码）+ L77 兜底 `'compaction.reason.unknown'` | ✅ 一致 |

宽骨架 `chatV2:*` 的唯一调用点锁（测试 L170 断言 InputBarV2 含 `` t(`chatV2:${compactionReasonI18nKey(result.reason)}`) ``）已核实存在于 InputBarV2.tsx L986。✅

### 3.5 defaultNamespace 前提

`ComposerPanel.tsx` 内两处 `useTranslation('chatV2')`（L93/L185），均为单命名空间，声明 `defaultNamespace: 'chatV2'` 成立。另验证 LITERAL_BARE_KEY 字符集排除冒号，确不会重复命中带命名空间字面量（`t('ns:key')` 中冒号阻断匹配到闭引号）。✅

## 4. check-i18n.mjs 复核

- **语法**：tree-sitter-javascript 全树解析无 ERROR/MISSING 节点，**语法有效，无需修复**。（本人自写的括号配对粗检曾报失配，确认为粗检器把正则字面量 `/\/\*…\*\//`、`(['"`])` 误当注释/字符串所致的假阳性。）
- **退出语义**：`computeExitCode` 与文件头文档一致——默认失败项 missingKeys/nonStringLeaves/parseErrors/missingLocaleFiles，strict 追加 missingUsedKeys/missingNamespaceFiles；`package.json` 已挂 `check:i18n` / `check:i18n:strict`。✅
- **叶子类型检查**：`collectNonStringLeaves` 字符串合法、数组放行（returnObjects 消费）、number/boolean/null 计入失败——与契约侧「非空字符串」口径不同但互补（脚本管类型、契约管非空），无冲突。
- **逻辑抽检**：glob 匹配的 `**/`/`/**` 占位替换顺序正确；第 5 节键索引含复数基名与中间节点、keyPrefix 文件只留显式 ns 引用，均无明显误报源。未发现逻辑缺陷。

## 5. 非阻断观察（不要求本轮处理）

1. **宽骨架 `chatV2:*` 的漏检窗口**：若未来新增另一个 `` t(`chatV2:${other}`) `` 调用点，骨架同为 `chatV2:*`，会被注册表按 compaction 键展开而不落 unexpandedTemplates，`other` 的真实键集不被校验。测试 L170 只断言 compaction 调用点存在，未断言该骨架无其他占用者。建议后续把锁收紧为「统计 InputBarV2 内该骨架形状的调用点数量 == 1」或改用更窄骨架。
2. zh-CN 的 `thinkingDepth.xhigh` 与 `.max` 同为「超高」（en-US 区分 Ultra/Max），与 `THINKING_DEPTH_LABEL_FALLBACKS` 一致，疑为有意文案，仅记录。
