# r5-review-arch:架构结论文档审阅(#7)

- 审阅人:0824 Wave2-A 第 5 轮子代理 #7「架构审阅员」(claude-fable-5-thinking-high)
- 日期:2026-08-26
- 审阅对象:`docs/dev/wave2-A-agent-architecture.md`(r5 #4 产出,untracked)
- 审阅基线:枝 `cursor/0824-wave2-agent-cache-a875` @ `2d70b400` + 第 5 轮并行席位的工作区未提交改动(取证时点快照)
- 铁律遵守:本席只写本文档;未改任何产品代码、未执行 cargo/npm/tsc/任何测试、未 commit/push。

---

## 一、总裁决:**确认,不翻案**

架构文档与第 2–5 轮落地事实一致,**未发现任何谎称已验证之处**。全部「已落地」表述均如实限定为源码层面静态落地;文档头免责声明、A10/A11 专设的「已改造(待验证)」类别、§4 路线一把验证轮列为最高优先,三层结构共同守住了诚实口径;「不标 Goal complete」执行到位。

需勘误三处(见第四节):一处**数字性错误**(§4 路线一的 diff 体量把未验证代码量低估了约一半)、一处措辞错误(A1「变化」列)、一处免责声明覆盖边界偏窄。三处均不推翻任何判定、不改变 21 行矩阵统计,属收轮时的文字修正。

## 二、核对方法

1. 通读架构文档全文(130 行)、`wave2-A-ledger.md` 四轮台账全文、`ROUND-05-TASKS.md`、`r1-agent-frameworks.md` 全文。
2. 对 B 行依据抽查 `r1-anthropic-cache.md`(G3 段)、`r1-deepseek-gemini.md`(CC400 段);对第 5 轮进行时态表述抽查 `r5-model2-telemetry.md` / `r5-provider-p2.md` / `r5-digest-generation-signal.md` / `r5-catalog-pending.md` 四份席位文档开头与结论。
3. 源码独立抽查(grep,不依赖任何文档转述):三键常量、统一冻结入口、`ContextInheritance` 四档、doom-loop/tool_limit、prompt_builder 稳定层符号、四个反例测试文件、providers 四槽守卫与 `CacheControl` 无 ttl、model2 retention 现状。
4. `git status` / `git diff --stat` / `git log` 独立取证:基线指纹、第 5 轮工作区改动面、2–4 轮源码 diff 精确总量、`r4-review-frontend.md` 补齐时点。

## 三、确认项(逐块核对,全部通过)

### 3.1 基线与输入清单

- 「基线 `2d70b400`(第 4 轮已收)」= 当前 HEAD,实测一致。
- §5 输入清单「r2 7 份 / r3 8 份 / r4 8 份」与树上实数逐一相符。特别核对:台账 R4-1 曾记「#9 前端审阅文档 `r4-review-frontend.md` 缺席」,但该文档实际已随收轮提交 `2d70b400` 补齐(`git log` 证实),`r3-skill-digest-producer.md` 同理随 `6069675e` 补齐——架构文档按 8+8 份计**正确**,台账的「缺席」记录是台账截稿时点的快照,二者不矛盾。

### 3.2 矩阵计数与 r1 迁移自洽性

- 21 行 = A 14 + B 7,判定分布 10(契合)+ 2(待验证)+ 4(半契合)+ 5(不契合)= 21,逐行点数无误;A/B 两部分各自内部点数也无误。
- 「r1 初判 14 行:契合 7 / 半契合 5 / 不契合 2」与 `r1-agent-frameworks.md` 逐行核对一致(契合 = 行 1/4/5/7/8/9/14,半契合 = 行 2/6/10/12/13,不契合 = 行 3/11)。
- §2.3 迁移句三处状态迁移(A11 不契合→已改造、A10 半契合→已改造、A13 半契合→契合)与 r1 初判逐行一致;A2/A3 「2–4 轮未动」与台账各轮红线自证(hooks.rs 零改动)一致。
- 「B 部分 5 个非契合项中 B2/B4/B7 由第 5 轮并行席位落地中、B1/B5 未认领」与 ROUND-05 任务卡分工核对一致(无任何席位持有 B1/B5)。

### 3.3 A 部分与 2–4 轮落地的一致性(对台账逐条)

- A1/A13(P8 落地、`167eb104`):台账 R1 §8 逐件取证一致。
- A10/A11(三键持久化、统一入口 `freeze_tool_face_for_prompt_cache`、方案 A 四层、并发/语义双审阅零翻案):台账 R2-2/R2-3/R2-5 一致;源码抽查三键常量在 `repo.rs`(23 处命中)、统一入口在 `tool_loop.rs` 在位。
- A12/A14(digest 合同+门禁+生产者、分支复制 meta_json 免费继承、tool_call_id 逐字保持):台账 R3-3/R3-4/R3-7 一致;A12 判「半契合(收口中)」与缺口 A 未收(R4-9)一致。
- A6/A9(compaction 换代标记入同一事务、目录原子首发 8 入口全闸、fail-closed 不发):台账 R4-5/R4-6 一致。A6 措辞「目录换代标记落进 compaction 同一事务」精确复现了 r4 #6 的诚实口径(写的是标记而非重生成的目录字符串),无夸大。
- A4/A5/A7/A8 的本仓现状描述(doom-loop 指纹终止、tool_limit 块、`ContextInheritance::{None,Summary,LastNTurns,Full}`、并发 4/深度 3/截 4000 字符)——doom-loop/tool_limit/ContextInheritance 三项本席源码 grep 直接证实,其余与 r1 行 5/8 转述一致。

### 3.4 B 部分与上游依据及第 5 轮实况的一致性

- **B1**:与 `r1-anthropic-cache.md` G3 逐句对应(单块 system 尾打点、`user_profile` 在 system 内、adapter「拆稳定/易变、保留调用方稳定段尾标记」机制在而上游从不拆块)。注意一处**判定口径收紧**:r1 对 G3 判「半对齐(机制在,用法不符)」,架构文档终稿判「不契合」并在判定格内自注同一括号说明。本席裁定**维持终稿判定**:按 §2 定义的口径(不契合 = 业界有明确实践而本仓缺位或相反),「上游从不拆块」即用法层面缺位,收紧成立且透明,非翻案对象。
- **B2**:「r5 #2 落地中:四槽预算 + 工具 marker 死分支修复」与工作区实况一致——`providers/mod.rs` 四槽守卫(`ANTHROPIC_CACHE_BREAKPOINT_BUDGET`、`:2412`/`:2923`/`:5796` 测试)与 marker 透传已落;本席同时 grep 证实该文件**仍无 ttl 字段**,故 TTL 缺口仍在、维持「半契合」正确。
- **B4**:架构文档描述的是基线 `2d70b400` 现状(死实现带 `ttl:"24h"`)并标注「r5 #1 裁决执行中」。工作区实测:r5 #1 已按「优先删除」执行(两函数已删,原位留 30m/仅官方端点的硬约束注释),与架构文档的裁决建议方向完全一致,无矛盾。
- **B5**:与 `r1-deepseek-gemini.md` 的 string-required/Fireworks 实锤名单/「确定性 400」逐点对应;「全矩阵唯一线上确定性炸项」定性与 r1 的 P0 级结论一致。
- **B7**:与 `r5-model2-telemetry.md` 实况一致(session_id/variant_id/run_id 分列、post-adapter 四段指纹、`cache-hit-report.py` +198 已改)。
- 第 5 轮全部进行时态表述(「落地中/执行中/进行中/正在收」)与 `git status` 快照一一对应:#1(model2 + llm_usage 五文件 + 新 migration)、#2(providers)、#3(脚本)、#8(history + helpers)、#9(TauriAdapter)均有未提交 diff 与自述文档。架构文档作为并行席位产出,用进行时而非断言完成,时态纪律正确。

### 3.5 「谎称已验证」专项检查:**零命中**

逐类排查:

1. 全文无一处把静态落地说成「已编译/已测试/已运行验证」;「已落地」在文档头被显式定义为源码层面静态落地,并点名台账 R2-6/R3-8/R4-10 的诚实归因节。
2. A10/A11 不直接判契合而专设「已改造(待验证)」类别,且改造建议里点名待跑的具体测试与待对拍项(digest 的 serde_json preserve_order 需运行时字节对拍)——比台账口径只紧不松。
3. §3 原则六明文「命中率结论只能建立在 B7 修复后的真实 session 维度数据上,禁止拿静态推理当收益证据」;§4 路线一把「静态落地变成可信事实」列为最高优先——即文档自己声明其全部判定的证据等级。
4. 「并发/语义双审阅零翻案」(A11)是对 R2-5 审阅事实的转述,属实;不是验证声明。
5. 「不标 Goal complete」:全文无该标记,执行到位。

## 四、勘误(三处,均不改判定)

### 勘误 1(数字,应修):§4 路线一的 diff 体量错误

原文:「第 2–4 轮累计 30+ 文件、约 +1700/−170 产品与测试源码一次都未编译」。

本席实测 `git diff --stat 167eb104..2d70b400 -- src-tauri/src src`(恰为第 2–4 轮全部源码改动):**26 个唯一文件,+3722/−162**;其中四个新增测试文件(`prefix_generation_fork/restore_tests`、`llm_content_crash_tests`、`skill_replay_digest_tests`)合计 +1465,产品面约 +2257(含产品文件内新增的 `#[cfg(test)]` 模块)。

成因推断:+1700 ≈ 台账三轮「工作区 diff」之和(405+371+897 = 1673),漏加了 R2 已提交部分 `d0505bc6`(repo/types/pipeline +360)与全部 1465 行测试文件;「30+ 文件」是按轮次人次计数(8+10+13 = 31 人次),唯一文件数为 26。

影响评估:方向不受影响(体量巨大且零验证),但该句是「诚实归因」的核心量化句,**把未验证体量低估了约一半**,反向削弱了自己的论点。建议收轮时改为:「第 2–4 轮累计 26 个源码文件、+3722/−162(其中新增测试文件 +1465)一次都未编译」。

### 勘误 2(措辞,建议修):A1「变化」列

原文:「半契合项(文档化)已收口」。r1 初判 A1 即为**契合**(r1 契合 7 行含行 1),A1 从未有过半契合判定;半契合→契合的迁移只发生在 A13。该格本意应是「r1 的改造建议(P8 文档化)已收口」,现措辞会让读者误以为 A1 发生过判定迁移。不影响 §2.3 统计(迁移句只列 A13,正确)。

### 勘误 3(口径边界,建议修):免责声明覆盖面偏窄

文档头写「第 2–4 轮全部改动尚未经 cargo check / cargo test / tsc 验证」。两处未被字面覆盖:

- 第 1 轮 P8 的 hooks.rs 产品改动(`167eb104`,+76/−26,含新增测试)同样未经任何编译/测试(台账 R1 §11 明记);
- A1 与 B3 所依赖的 Step 22「已修」裁决为源码阅读结论,其自带的 12/13 个测试从未执行(台账 §5:「零验证成立,零测试不成立」)。B3 判「契合」的证据等级与 A10/A11 相同,却未在行内标注静态属性。

建议免责声明扩为「第 1–4 轮全部产品改动与 Step 22 已修裁决均为静态结论,未经运行时验证」,或在 B3 行内补一句「已修裁决为源码阅读,自带测试未执行」。属表述收紧,不改 B3 判定(判定口径本就允许基于源码现状)。

## 五、备注(不要求修改)

1. A11 改造建议格把 r5 #8/#9(digest 冲突切代信号、前端 pending generation 消费)挂在 A11 名下——两项主属 A12 的缺口收口,但 #8 的信号确实接线到 prefix generation 换代机制,交叉引用可接受;§4 路线二已把两项正确归入 A11/A12 联合收口。
2. B 部分是新增矩阵面(r1 #4 矩阵仅 A 部分前身),其 7 行判定为综合 r1 #1/#2/#3 的首次定稿,不存在「相对 r1 迁移」列,文档处理(仅 A 部分带「变化」列)正确。
3. 本文档取证时点晚于架构文档成稿,第 5 轮工作区仍在滚动;本文所引第 5 轮行号/符号为快照口径。

## 六、结论

**确认。** 架构文档忠实汇总了 2–5 轮落地,判定与统计自洽,证据等级标注诚实,无谎称验证。三处勘误(diff 体量数字、A1 变化列措辞、免责声明覆盖面)建议由收轮父代理或 #4 顺手修正,均不构成翻案理由。
