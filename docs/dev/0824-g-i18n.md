# 0824×G i18n/a11y 收口报告

分支 `cursor/0824-g-fix-i18n-cde6`。任务：在最新 0824×G 上做完整 i18n/a11y
扫描并修复 G（mobile 主题）引入的回归。

## 0. 基点与合并

- 基线：`origin/cursor/0824-cde6` @ `362dd2df`（含 leftovers-safe GenUI 加固；
  与 G 触碰面重叠为零，已实证）。
- G：`origin/cursor/0824-theme-mobile-cde6` @ `4ab24435`。
- G 尚未正式合入 0824，本分支自行构造 0824×G：`git merge --no-ff`，
  52 处冲突（45 content + 7 modify/delete）与第九轮 checklist
  （`docs/dev/0824-g-merge-checklist.md`）完全同面。
- 45 处 content 按 checklist 授权表放置，blob 取自已验证的
  `cursor/0824-rehearse-step6-fg-cde6`（44/45 与授权 blob 逐字节一致；
  `ComposerToolbar.tsx` 为该预演对 fxg `221e703` 的重裁决版，与 fxg 仅差注释，
  coarse 站点等价）。7 处 modify/delete 全部跟删除方向 `git rm`
  （legacy notes ×6 + `PracticeModeSelector.tsx`）。静默重放两文件
  （`ComposerToolbar.tsx`/`AttachmentPanelBody.tsx`）已主动放置。
- checklist 第 6 节红线全过：祖先关系、不存在清单、InputBarUI 仍 import
  拆分组件、热区行计数（ReviewQuestionsView 10 / IndexStatusView 34 /
  DataGovernanceDashboard 8 / McpToolsSection 83 / SkillsList 5）、
  非冲突面零污染（qbank-tools / finderStore / package*.json / src-tauri 零 diff）。
- 未触碰 `main` 与 `cursor/0824-cde6`。

## 1. 修复清单

本轮实际修复 4 处，全部是 step6-fg 预演漏做的 checklist 织合项
（A 侧 wrapup i18n aria-label 被 G 谱系 blob 覆盖），已在合并提交
`947925c5` 内完成，全部对齐既有 i18n key，未新造命名空间：

| # | 文件 | 回归 | 修复 |
|---|---|---|---|
| 1 | `src/components/skills-management/SkillsList.tsx` | 收藏按钮 `aria-label="favorite"` 硬编码英文，且丢失本地化 `title` | 恢复 `title`/`aria-label` = `t('skills:favorite.remove'/'skills:favorite.add')`（checklist 3.3 验证点 `skills:favorite` 引用 3 处，恢复后达标） |
| 2 | 同上 | 编辑按钮 `aria-label="edit"` 硬编码英文 | `aria-label={t('common:actions.edit')}`（与相邻 `title` 一致） |
| 3 | 同上 | 更多按钮 `aria-label="more"` 硬编码英文 | `aria-label={t('common:more')}` |
| 4 | `src/features/anki-tasks/AnkiTasksApp.tsx` | 清空搜索按钮 `aria-label="clear"` 硬编码英文（fxg/s3fg 基于 F tip 的旧值） | `aria-label={t('common:clear', { defaultValue: 'Clear' })}`（checklist 3.1 明文织合项） |

三处 G 的 `[@media(pointer:coarse)]` 热区增量全部保留，修复只回填可访问名。

## 2. 扫描范围与方法（全量，非抽查）

合并后 G 增量共 690 个文件（`git diff 362dd2df..HEAD`）。四层扫描：

1. **逐文件计数对比**（脚本）：对全部 690 文件比较合并前后
   `aria-label/title/placeholder/alt/aria-description` 的本地化（`t(`）计数
   与硬编码英文字面量计数，标记任何本地化下降或硬编码上升。
2. **diff 行级扫描**：新增行中的硬编码英文属性、英文按钮可见文本
   （Cancel/Close/Save/... 26 词）、toast/alert 硬编码；删除行中的
   `t(` 属性与 landmark 语义（`role="region"/"toolbar"/"dialog"`、
   `aria-labelledby/-describedby`）。
3. **全树扫描**：G 触碰文件在合并终态上的硬编码英文
   `aria-label/title/placeholder`（含 `={'...'}`/`={\`...\`}` 变体）。
4. **定点核查**：任务点名的 #220 空态、BatchToolbar/ImageViewer/Settings/
   QuestionBank wrapup a11y、DataGovernance 页签。

## 3. 核查结论（无需修复项及依据）

- **新引入硬编码英文属性**：除第 1 节 4 处外为零。新增行仅 2 处硬编码
  `title`，均为中文 dev 面板文案（style-lab 原生 title 演示、
  WorkspaceOrchestrationTestPlugin），非英文非产品面。全树扫描中的
  `placeholder="GOOGLE_API_KEY"`、`"mcp_filesystem"`、`"npx, node, python..."`
  等均为技术占位符且 G 之前已存在（计数无变化）。
- **wrapup 本地化被改回英文**：计数对比仅标记
  `src/components/translation/PromptPanel.tsx`（本地化 aria-label 10→9）。
  核查为 G 有意变更：小屏内联设置区不再自绘标题行（避免与宿主功能条双头部），
  整行删除了标题+关闭按钮，代码注释写明替代关闭路径（Android 返回键/宿主
  顶栏开关/保存按钮）；桌面端关闭按钮的 `t('common:close')` 保留。不属回归。
- **#220 空态区域名称（移动端）**：三个能力全部在位——
  `ContentEmptyState`（`role="region"` + `aria-labelledby` + h2）、
  `ThreadEmptyStateShell`（section `aria-labelledby` 指向内部 h2，`useId`；
  `MessageList` 与 `ChatV2Page` 均直接渲染，无移动端旁路分支）、
  `WelcomeOnboardingDialog`（`aria-describedby="welcome-onboarding-subtitle"`，
  G 触碰该文件但语义保留）。三份 a11y 契约测试在位且绿。
- **BatchToolbar**：`index.tsx` 清空搜索 `t('common:clear')`、更多
  `t('common:more')` + `aria-expanded`、全选 `t('select_all')`；
  `BatchEditDialog` 关闭/移除标签/预览翻页、`FilterBuilder` 关闭/移除过滤器
  全部本地化，G 仅叠加 coarse 热区类名。
- **ImageViewer**：全部操作钮（关闭/翻页/缩放/旋转/重置/裁剪/OCR/下载/模糊）
  aria-label 走 `t()`；`image-viewer-aria-label-i18n.source.test.ts` 绿。
- **Settings（#268 点位）**：`VendorDetailPanel` 收藏
  `t('settings:api_config.toggle_favorite')` / 测试
  `t('settings:api_config.test_button')`；`OcrEngineCard` 上移/下移/删除/关闭
  本地化 + coarse 热区；`McpEditorSection` env 行删除
  `t('common:remove')`。均未被 G 覆盖。
- **QuestionBankListView**：收藏 `t('learningHub:exam.tab.favorites')`、
  添加题目 `t('learningHub:exam.tab.addQuestion')`、批量管理
  `t('exam_sheet:questionBank.manage')`（含 title），G 44px 热区共存
  （即 step6-mobile `f0928dfe` 裁决的超集版，checklist 授权 blob 已含）。
- **DataGovernance 页签可读名**：`TabsList` + 8 个 `TabsTrigger` 全部
  `aria-label={t('data:governance.tab_*')}`，仅 DEV Debug 页签门禁与
  B 的 E2EE 链路完好，G 的 8 处 coarse 热区在位；debug-tab-visibility
  5 用例绿。
- **landmark 语义删除**：diff 中 5 处 `role="dialog"`/`aria-describedby`/
  `role="treeitem"` 删除行全部来自 G 整体删除的 legacy notes 文件
  （DndFileTree/NoteTagsEditor/TrashDialog 等，checklist 4.1 授权删除，
  workbench notes 为替代实现），非能力回退。
- **locale 文件**：G 仅新增 `workbench:legacyFallback.desktopOnly`，
  en-US/zh-CN 双语齐全，JSON 有效。

## 4. 门禁结果（本分支合并终态上执行）

- `npm run check:i18n`：通过（exit 0）。
- a11y/i18n 契约定向 Vitest：**15 files / 61 tests 全绿**——
  SkillsList.a11y、SecurityStatusIndicator.a11y、
  QuestionBankListView.a11y.source、ContentEmptyState.a11y、
  ThreadEmptyStateShell.a11y、WelcomeOnboardingDialog.a11y、
  image-viewer-aria-label-i18n.source、errorBoundaryCopy、
  DataGovernanceDashboard.debug-tab-visibility、mobile-uiux 三契约、
  secondarySurfaceShellContract、chatEmptySessionTitleContract、
  AnkiTasksApp.emptyState。
- 兜底回退保险：MCP 契约 ×3 + `input-bar/` 全目录 **22 files / 184 tests 全绿**
  （InputBar 拆分未回退）。
- `npm run typecheck`：0 错误。
