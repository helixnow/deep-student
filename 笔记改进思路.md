# DeepStudent 笔记模块：从人的使用过程出发的设计审视与优化方案

日期：2026-09-16  
方法：源码审阅、交互路径推演、既有研究交叉核对、Obsidian 官方交互资料对照。  
范围：资源库、学习桌面、Workbench、独立笔记窗口、多标签与分屏、编辑与阅读、大纲与属性、标签与双链、搜索、AI 操作、移动端及异常状态。

## 一、核心判断

**笔记模块需要一次以“持续阅读、持续写作”为中心的产品收敛。继续逐个增加按钮、面板和局部补丁，很难让它变得顺手。**

目前已经有丰富的能力，也做过不少正确的修复：保存队列、冲突保护、移动工具栏、活动实例管理、窄屏子页面、搜索定位、块操作等。问题在于这些能力仍由不同界面层和生命周期分别组织。用户在写作过程中，需要频繁识别“我在哪里”“这个按钮管什么”“现在操作的是哪篇”“返回会退到哪里”。

这正是手感变差的一种根本来源：**一个本应连续的思考过程，被界面不断切分成导航、选模式、找入口、确认状态和恢复位置。**

我最明确的五个建议是：

1. **把正文确立为所有笔记入口共同的中心。** 从资源库、搜索、双链、AI 产物进入，应该获得同一套文档行为；窗口壳可以不同。
2. **大幅减少默认可见的管理界面。** 文件树、属性、统计、摘要、格式按钮都必须证明它们值得长期占用写作空间。
3. **重新定义命令的对象和作用范围。** 页面、文本、块、整个文库分别有明确入口；同一命令可以有快捷键、菜单等多个入口，但只能有一种语义。
4. **把手机作为独立的笔记使用场景设计。** 阅读、键盘输入、选中文本、打开辅助页面是四种不同状态，不能主要依赖窗口宽度来决定。
5. **先保留 Crepe/Milkdown。** 源码已能指出大量宿主设计与接线问题。更换编辑器不会自动解决这些问题，反而会扩大内容兼容和保存链路的调整范围。

这里既有可以直接从代码确定的问题，也有明确的产品设计判断。**设计判断不需要等待实测才能提出。** 例如，把大纲放在重复统计卡片后面、在局部标签编辑中执行全库改名、在阅读状态保留一排不可用格式按钮，单从结构和语义就足以判断其代价。

---

## 二、人的任务与当前视图结构

### 2.1 用户真正想完成的任务

| 用户的想法 | 应有的连续过程 | 最需要避免的打断 |
|---|---|---|
| “先把这句话记下来” | 新建 → 光标就位 → 输入 → 自动保存 | 先选位置、先填元数据、选择模板、确认保存 |
| “接着上次那篇写” | 最近笔记/快速切换 → 恢复位置 → 继续输入 | 回到文档顶部、重新打开上下文、撤销历史消失 |
| “我记得写过这件事” | 搜索 → 判断摘要 → 到达命中段落 → 返回结果 | 只打开文件却不定位、旧结果可点击、返回丢查询 |
| “对照另一篇整理内容” | 打开分屏 → 清楚知道焦点 → 引用/复制 → 关掉辅助篇 | 两边一起响应命令、关闭后主篇失焦、工具条跨 pane |
| “看一下文章结构” | 打开大纲 → 选章节 → 接着读 | 先看统计和属性、点了只改变隐藏页面的滚动位置 |
| “给这篇做个分类” | 添加或替换本篇标签 → 返回正文 | 一个局部修改意外影响整个文库 |
| “把这段交给 AI” | 选中范围 → 指定动作 → 看差异 → 接受/拒绝 → 继续写 | 页面级与选区级动作不分、覆盖等待期间的新输入 |
| “手机上补两句” | 找到笔记 → 点正文 → 键盘出现 → 写完退出 | 屏幕被多层导航占满、工具条挤占正文、返回误退出 |
| “别把刚写的东西弄丢” | 输入有可靠归属 → 离开可恢复 → 异常有出路 | 界面显示已保存，但只保存了较早版本；冲突先覆盖后解释 |

优化应围绕这些过程展开。组件功能齐全，并不意味着这些过程顺畅。

### 2.2 视图地图与职责

| 视图/入口 | 当前主要承载 | 建议的产品职责 |
|---|---|---|
| 资源库 / Finder | 浏览资源、文件夹、打开笔记 | 找到东西、快速预览；进入持续编辑时让出管理空间 |
| 学习桌面笔记窗口 | `UnifiedAppPanel` / `NoteContentView` | 与其他学习材料并用的文档窗口 |
| Workbench 笔记应用 | `NotesWorkspaceApp` | 长时间使用的笔记工作区，提供文库导航与文档组 |
| 单篇笔记 | `NoteContentView` → `NotesCrepeEditor` | 标题、正文、明确的文档状态与页面操作 |
| 多标签 | Workbench 标签条 / `TabPanelContainer` | 保持阅读位置与上下文，不把缓存策略暴露给用户 |
| 分屏 | 双 pane / `PanelGroup` | 对照阅读、引用和整理，只有一个命令焦点 |
| 大纲 | `NotesContextPanel` | 去哪里读；优先提供章节导航 |
| 属性 | 内嵌上下文页 / `NotesPropertiesTab` | 管理标签、时间、自定义字段 |
| 双链与背链 | `NotesBacklinksPanel` | 理解引用关系，并回到具体来源位置 |
| 图谱 | `NotesGraphTab` | 探索相关笔记；辅助阅读，不占据默认写作界面 |
| 快速切换 | `NotesSearchOverlay` quick-open | 找文件、回到最近笔记 |
| 全文搜索 | 同一 overlay 的 full-text 模式 | 找内容、看上下文、定位命中 |
| 文内查找替换 | `FindReplacePanel` | 当前文档中的定位与局部修改 |
| 阅读 / 编辑 / 专注 | 编辑器及宿主分别控制 | 区分内容交互方式和界面显示密度 |
| 导入导出 / 回收站 | 文库管理面板及宿主 | 文库维护，应有独立任务范围 |
| 移动端正文 | 同一正文组件 + 移动顶栏与工具条 | 单篇阅读和轻量编辑 |
| 移动端文件、属性、链接 | 全屏内联子页面 | 临时离开正文完成一件事，完成后准确返回 |

注意：`NotesLibraryManager` 主要是导入导出管理，不能把它等同于整个资源浏览入口。Workbench 与学习桌面也有不同的标签保活、命令路由和上下文宿主，不能只分析一个页面就推断全部视图。

---

## 三、应该直接调整的产品设计

### 3.1 笔记缺少一个跨入口稳定的“文档中心”

源码里同一篇笔记可以被资源管理器、学习桌面、Workbench 和独立窗口承载。复用正文组件是好的，但宿主分别安排导航、标签、属性、焦点和保存入口，使文档的使用规则容易随入口改变。[S1][S2][S3]

从人的角度看，入口只回答“我怎么找到它”，不应该改变“我怎么使用它”。

**建议：**

- 所有入口共享文档标题、正文、阅读/编辑、查找、属性、关联、保存与恢复的语义。
- 资源库点击笔记可以先预览，但必须有明确且稳定的“打开编辑”行为。
- 正式编辑后，正文成为该区域的主要对象；不要求用户始终面对资源卡片、文件树和文档窗口的多层管理结构。
- 同一文档在多个窗口出现时，区分“文档身份”和“视图身份”。视图保留各自滚动与选区；文档版本和保存状态有共同事实来源。
- 返回应优先回到进入前的任务上下文：原搜索、原文件夹、原引用段落，而不只是某个固定首页。

**取舍：**不必统一所有窗口外观，也不必删除 Workbench。需要统一的是人的操作预期。

### 3.2 格式工具栏展示了太多“可做的事”，没有突出“此刻要做的事”

桌面 `NotesEditorToolbar` 的内联分组包含约 16 项格式/插入动作，另有制卡和溢出入口。极窄容器再整体隐藏内联区。代码本身已体现出：默认先铺开能力，再通过响应式压缩处理空间不足。[S4]

这样的工具栏容易出现两种体验：

- 宽时像操作面板，正文让位于功能展示；
- 窄时常用命令与低频命令一起消失，用户重新寻找入口。

**建议按对象分配主入口：**

| 对象 | 主入口 | 内容 |
|---|---|---|
| 整篇笔记 | 页面菜单 | 重命名、移动、导出、阅读方式、文档级 AI、属性 |
| 选中文本 | 单一选区浮条 | 粗体、链接、行内代码、引用到聊天、选区 AI |
| 当前块 | 块菜单 / `/` / 移动端 `+` | 转换、插入、移动、复制、删除 |
| 文库 | 导航区域与文库菜单 | 新建、文件夹、搜索、标签管理、导入导出、回收站 |
| 熟练用户的跨对象操作 | 命令面板 / 快捷键 | 上述能力的快捷入口，复用相同命令 |

桌面常驻条可以保留很短的格式入口，但不应试图长期展示整个 Markdown 能力集合。比如文字样式、列表、插入、更多，已经可以覆盖大部分发现需求。

**不是要求每个动作只能出现一次。** 快捷键、菜单、工具条并存是有价值的；关键是名称、作用对象、可用条件、执行逻辑、反馈一致。

### 3.3 属性面板把导航、管理和“展示能力”混在了一起

目前 `NotesContextPanel` 的主要顺序是：

```text
创建/更新时间
标签管理
宿主插入的自定义属性
“AI 笔记摘要”
大纲
```

对来找章节的人来说，真正的任务入口被排到了最后。[S5]

更明确的问题是，“AI 笔记摘要”在当前实现中由元数据**确定性生成**，没有调用 LLM；其中包含章节数卡片、键值概览、Markdown 概览和前几个标题列表。日期、标签、字数、标题在相邻区域重复出现；中文文案却叫“AI 笔记摘要”。[S6][S7]

这同时是信息层级问题与语义问题：

- 用户付出了更多阅读成本，却没有获得相应的新信息；
- “AI 摘要”承诺的是内容理解，实际展示的是统计与摘取；
- 大纲被介绍大纲的卡片挤下去；
- 短笔记也会被整理成一组仪表盘式区块。

**建议直接做减法：**

1. 将“大纲”“关联”“属性”设为同级、可直接到达的上下文入口。
2. 大纲页首先就是大纲，无需先经过元数据。
3. 属性页保留字段编辑，把创建时间等低频信息折叠或后置。
4. 从默认上下文面板移除这组生成式统计卡片；若仍保留，应诚实命名为“文档概览”，并去掉重复内容。
5. 真正的 AI 摘要由用户主动请求，展示来源版本、生成状态和是否过期；没有内容理解就不叫 AI 摘要。

这是无需实测即可做出的设计判断：**重复展示已有信息，不会因为采用卡片或 AI 风格而变得更有用。**

### 3.4 局部标签编辑的动作范围超出了人的预期

在当前笔记属性里的标签上，双击或点铅笔进入“重命名标签”；提交后，代码先修改当前笔记，再调用跨笔记重命名，遍历其他笔记并更新标签。入口文案只是“重命名标签”，影响其他笔记的数量是在成功后通知。[S5][S8]

用户很可能认为自己在编辑“这篇笔记的一个标签”，实际操作的是全局分类系统。这是作用范围不透明，不只是少了一个确认框。

**建议：**

- 本篇属性默认提供“移除此标签”“替换本篇标签”。
- “在整个文库重命名标签”放到标签管理入口，或者明确标出作用范围的二级操作。
- 在执行前展示受影响数量；执行后支持批次结果、失败重试与恢复。
- 不把逐篇顺序写回包装成一个天然原子的动作。当前路径可能在中途失败，导致部分笔记已更名、部分未更名。

相同原则应适用于文件夹移动、导入覆盖、批量删除和批量 AI 修改：**界面承诺的对象范围，必须等于实际写入范围。**

### 3.5 阅读、编辑、专注和窗口最大化需要清楚分工

当前阅读模式主要通过 `effectiveReadOnly = readOnly || readingMode` 改变编辑能力；桌面格式工具栏仍被渲染，内部把格式操作禁用。专注模式另有界面隐藏和过渡状态，宿主又有自己的窗口与侧栏状态。[S2][S4]

这里有三个不同问题：

- 阅读的人需要轻松浏览，不需要面对大量灰色编辑按钮；
- 专注写作的人仍然需要编辑能力，只是不希望被导航与管理界面打断；
- 窗口变大只改变空间，并不自动成为专注。

**建议建立两个独立轴：**

```text
内容交互：编辑 / 阅读
界面密度：普通 / 专注
```

窗口最大化单独存在，不再与上述模式混称。外部只读权限也单独表达，不伪装成用户可随时切回的阅读偏好。

- 阅读：隐藏格式工具，保留查找、链接、目录、引用和“编辑”。
- 专注编辑：隐藏文库导航与默认上下文栏，仍可通过选区、块菜单与快捷键操作。
- 专注阅读：优先正文与章节导航。
- 切换模式保留文档、滚动锚点、选区及历史；有未保存内容时仍可阅读，但保存异常必须继续可达。

目前没有看到与 Obsidian 对等的一套显式 Source mode 产品入口。若增加源码模式，应定位为 Markdown 的直接控制与兼容兜底，不能借此要求普通用户承担三种编辑模式的选择成本。

### 3.6 “已保存”首先是一种信任关系

用户不关心内部有多少队列、重试与乐观锁；他们关心现在能否放心离开。

当前编辑器内有五类状态：已保存、保存中、未保存、失败、冲突；向宿主输出的类型却只有 `saved / saving / dirty`。Workbench 状态栏按三态展示，不能准确表达编辑器中的失败与冲突。[S2][S3]

**建议：**

- 正文标题区、标签条、状态栏共用同一个保存状态，而不是分别猜测。
- 正常保存保持安静，不频繁弹成功通知，不让标签宽度随“保存中”文本抖动。
- 保存失败给出持续、就地、可操作的提示：“尚未写入磁盘，内容仍保留”，并提供重试、复制、另存为副本。
- 冲突先保留用户可见的草稿及两个版本，再让用户选择；不把“已刷新到其他版本”当作普通成功。
- “自动保存完成”必须对应当前文档版本，不能仅对应一个较早请求成功。
- 明确区分本地持久化与其他同步动作；不能因为本地保存成功就暗示全部远端工作也完成。

### 3.7 AI 应在人的意图明确时介入

当前制卡位于格式工具栏，选区又有引用到聊天，AI 修改有独立 diff 与检查点，属性页另有“AI 摘要”。能力丰富，但不同 AI 入口与对象边界需要重新安排。[S2][S4][S6]

我的建议是：

- 选中文本：默认操作这段，并能看到选区范围。
- 页面菜单：操作整篇，明确“根据全文生成卡片”“总结全文”等。
- AI 建议：在正文附近呈现差异，但不持续吞掉主要阅读空间。
- 接受建议后继续允许写作，检查点入口可回看，不要求用户一直面对操作横幅。
- 等待期间用户又编辑了正文，原建议必须重新校验或重算，不能悄悄覆盖新内容。
- 长文未完全加载时，文档级 AI 仍必须取得全文，不能把当前渲染片段当作整篇。

不要让每一篇普通笔记都像一个 AI 功能启动页。**AI 的价值应由它帮用户完成了什么体现，而不是由常驻入口的数量体现。**

---

## 四、桌面端应该如何组织

### 4.1 默认结构

以下是目标结构示意，不是当前界面截图：

```text
应用导航 / 窗口管理
┌────────────────┬──────────────────────────────┬──────────────┐
│ 文库导航       │ 文档标签 / 返回与前进       │ 上下文入口   │
│ 搜索、最近     ├──────────────────────────────┤ 大纲         │
│ 文件夹、收藏   │ 标题                 页面菜单│ 关联         │
│                │                              │ 属性         │
│                │            正文              │              │
│                │                              │ 默认按需打开 │
└────────────────┴──────────────────────────────┴──────────────┘
```

三条硬规则：

1. **文档只有一套稳定的页面操作。** 不让宿主标题栏和正文标题区各自再长出一套同义按钮。
2. **导航可退出视野。** 文件树用于找文件；找到后，用户应能舒服地读写。
3. **辅助区打开后仍需保障正文的有效宽度。** 空间不足时切换为辅助页面，而不是挤出一列勉强能输入的正文。

### 4.2 不同桌面状态的安排

| 状态 | 推荐行为 | 应避免 |
|---|---|---|
| 宽窗口、单篇 | 文库导航可见，正文有合理最大行宽，上下文按需开启 | 默认三栏全部铺开、正文随屏幕无限拉长 |
| 中等窗口 | 优先保正文；一侧管理区可折叠 | 左侧全局导航、Finder、右侧属性共同挤压正文 |
| 窄窗口 | 保留桌面交互语义，收起导航、减少按钮；必要时辅助页替换正文 | 只因 pane 窄就挂一个全窗口底部手机工具条 |
| 双栏对照 | 两个正文区域、一个明确活动 pane；上下文跟随活动篇 | 两套全局工具、相同快捷键同时影响两篇 |
| 同篇多视图 | 文档版本共享，滚动与选区独立 | 用 noteId 代替视图身份，导致两边一起滚动或覆盖 |
| 文件树关闭 | 返回/前进、快速切换仍可达 | 把文档导航只放在文件树工具栏里 |
| 多标签溢出 | 明确的标签列表入口，能识别脏状态、固定状态和同名路径 | 只隐藏滚动条，让用户猜有多少篇未关闭 |
| 专注 | 恢复正文优先，退出入口稳定，辅助命令仍可调用 | 仅把窗口放大；退出时丢失原侧栏状态 |

### 4.3 空间分配与视觉品味

这里不建议先做一轮全局换色、加阴影或改圆角。

更有收益的是：

- 用排版区分正文与管理界面，少用一层层卡片来区分每块信息。
- 标题是文档内容的起点，不是仪表盘抬头；长标题自然换行，辅助信息让位。
- 元数据没有内容时，不常驻“暂无标签”等提醒；空白本身可以是正常状态。
- 标签很多时压缩为前几个及数量入口，避免正文首屏被 chip 推走。
- 正文行宽使用可配置上限；表格、代码和大图可有局部宽展示，不强迫整个页面横向滚动。
- 选区浮条、块句柄、链接提示避免同时抢占光标附近的狭小区域。
- 更高层级的 UI 不应该比用户的文字更亮、更密或更有动效。
- 已有 reduced-motion 支持应保留；动效用于解释空间关系，不用于庆祝每次统计变化。

“看起来干净”的实质是信息有优先级。把按钮缩小、文字变灰、滚动条隐藏，并不会真正降低复杂度。

---

## 五、移动端应从四种人的状态重新设计

### 5.1 浏览与阅读

```text
返回      笔记标题                  更多
───────────────────────────────────────
正文
正文
正文
───────────────────────────────────────
笔记切换 / 搜索          编辑          关联入口
```

- 不显示桌面标签条、文件树与格式工具栏的完整组合。
- 标题区保持紧凑；完整标题在正文起点可读，不复制多份。
- 阅读时点文字不应意外弹键盘；进入编辑有明确入口。
- 普通链接点击打开，长按提供上下文操作；阅读与编辑中的链接手势需可解释。
- 图谱、属性、文件列表都是单独任务页，返回恢复原阅读位置。

### 5.2 输入与编辑

```text
返回/完成      当前笔记             更多
───────────────────────────────────────
正文
当前光标附近的内容
───────────────────────────────────────
+       常用格式       撤销/重做       收键盘
───────────────────────────────────────
系统键盘
```

- 键盘上方只出现一条编辑工具条，取代当前不需要的底部导航。
- 默认保留少量动作，`+` 稳定可见，低频命令进入有文字的完整菜单。
- 常用动作允许自定义排序；默认配置服务多数人的输入，而不是展示全部能力。
- 工具条横向滚动可以保留。Obsidian 也采用这种方式；问题在于没有明确入口、动作不可发现、滚动与点击互相干扰，而不是横向滚动本身。
- 工具条展开不能让光标消失在键盘或菜单下；选择完操作后回到刚才的输入位置。
- 当前代码有 pointer/mouse 防抢焦点、VisualViewport 偏移和 44px 控件设计，应保留这些已有基础。

### 5.3 选中内容

- 避免系统复制/粘贴菜单、Crepe 格式浮条和“引用到聊天”同时在同一位置竞争。
- 原生选择手柄应优先可用；扩展动作靠一组明确入口提供。
- 引用到聊天、生成卡片、AI 修改必须标出“选中内容”还是“整篇”。
- 图片选择器和链接输入结束后，恢复原文档、原选区；取消不生成空节点。
- 手机的块操作不能依赖 hover 小手柄，必须从当前块的菜单到达。

### 5.4 辅助页面

当前代码已将窄屏属性/文件等区域做成全屏内联子页，这个方向合理。要继续把它做完整：

- 顶栏只保留一个返回。
- 点大纲章节属于“完成选择”，应返回正文并定位，不让用户额外退出一层。
- 改属性属于“编辑辅助信息”，可留在该页；返回保存页面自身滚动位置。
- 返回优先关闭最上层临时交互；键盘关闭遵循平台原生行为；只有没有待处理层级时才离开笔记。
- 普通返回不丢文本，也不自动变成每次都弹确认；失败或明确丢弃时才需要决策。

### 5.5 将“宽度”和“输入方式”拆开

当前 `isTouchEditingSurface = isSmallScreen || isCoarsePointer` 将窄屏与触摸一起用于切换编辑工具界面；底部工具条又 portal 到 body。[S2][S9]

这混合了三个不同维度：

1. **可用空间**：决定并排还是替换、多少按钮可见。
2. **输入能力**：决定是否需要触摸块菜单、更大命中区。
3. **键盘/编辑状态**：决定工具条放在哪里、是否替换导航。

窄桌面 pane 仍然可能用鼠标和硬件键盘；平板宽屏也可能用触摸；外接键盘时手机不应制造一块“软键盘空位”。建议将这些维度分别表达，再派生布局。

这比继续增加 `max-width` 补丁更接近长期解法。

---

## 六、功能与微交互：每个动作都应有自然的下一步

| 场景 | 建议的具体行为 |
|---|---|
| 新建笔记 | 从文件夹发起则继承位置；从全局发起用稳定的默认位置。创建后直接输入，标题允许稍后补充 |
| 空笔记 | 简短输入提示即可；模板是可选帮助，不用大卡片占据第一屏 |
| 标题编辑 | Enter 完成，Esc 取消；中文候选确认不等于提交；保存失败留在就地错误与可恢复输入 |
| 快速切换 | 名称、路径和最近使用足以区分同名笔记；方向键选择、Enter 打开；无结果可直接新建 |
| 全文搜索 | 与按文件名查找清楚区分；结果显示命中上下文；打开后定位，返回保留查询和滚动 |
| 搜索语法 | `tag:`、`path:`、属性过滤可提供可视提示；不要强迫普通用户学语法 |
| 文内查找 | 只作用于当前活动文档；关闭回到合理焦点，不让后台面板抢快捷键 |
| 替换 | 当前/全部范围清晰；替换计入撤销；无匹配、非法正则和只读状态有就地解释 |
| 撤销 | 用户能理解“一次撤销”是什么：一次格式动作、一次替换、一段 AI 接受；不要悄悄跨篇操作 |
| 大纲 | 直接导航、支持折叠；同名标题能区分；点选后进入正确章节，不能只高亮目录行 |
| 双链 | 标题与路径消歧；未解析链接可创建；完成后原引用与新文档形成闭环 |
| 背链 | 默认显示少量有意义的上下文；点击到具体引用处；明确区分真实链接、未链接提及与扫描范围 |
| 图谱 | 默认围绕当前笔记展示局部关系；过滤、返回来源和列表替代入口比动画更重要 |
| 标签 | 本篇关联与全库标签维护分开；移除关联不等于删除标签或修改其他笔记 |
| 属性 | 常用字段优先、空字段按需添加；大纲不埋在字段后；失败保留用户输入 |
| 链接插入 | 选中文本作为链接文字；确认后恢复选区；编辑已有链接优先于再插入新链接 |
| 图片与附件 | 展示处理中/失败/重试；取消选择不改正文；缺失资源给可修复状态，不能只留破图 |
| 列表与任务 | Enter、空项退出、缩进、退格行为一致；点击勾选不能把阅读滚动变成编辑跳动 |
| 表格 | Tab 增行与退出行为清楚；手机横向滚动限定在表格；删除整表与清空单元格不能混淆 |
| 公式、代码 | 编辑入口与渲染状态明确；离开/取消可预期；错误源码保留，不静默吞掉 |
| Callout / Toggle | 用块语义做转换与移动；展开状态不应让内容在导出或往返中丢失 |
| 选区引用与资源 ID | 保存、重开、重命名后仍指向原资源；可见文案与稳定标识分开 |
| 导入 | 先说明冲突策略和范围；进度区分笔记/附件；部分失败有结果清单，避免一句“导入失败” |
| 导出 | “当前篇”“全文库”明确；成功说明实际包含的资源范围；导出文件可再次导入 |
| 删除与回收站 | 明确删除的是当前文档还是一个视图；外部删除后草稿能另存；恢复后正常回到文档 |

这些不是新增功能清单。很多能力已经存在，真正需要调整的是默认值、作用范围、完成后的落点和异常的出路。

---

## 七、从源码能直接指出的实现问题与风险

本节区分两种证据：

- **代码可确定**：调用、状态或参数契约本身已经不符合预期。
- **条件风险**：代码存在明确时序或多实例缺口，影响在给定条件下出现；不把推演写成现场复现。

### C1．“未保存”检测晚于真实输入，留下离开与外部更新的判断窗口

**优先级：P0；证据：明确的时序风险。**

Crepe 的 Markdown 变更通知有约 250ms 合并窗口。`isCurrentNoteDirty` 读取的是 `draftByNoteRef`、保存队列和在途请求，没有读取即时文档变化标志。`closeTabGate` 在查询结果为 clean 时直接允许关闭；外部更新也依赖该 dirty registry。[S2][S10][S11]

因此，在输入已进入 ProseMirror、草稿回调尚未执行期间，可能仍被判断为 clean。已有 `flushNoteDraft` 会在生命周期边界读取 live Markdown，这是正确补救，但不能替代**在决定是否关闭或应用外部内容之前**作出正确判断。

**改法：**事务产生时立即更新轻量文档版本/dirty 标志，序列化与持久化继续延迟；dirty 判断按文档版本与保存版本比较。不要为了同步 dirty 而每按一个键序列化全文。

**行为标准：**最后一次输入刚发生就关闭、切换或收到外部更新，也不能被当作无改动；失败时保留当前草稿。

### C2．手动保存的入口没有全部进入同一条队列

**优先级：P0；证据：调用路径可确定，并发影响为条件风险。**

编辑器的快捷键保存走 `handleManualSave → flushNoteDraft → queueSave`；`NoteContentView` 对 `NOTES_FORCE_SAVE` 的处理却直接调用 `handleSave(editor.getMarkdown())`。AI 保存也有直达宿主回调的路径。[S1][S2]

这意味着用户认为同义的“保存”，具有不同的串行化、快照更新与反馈路径。自动保存恰在进行时，额外直达请求会增加乐观锁冲突和状态不同步的可能。

**改法：**所有保存入口统一调用文档级持久化协调器；提交时绑定文档 ID 和版本，成功后统一更新状态。保留现有队列/OCC，而不是新增另一套保存机制。

**行为标准：**自动保存、快捷键、命令面板、保存并关闭和 AI 接受之间，不能出现旧版本成功覆盖新状态或重复成功提示。

### C3．Workbench 的两标签挂载上限没有学习桌面的脏标签豁免

**优先级：P0；证据：保活策略差异可确定。**

Workbench pane 的 `mountedKeys` 使用 `.slice(0, 2)`；学习桌面 `TabPanelContainer` 则在五个保活名额之外额外保留 dirty 标签。[S3][S11]

在 A 未保存、切 B、再切 C 的路径中，Workbench 的 A 可能被卸载，保存依赖卸载时的异步 flush。若写入持续失败，草稿不应仅靠即将销毁的组件与内存闭包维持。

**改法：**先统一脏标签淘汰策略；随后将草稿与保存生命周期移出 editor view。文档可以继续保存和恢复，视图可以被释放。

**行为标准：**打开再多笔记也不能以丢弃未持久化草稿换取内存下降；恢复 A 时有正确内容与明确异常状态。

### C4．强制外部更新按 noteId 广播，缺少草稿实例的保护边界

**优先级：P0；证据：多实例条件风险。**

冲突路径会发出 `notes:external-updated`，携带 `noteId` 和 `force:true`。编辑器收到 force 时清除对应待保存项并替换内容；事件没有限定目标 view/session。恢复本地版本的请求也主要按 noteId 分发。[S1][S2]

同一篇笔记在多个窗口中打开、各有草稿时，一个视图解决冲突可能影响其他视图的未保存内容。仅按文档 ID 过滤可以区分不同笔记，不能区分同篇的不同编辑会话。

**改法：**文档版本更新统一传播，但每个未合并草稿保留；“应用到某个草稿实例”的动作包含 view/session 标识。保留本地、保留外部、另存副本等操作都要有明确对象。

### C5．查找面板的全局快捷键可以由隐藏或另一 pane 的实例响应

**优先级：P1；证据：监听范围可确定。**

`FindReplacePanel` 在 document 捕获阶段监听 F3 和 Ctrl/Cmd+F，这两条分支没有当前 pane/可见性判断。父级只按 `isFindReplaceOpen` 挂载；标签隐藏保活时组件不一定卸载。Ctrl/Cmd+Z 的分支反而已有 `inPanel` 检查。[S12][S2]

**影响：**后台查找框或另一 pane 可以争抢焦点；多个面板可能响应同一次导航。专注模式还在 window 捕获阶段处理 Esc，没有先让最上层菜单消费，可能出现一次 Esc 同时改变多层状态。

**改法：**共享活动视图和顶层交互所有权；隐藏 view 不监听用户命令；Esc 一次只结束当前最上层交互。查找字段内的撤销也应区分“撤销输入查询”与“撤销替换”。

### C6．重复标题的大纲定位存在实质性歧义

**优先级：P1；证据：代码与 ProseMirror 遍历语义可确定。**

大纲内部记录了同名标题的 `occurrence`，但点击事件只传文字、级别和 noteId，没有传 occurrence。`CrepeEditor.scrollToHeading` 遍历匹配时写入 `targetPos`，并把 `return false` 当作结束遍历。[S5][S10]

ProseMirror 的 `descendants` 回调返回 false 是不再遍历该节点的子节点，不是终止后续兄弟节点。因此，后面同名同级标题会继续覆盖 `targetPos`。

使用当前依赖的三个“总结”标题进行纯模型推演，访问位置为 `[0, 4, 8]`，该写法最终选中 `8`。这是算法语义验证，不是 UI 实测。

**改法：**优先使用稳定标题标识；最少把 occurrence 贯穿解析、点击、定位与高亮。模糊匹配必须是显式回退，不应在精确目标存在时偷偷跳到相似章节。

### C7．长文的“全文”和“已加载片段”没有贯穿所有能力

**优先级：P1；涉及修改全文时按 P0 处理。**

笔记默认可按约 600 行窗口加载。保存有 `composeWindowedSave` 保留未加载后缀，但文内查找读取当前 ProseMirror doc，嵌入属性页的大纲读取 `visibleContent`。Workspace 属性页先读取全文，又接收编辑器的片段级实时事件。[S1][S2][S5][S12][S13]

制卡 helper 已优先使用 `getFullMarkdown`，但全文 API 是在宿主为 ACR 注册时包装的；编辑器内部传给桌面/移动工具栏的仍是自己的 `editorApi`，因此不能仅因为 helper 支持全文就认为接线已完整。[S1][S2][S14]

**人的体验：**“搜索不到明明存在的内容”“目录后半段不见”“整篇制卡只覆盖前半篇”，都会被理解为文档不可靠。

**改法：**

- 文档级功能统一从 document session 取全文。
- 查找/大纲到达未加载区域时，先扩展到目标，再定位。
- 界面显示的是全文统计还是片段统计必须明确。
- 只对片段执行的替换不能叫“全部替换”；全文写入必须校验原始版本和未加载部分。

### C8．窄屏大纲点击没有完成“回到正文”的任务

**优先级：P1；证据：接线缺口可确定。**

窄屏属性打开时正文 main 被隐藏。大纲点击只广播滚动事件，没有请求宿主关闭属性子页；`NotesContextPanel` 也没有完成章节选择的宿主回调。[S1][S5]

**改法：**大纲选择作为导航动作交给宿主：记录目标 → 返回正文 → 视图恢复后定位。宽屏可留侧栏，窄屏应完成返回；不能要求用户猜刚才点了有没有生效。

### C9．属性页有“新身份配旧正文”的中间状态

**优先级：P1；证据：状态逻辑可确定。**

`NotesPropertiesTab` 的 `content` 没有与 noteId 配对；从 A 切到 B 时，新的 note 元数据会进入渲染，但旧 content 要等 B 的异步读取完成才替换。读取失败则直接 return，没有对应错误态。[S15]

此外，大纲解析优先取 `liveContent`，而文档概览取 `effectiveActive.content_md`，同一上下文面板内部也有不同的新鲜度来源。[S5]

**改法：**身份与内容快照一起管理；切篇后不把旧内容作为新篇可操作的大纲；加载、失败与重试就地表达。实时内容应带版本，不能仅凭非 null 就永远优先。

### C10．中文输入法的提交语义没有一致处理

**优先级：P1；证据：处理方式差异可确定。**

标题编辑对 `isComposing/keyCode 229` 做了保护；快速切换的 Enter 分支，以及上下文标签添加/改名的 Enter 分支没有同样的保护。[S16][S5][S8]

**改法：**候选词确认与业务提交分开，统一 composition 守卫。不能在用户确认中文候选时打开搜索结果、提交标签，尤其不能触发全库改名。

### C11．保存状态在宿主边界降级

**优先级：P1；证据：类型与渲染可确定。**

内部五态经三态回调输出，失败和冲突不能准确呈现在标签与状态栏。应输出完整状态、版本和可恢复动作，而不是由各层组合几个布尔值推断。[S2][S3]

### C12．正常阅读仍背负编辑工具界面

**优先级：P1；证据：渲染路径可确定。**

阅读状态继续渲染桌面格式栏，仅以 `readOnly` 禁用其格式按钮。移动工具条则有更明确的隐藏条件，造成两端阅读语义不够一致。[S2][S4]

**改法：**阅读模式给阅读任务需要的控件；不能把“不可编辑”简单呈现为“编辑界面里所有按钮变灰”。

---

## 八、对标 Obsidian：借鉴稳定的行为模型

以下竞品事实来自官方文档；设计建议是针对 DeepStudent 的判断，不把 Obsidian 描述为没有缺点。

| 维度 | Obsidian 官方行为 | DeepStudent 应吸收的原则 |
|---|---|---|
| 编辑/阅读 | 明确区分 Reading 与 Editing；Editing 下有 Live Preview 和 Source mode | 模式有清楚语义；阅读、界面密度、写权限分别表达 |
| 标签与分组 | 支持标签组、分屏、拖拽到其他组/窗口、固定标签、恢复布局 | 打开位置与文档状态稳定；复杂布局不改变基础操作 |
| 关联视图 | Outline、Backlinks、局部 Graph 可成为 linked views | 上下文绑定文档，不应被全局活动状态随意带走 |
| 快速切换 | 名称搜索、最近笔记、方向键/Enter、新标签打开、无结果创建 | 将“找到”和“开始写”接起来，避免多余导航 |
| 命令面板 | 可搜索操作并执行，支持快捷键 | 长尾能力有统一发现入口，不必全部常驻 |
| 手机导航 | 有返回/前进、快速切换、标签数量、菜单入口 | 保持少量稳定入口，让正文获得空间 |
| 手机编辑条 | 编辑时显示，可横向滚动并自定义；收键盘后回到导航 | 按任务切换工具；自定义比无限追加默认按钮更可持续 |

官方资料：

- https://help.obsidian.md/edit-and-read
- https://help.obsidian.md/tabs
- https://help.obsidian.md/plugins/quick-switcher
- https://help.obsidian.md/command-palette
- https://help.obsidian.md/mobile

**不建议照搬的部分：**

- 不为形式上的对标立即引入更多视图、更多模式或更复杂的标签玩法。
- 不把 Markdown 标点知识变成初学者的使用门槛。
- 不把所有低频功能塞入命令面板后就宣称解决了可发现性；常用动作仍需可见入口。
- 不复制插件生态的全部扩展自由度，先稳定应用自己的行为。
- DeepStudent 的学习引用、AI 修改和制卡有自身价值，应围绕学习任务组织，不必假装自己只是纯笔记库。

还有一个具体冲突：Obsidian 用 Ctrl/Cmd+E 切阅读/编辑，当前 Milkdown 工具栏将 Mod-E 表达为行内代码。不能为了“像 Obsidian”直接覆盖现有快捷键；需要统一快捷键定义、冲突提示和迁移方式。[S4]

---

## 九、状态设计：让异常也符合人的理解

### 9.1 状态应分层，而非堆叠布尔值

```text
文档状态：加载中 → 可用 → 已删除/不可访问
保存状态：clean → dirty → saving → saved
                              └→ failed / conflict
视图状态：active / inactive，编辑 / 阅读，普通 / 专注
导航状态：正文 / 文件列表 / 大纲 / 属性 / 关联
临时交互：查找、块菜单、链接输入、AI 差异、原生选择器
```

这些状态可以组合，但应规定合法组合。例如：隐藏标签不能拥有全局查找焦点；属性替换正文时，正文不能继续占有移动工具条；保存失败不能被阅读模式切换隐藏。

### 9.2 各状态的产品标准

| 状态 | 用户应该看到和能做什么 |
|---|---|
| 首次加载 | 与正文结构接近的轻量占位；不能出现可编辑的上篇内容 |
| 切篇加载 | 标题与内容身份一致；保持空间稳定；旧请求完成不覆盖新篇 |
| 空笔记 | 直接写；没有元数据也正常 |
| 加载失败 | 就地原因、重试、返回；不把失败当空文档 |
| 已保存 | 安静的确认，可放心离开 |
| 未保存 / 保存中 | 不阻断输入；当前版本的进度可理解 |
| 保存失败 | 内容仍在、可以重试/复制/另存；不能只弹一次 toast |
| 冲突 | 两版都在，明确来源与差异；选择后落盘结果可确认 |
| 外部更新、当前 clean | 原位更新，尽量保持阅读位置，避免闪回顶部 |
| 外部更新、当前 dirty | 不静默覆盖；保留草稿，提供解决入口 |
| 只读 | 告知原因；仍能阅读、查找与引用；不提供看似可用的写操作 |
| 长文加载更多 | 正文仍可用；加载错误只影响该步骤；全篇命令语义不缩水 |
| 外部删除 | 无草稿时显示已删除；有草稿时可另存，不让用户在幽灵文档上反复保存 |
| 离线 / 网络不可用 | 本地笔记继续工作；依赖网络的 AI/资源分别解释，不能把整个编辑器判为不可用 |
| 标题或属性提交失败 | 保留输入、就地纠错；不要假显示为已成功的新值 |
| AI 执行中 | 可取消或离开；范围与版本明确；完成后不强抢焦点 |

### 9.3 必须统一的生命周期约束

1. 能否关闭由文档事实决定，不由某个按钮的局部布尔值决定。
2. 视图卸载不等于草稿销毁。
3. 同篇多视图不能共享一个可变选区或滚动位置。
4. 页面命令、菜单、快捷键、AI 接受都走同一文档命令与保存边界。
5. 任何异步结果都携带文档身份和基准版本；回调前再次确认目标。
6. 渲染窗口只影响显示性能，不改变“整篇笔记”的业务含义。

---

## 十、改造路线：先改变默认体验，再做长期抽象

### P0：保存与作用范围的底线

| 工作包 | 具体内容 | 完成标准 |
|---|---|---|
| P0-A 文档写入一致性 | C1、C2、C11：即时 dirty、统一保存入口、完整状态向上输出 | 刚输入就离开也能识别；所有保存方式指向同一版本；错误不被包装成已保存 |
| P0-B 草稿与视图隔离 | C3、C4：脏标签保活、保存不依赖视图、冲突保护每个草稿实例 | 切第三篇、关辅助窗、同篇多开都不隐式丢弃内容 |
| P0-C 命令目标正确 | C5 及全文写操作：活动 pane、临时交互所有权、文档/片段范围 | 一次命令只影响预期对象；隐藏 view 无权抢焦点或改内容 |
| P0-D 标签改名范围 | 局部替换与全库改名分离，批次结果明确 | 本篇操作不隐式修改其他笔记 |

### P1：最能改变日常手感的一轮设计收敛

| 工作包 | 具体内容 | 完成标准 |
|---|---|---|
| P1-A 正文优先 | 精简默认格式条，移除重复概览卡，标签/统计后置 | 打开普通笔记首先进入阅读/写作，而不是管理面板 |
| P1-B 上下文重组 | 大纲、关联、属性各自有直接入口；窄屏选择后返回正文 | 找章节不经过元数据；同名标题与长文目标正确 |
| P1-C 手机四状态 | 浏览、输入、选区、辅助页分别定义 chrome | 无双层底栏、无重复返回、无整套桌面工具长期挤占正文 |
| P1-D 导航与搜索闭环 | 快速切换、新建、全文命中、返回原结果、同名消歧 | 找到内容后直接继续任务，不反复重新搜索 |
| P1-E 模式与焦点 | 阅读隐藏编辑工具；专注保留能力；Esc 按层级处理 | 模式切换不丢位置，操作对象始终清楚 |
| P1-F 内容与语言边界 | C6–C10：标题锚点、全文 API、属性身份、IME | 正确中文输入、可靠目录、全文能力无片段缩水 |

### P2：稳定之后再投入的能力

- 手机常用命令配置、快捷键个性化。
- 更好的局部图谱、背链分组与关系探索。
- 文档级与选区级 AI 工作流优化。
- 真正的内容摘要与摘要版本管理。
- 长文的增量解析、索引、视图恢复和性能观测。
- 编辑器 adapter 与候选底座 PoC。

**可以先交付的一组明显改善：**移除默认重复摘要卡；让大纲直接到达且点后回正文；阅读模式隐藏格式条；标签局部编辑不做全局传播；把桌面常驻格式工具缩为少量入口。这些改动能改变日常体验，不需要等待大规模架构重写。

与此同时，保存与命令范围问题应在同一轮优先处理，不能只完成视觉减法。

---

## 十一、工程组织建议

不建议再把更多独立状态堆进 `NotesCrepeEditor`。也不建议第一步就重写一个完整编辑器框架。

可以沿现有边界逐步收敛：

### 11.1 文档会话

集中管理文档 ID、全文、当前版本、持久化版本、草稿、保存队列、外部更新与冲突。首先复用既有 DSTU/VFS 和 OCC 逻辑，把事实来源收拢。

Rust 侧已存在内容/元数据更新的乐观锁与事务/保存点，正文和链接索引有原子更新路径。这些是可靠性基础，应继续使用；本轮问题主要是上层入口和生命周期没有完全共享这些边界。[S17]

### 11.2 文档视图会话

独立保存 viewId、pane/window、选区、滚动锚点、查找状态、阅读方式与当前辅助页。视图可以卸载，文档草稿不能因此失去主人。

### 11.3 命令定义

一条命令至少回答：

```text
作用对象是什么？
需要哪个文档和哪个视图？
现在能否执行，不能执行的原因是什么？
是否修改内容，是否进入撤销历史？
执行后焦点在哪里？
是否需要全文，成功如何确认？
```

先收口保存、查找、插入链接、图片、格式与 AI 接受等高价值动作，不必一次迁移所有命令。

### 11.4 交互层所有权

继承已有活动实例、移动工具条 owner、Android back coordinator 和 focus scope 的思路，统一管理谁有权消费全局按键、显示 body portal、捕获返回。

一个 view 被隐藏时，应释放临时交互所有权；恢复时可以恢复查找文本等状态，但不能自动抢焦点。

### 11.5 编辑器适配层

建议逐步明确的能力包括：全文与可视片段读取、版本、选区捕获/恢复、格式操作、块操作、定位、历史、焦点、只读、持久化确认。

若未来做 BlockNote/Tiptap PoC，至少比较以下语料往返：

- 标题与重复标题、列表与任务、表格；
- 行内/块级公式、代码、callout、toggle；
- 图片、附件、wikilink、标题锚点；
- AI 引用、学习资源 ID、缺失资源；
- 中文输入、撤销重做、选区与滚动；
- 保存、关闭重开、导出再导入、外部修改。

只有在明确的体验瓶颈无法通过现有底座合理解决，且候选方案确实保持这些语义时，再决定迁移。单凭组件代码长或 UI 难看，不足以支持换底座。

---

## 十二、改造后的行为验收清单

这是一组未来实施的完成标准，不是本次调研的实测要求。

| 编号 | 使用路径 | 通过标准 |
|---|---|---|
| A01 | 从资源库/Workbench/搜索分别打开同篇 | 文档操作语义一致，返回各自来源上下文 |
| A02 | 新建普通笔记 | 直接输入，不必先补元数据或选模板 |
| A03 | 刚输入就切篇/关页 | 当前输入被识别，持久化失败时可恢复 |
| A04 | 自动保存同时触发手动保存 | 版本顺序正确，无多条竞争保存路径 |
| A05 | 保存失败后切过多篇 | 草稿仍可找回，标签与正文状态一致 |
| A06 | 同篇开两视图并发生冲突 | 不静默覆盖另一视图草稿，各自位置保留 |
| A07 | 双栏分别打开查找 | Ctrl/Cmd+F、F3、撤销只影响当前目标 |
| A08 | 隐藏标签保留查找/菜单状态 | 不消费当前页面快捷键，不残留 body 浮层 |
| A09 | 同名同级标题、大纲、双链锚点 | 跳到明确目标，不落到最后一个同名标题 |
| A10 | 超过窗口行数的长文 | 查找、大纲、制卡等全文能力覆盖后半篇 |
| A11 | 窄屏打开大纲并选章节 | 回正文并准确定位，无多余返回操作 |
| A12 | 切篇时属性读取慢/失败 | 不显示属于上篇的可操作目录，能重试 |
| A13 | 中文输入搜索/标签/标题 | 候选确认不触发业务提交 |
| A14 | 阅读/编辑/专注来回切换 | 对应工具正确，位置与内容历史保留 |
| A15 | 当前篇替换标签 | 只影响当前篇；全库操作另有范围说明 |
| A16 | 插入链接/图片后取消或切篇 | 不误插到其他笔记、不丢原选区、不生成空资源 |
| A17 | AI 建议等待期间继续编辑 | 接受时校验基线，可撤销，不覆盖新输入 |
| A18 | 搜索打开命中再返回 | 查询、结果位置和过滤条件恢复 |
| A19 | 从 320/375/390/768 到宽桌面容器 | 布局有明确优先级，无控件盖住正文；窄桌面不误用手机键盘逻辑 |
| A20 | 手机键盘、选区、菜单、返回组合 | 一次返回结束一层，无重复工具条，光标可见 |
| A21 | 图片/双链/公式/表格/引用往返 | 保存、重开、导出导入后语义与资源身份保留 |
| A22 | 外部删除、只读、离线、加载失败 | 每种状态都有符合任务的恢复路径 |
| A23 | 文库导入部分失败 | 已完成、未完成、冲突项清楚，可继续处理 |
| A24 | 键盘与辅助技术使用 | 菜单可导航、焦点可见、按钮有名称、状态不只依赖颜色 |

性能目标应先建立测量基线。可以把“热切篇反馈约 100ms 内”“普通输入不出现明显长任务”“打开辅助区不改变当前阅读锚点”作为初始设计预算，但这些不是当前产品的已测数据，也不应以牺牲全文正确性实现。

---

## 十三、证据边界与历史修复的处理

### 本轮源码基线

- 静态仓库：`helixnow/deep-student`。
- Commit：`69fbed572187b2d4470d3df61e9abdc4b74da181`。
- 用户本机桥接读取的 `NoteContentView.tsx`、`NotesCrepeEditor.tsx` 快照与本轮静态仓库对应文件一致。
- 本机 `NotesContext.tsx` 快照与静态仓库不同，存在额外并发/刷新修复；没有把静态版本的该文件问题直接当作用户最新工作区的问题。
- 其他文件主要以该 commit 为依据；进入实施时应对照届时工作区，不覆盖已有改动。

本报告按用户补充要求，以源码和设计分析为交付，不要求实测作为判断前提，也不声称本轮完成当前版本 UI 复现。前期已运行的八个现有前端测试文件共 81 项通过，只说明这些已覆盖逻辑通过，不证明整体手感或本文新指出的边界已经正确。

### 已有正确工作应保留

2026-09-12 的审计与后续验收记录提到：窄窗工具栏、属性遮挡、重复移动 chrome、非活动浮层、响应式切换 flush、链接与图片选区等已有修复。当前源码也能看到相应保护。

因此本报告没有把那些旧问题全部重新宣布为现存 bug。需要继续推进的是：

- 从“避免重叠”走到“默认信息层级正确”；
- 从“按钮有接线”走到“动作范围和完成路径符合预期”；
- 从“卸载时尽量保存”走到“草稿有独立生命周期”；
- 从“按 noteId 过滤”走到“文档、草稿与视图各自有身份”；
- 从“可以缩到手机宽度”走到“手机阅读与输入有独立设计”。

历史窄窗记录只能说明当时的桌面响应式行为，不等于手机原生交互事实。本轮也不借历史截图推断当前像素表现。

---

## 源码索引

以下链接固定到本轮审阅 commit。正文中的 S 编号可直接打开对应文件。

- [S1 — NoteContentView：加载、窗口化、OCC、保存命令、上下文子页][S1]
- [S2 — NotesCrepeEditor：保存队列、dirty、模式、工具条、事件与生命周期][S2]
- [S3 — NotesWorkspaceApp：工作区导航、多标签、分屏、两标签挂载与状态栏][S3]
- [S4 — NotesEditorToolbar：默认格式条、只读处理、制卡与快捷键][S4]
- [S5 — NotesContextPanel：信息顺序、实时大纲、标签全局改名、标题点击][S5]
- [S6 — NotesGenerativeSummary：确定性概览的展示][S6]
- [S7 — buildNoteSummaryIntent：统计、概览与标题列表的重复组织][S7]
- [S8 — 中文文案：标签改名语义][S8]
- [S9 — MobileEditorToolbar：工具、portal、键盘偏移和动作可用性][S9]
- [S10 — CrepeEditor：Markdown 变更通知与标题遍历定位][S10]
- [S11 — TabPanelContainer：五标签保活与 dirty 豁免][S11]
- [S12 — FindReplacePanel：全局快捷键、当前文档查找与替换][S12]
- [S13 — markdownWindow：默认窗口、加载边界与全文保存组合][S13]
- [S14 — generateCardsFromNote：全文优先读取契约][S14]
- [S15 — NotesPropertiesTab：元数据、正文异步加载与 watch][S15]
- [S16 — NotesSearchOverlay：快速切换、全文搜索与键盘提交][S16]
- [S17 — note_repo.rs：持久化、OCC、事务与链接索引][S17]
- [S18 — closeTabGate：dirty 查询、保存并关闭与取消][S18]
- [S19 — 2026-09-12 历史验收记录][S19]
- [S20 — 2026-09-12 历史 UX 审计][S20]
- [S21 — “AI 笔记摘要”中文文案][S21]

[S1]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/learning-hub/apps/views/NoteContentView.tsx
[S2]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/NotesCrepeEditor.tsx
[S3]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/notes/NotesWorkspaceApp.tsx
[S4]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/components/NotesEditorToolbar.tsx
[S5]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/NotesContextPanel.tsx
[S6]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/components/NotesGenerativeSummary.tsx
[S7]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/generative-ui/utils/buildNoteSummaryIntent.ts
[S8]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/locales/zh-CN/notes.json
[S9]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/components/MobileEditorToolbar.tsx
[S10]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/components/crepe/CrepeEditor.tsx
[S11]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/learning-hub/apps/TabPanelContainer.tsx
[S12]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/components/FindReplacePanel.tsx
[S13]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/markdownWindow.ts
[S14]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/generateCardsFromNote.ts
[S15]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/notes/NotesPropertiesTab.tsx
[S16]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/notes/NotesSearchOverlay.tsx
[S17]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src-tauri/src/vfs/repos/note_repo.rs
[S18]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/learning-hub/closeTabGate.ts
[S19]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/docs/research/notes-ux-verification-20260912.md
[S20]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/docs/audits/2026-09-12-notes-ux-audit.md
[S21]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/locales/zh-CN/generativeUi.json
