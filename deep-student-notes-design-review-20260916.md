# DeepStudent 笔记模块：从人的使用过程出发的设计审视与优化方案

日期：2026-09-16  
版本：第二轮修订；保留首轮的产品判断，补充逐项复核、调用链证据、施工规格与依赖路线。  
方法：源码审阅、交互路径推演、既有研究交叉核对、Obsidian 官方交互资料对照。  
范围：资源库、学习桌面、Workbench、独立笔记窗口、多标签与分屏、编辑与阅读、大纲与属性、标签与双链、搜索、AI 操作、移动端及异常状态。

### 第二轮阅读指引

- 看产品方向：第一至六章。
- 看哪些判断仍成立、哪些需要收窄：第七章 C1–C16，以及第十三章的证据边界。
- 看具体怎么改：第十四章 R01–R14，每项包含当前事实、交互规则、接线位置、实施顺序和验收。
- 安排实施：第十五章 B01–B12；第十六章给出故障注入与状态组合验收。

**第二轮最重要的变化：**不能再用“缺少保存保护、缺少全文 API、缺少移动适配”笼统解释问题。这些基础很多已经存在。剩余问题集中在：同一行为的不同入口没有共用同一契约，保护的粒度有时是 noteId、有时是窗口、有时只是一个组件，界面又把这些不同步的结果组合给用户。

进一步确认的具体缺口包括：属性整对象写回没有使用现成的元数据 OCC 参数；正文标题改名和文件树改名的双链维护路径不同；重命名来源扫描失败没有在宿主完整呈现；Workbench 内部标签关闭没有复用已有三选项保存关闭协议；文库导出入口没有等待当前草稿持久化。这些都比继续增加格式按钮更值得先处理。

本文是一份研究和实施规格，未修改生产代码。所有像素、耗时、真机键盘与触摸表现均未在第二轮实测。

## 一、核心判断

**笔记模块需要一次以“持续阅读、持续写作”为中心的产品收敛。继续逐个增加按钮、面板和局部补丁，很难让它变得顺手。**

目前已经有丰富的能力，也做过不少正确的修复：保存队列、冲突保护、移动工具栏、活动实例管理、窄屏子页面、搜索定位、块操作等。问题在于这些能力仍由不同界面层和生命周期分别组织。用户在写作过程中，需要频繁识别“我在哪里”“这个按钮管什么”“现在操作的是哪篇”“返回会退到哪里”。

这正是手感变差的一种根本来源：**一个本应连续的思考过程，被界面不断切分成导航、选模式、找入口、确认状态和恢复位置。**

我最明确的五个建议是：

1. **把正文确立为所有笔记入口共同的中心。** 从资源库、搜索、双链、AI 产物进入，应该获得同一套文档行为；窗口壳可以不同。
2. **大幅减少默认可见的管理界面。** 文件树、属性、统计、摘要、格式按钮都必须证明它们值得长期占用写作空间。
3. **重新定义命令的对象和作用范围。** 页面、文本、块、整个文库分别有明确入口；同一命令可以有快捷键、菜单等多个入口，但只能有一种语义。
4. **把手机作为独立的笔记使用场景设计。** 阅读、键盘输入、选中文本、打开辅助页面是四种不同状态，不能主要依赖窗口宽度来决定。
5. **先保留 Crepe/Milkdown。** 源码已能指出大量宿主设计与接线问题。更换编辑器不会自动解决这些问题，反而会扩大内容兼容和保存链路的调整范围。

这里既有可以直接从代码确定的问题，也有明确的产品设计判断。**设计判断不需要等待实测才能提出。** 例如，把大纲放在重复统计卡片后面、在局部标签编辑中执行全库改名、在阅读状态保留一排不可用格式按钮，单从结构和语义就足以判断其代价。

---

## 二、人的任务与当前视图结构

### 2.1 用户真正想完成的任务

| 用户的想法 | 应有的连续过程 | 最需要避免的打断 |
|---|---|---|
| “先把这句话记下来” | 新建 → 光标就位 → 输入 → 自动保存 | 先选位置、先填元数据、选择模板、确认保存 |
| “接着上次那篇写” | 最近笔记/快速切换 → 恢复位置 → 继续输入 | 回到文档顶部、重新打开上下文、撤销历史消失 |
| “我记得写过这件事” | 搜索 → 判断摘要 → 到达命中段落 → 返回结果 | 只打开文件却不定位、旧结果可点击、返回丢查询 |
| “对照另一篇整理内容” | 打开分屏 → 清楚知道焦点 → 引用/复制 → 关掉辅助篇 | 两边一起响应命令、关闭后主篇失焦、工具条跨 pane |
| “看一下文章结构” | 打开大纲 → 选章节 → 接着读 | 先看统计和属性、点了只改变隐藏页面的滚动位置 |
| “给这篇做个分类” | 添加或替换本篇标签 → 返回正文 | 一个局部修改意外影响整个文库 |
| “把这段交给 AI” | 选中范围 → 指定动作 → 看差异 → 接受/拒绝 → 继续写 | 页面级与选区级动作不分、覆盖等待期间的新输入 |
| “手机上补两句” | 找到笔记 → 点正文 → 键盘出现 → 写完退出 | 屏幕被多层导航占满、工具条挤占正文、返回误退出 |
| “别把刚写的东西弄丢” | 输入有可靠归属 → 离开可恢复 → 异常有出路 | 界面显示已保存，但只保存了较早版本；冲突先覆盖后解释 |

优化应围绕这些过程展开。组件功能齐全，并不意味着这些过程顺畅。

### 2.2 视图地图与职责

| 视图/入口 | 当前主要承载 | 建议的产品职责 |
|---|---|---|
| 资源库 / Finder | 浏览资源、文件夹、打开笔记 | 找到东西、快速预览；进入持续编辑时让出管理空间 |
| 学习桌面笔记窗口 | `UnifiedAppPanel` / `NoteContentView` | 与其他学习材料并用的文档窗口 |
| Workbench 笔记应用 | `NotesWorkspaceApp` | 长时间使用的笔记工作区，提供文库导航与文档组 |
| 单篇笔记 | `NoteContentView` → `NotesCrepeEditor` | 标题、正文、明确的文档状态与页面操作 |
| 多标签 | Workbench 标签条 / `TabPanelContainer` | 保持阅读位置与上下文，不把缓存策略暴露给用户 |
| 分屏 | 双 pane / `PanelGroup` | 对照阅读、引用和整理，只有一个命令焦点 |
| 大纲 | `NotesContextPanel` | 去哪里读；优先提供章节导航 |
| 属性 | 内嵌上下文页 / `NotesPropertiesTab` | 管理标签、时间、自定义字段 |
| 双链与背链 | `NotesBacklinksPanel` | 理解引用关系，并回到具体来源位置 |
| 图谱 | `NotesGraphTab` | 探索相关笔记；辅助阅读，不占据默认写作界面 |
| 快速切换 | `NotesSearchOverlay` quick-open | 找文件、回到最近笔记 |
| 全文搜索 | 同一 overlay 的 full-text 模式 | 找内容、看上下文、定位命中 |
| 文内查找替换 | `FindReplacePanel` | 当前文档中的定位与局部修改 |
| 阅读 / 编辑 / 专注 | 编辑器及宿主分别控制 | 区分内容交互方式和界面显示密度 |
| 导入导出 / 回收站 | 文库管理面板及宿主 | 文库维护，应有独立任务范围 |
| 移动端正文 | 同一正文组件 + 移动顶栏与工具条 | 单篇阅读和轻量编辑 |
| 移动端文件、属性、链接 | 全屏内联子页面 | 临时离开正文完成一件事，完成后准确返回 |

注意：`NotesLibraryManager` 主要是导入导出管理，不能把它等同于整个资源浏览入口。Workbench 与学习桌面也有不同的标签保活、命令路由和上下文宿主，不能只分析一个页面就推断全部视图。

---

## 三、应该直接调整的产品设计

### 3.1 笔记缺少一个跨入口稳定的“文档中心”

源码里同一篇笔记可以被资源管理器、学习桌面、Workbench 和独立窗口承载。复用正文组件是好的，但宿主分别安排导航、标签、属性、焦点和保存入口，使文档的使用规则容易随入口改变。[S1][S2][S3]

从人的角度看，入口只回答“我怎么找到它”，不应该改变“我怎么使用它”。

**建议：**

- 所有入口共享文档标题、正文、阅读/编辑、查找、属性、关联、保存与恢复的语义。
- 资源库点击笔记可以先预览，但必须有明确且稳定的“打开编辑”行为。
- 正式编辑后，正文成为该区域的主要对象；不要求用户始终面对资源卡片、文件树和文档窗口的多层管理结构。
- 同一文档在多个窗口出现时，区分“文档身份”和“视图身份”。视图保留各自滚动与选区；文档版本和保存状态有共同事实来源。
- 返回应优先回到进入前的任务上下文：原搜索、原文件夹、原引用段落，而不只是某个固定首页。

**取舍：**不必统一所有窗口外观，也不必删除 Workbench。需要统一的是人的操作预期。

### 3.2 格式工具栏展示了太多“可做的事”，没有突出“此刻要做的事”

桌面 `NotesEditorToolbar` 的内联分组包含约 16 项格式/插入动作，另有制卡和溢出入口。极窄容器再整体隐藏内联区。代码本身已体现出：默认先铺开能力，再通过响应式压缩处理空间不足。[S4]

这样的工具栏容易出现两种体验：

- 宽时像操作面板，正文让位于功能展示；
- 窄时常用命令与低频命令一起消失，用户重新寻找入口。

**建议按对象分配主入口：**

| 对象 | 主入口 | 内容 |
|---|---|---|
| 整篇笔记 | 页面菜单 | 重命名、移动、导出、阅读方式、文档级 AI、属性 |
| 选中文本 | 单一选区浮条 | 粗体、链接、行内代码、引用到聊天、选区 AI |
| 当前块 | 块菜单 / `/` / 移动端 `+` | 转换、插入、移动、复制、删除 |
| 文库 | 导航区域与文库菜单 | 新建、文件夹、搜索、标签管理、导入导出、回收站 |
| 熟练用户的跨对象操作 | 命令面板 / 快捷键 | 上述能力的快捷入口，复用相同命令 |

桌面常驻条可以保留很短的格式入口，但不应试图长期展示整个 Markdown 能力集合。比如文字样式、列表、插入、更多，已经可以覆盖大部分发现需求。

**不是要求每个动作只能出现一次。** 快捷键、菜单、工具条并存是有价值的；关键是名称、作用对象、可用条件、执行逻辑、反馈一致。

### 3.3 属性面板把导航、管理和“展示能力”混在了一起

目前 `NotesContextPanel` 的主要顺序是：

```text
创建/更新时间
标签管理
宿主插入的自定义属性
“AI 笔记摘要”
大纲
```

对来找章节的人来说，真正的任务入口被排到了最后。[S5]

更明确的问题是，“AI 笔记摘要”在当前实现中由元数据**确定性生成**，没有调用 LLM；其中包含章节数卡片、键值概览、Markdown 概览和前几个标题列表。日期、标签、字数、标题在相邻区域重复出现；中文文案却叫“AI 笔记摘要”。[S6][S7][S21]

这同时是信息层级问题与语义问题：

- 用户付出了更多阅读成本，却没有获得相应的新信息；
- “AI 摘要”承诺的是内容理解，实际展示的是统计与摘取；
- 大纲被介绍大纲的卡片挤下去；
- 短笔记也会被整理成一组仪表盘式区块。

**建议直接做减法：**

1. 将“大纲”“关联”“属性”设为同级、可直接到达的上下文入口。
2. 大纲页首先就是大纲，无需先经过元数据。
3. 属性页保留字段编辑，把创建时间等低频信息折叠或后置。
4. 从默认上下文面板移除这组生成式统计卡片；若仍保留，应诚实命名为“文档概览”，并去掉重复内容。
5. 真正的 AI 摘要由用户主动请求，展示来源版本、生成状态和是否过期；没有内容理解就不叫 AI 摘要。

这是无需实测即可做出的设计判断：**重复展示已有信息，不会因为采用卡片或 AI 风格而变得更有用。**

### 3.4 局部标签编辑的动作范围超出了人的预期

在当前笔记属性里的标签上，双击或点铅笔进入“重命名标签”；提交后，代码先修改当前笔记，再调用跨笔记重命名，遍历其他笔记并更新标签。入口文案只是“重命名标签”，影响其他笔记的数量是在成功后通知。[S5][S8]

用户很可能认为自己在编辑“这篇笔记的一个标签”，实际操作的是全局分类系统。这是作用范围不透明，不只是少了一个确认框。

**建议：**

- 本篇属性默认提供“移除此标签”“替换本篇标签”。
- “在整个文库重命名标签”放到标签管理入口，或者明确标出作用范围的二级操作。
- 在执行前展示受影响数量；执行后支持批次结果、失败重试与恢复。
- 不把逐篇顺序写回包装成一个天然原子的动作。当前路径可能在中途失败，导致部分笔记已更名、部分未更名。

相同原则应适用于文件夹移动、导入覆盖、批量删除和批量 AI 修改：**界面承诺的对象范围，必须等于实际写入范围。**

### 3.5 阅读、编辑、专注和窗口最大化需要清楚分工

当前阅读模式主要通过 `effectiveReadOnly = readOnly || readingMode` 改变编辑能力；桌面格式工具栏仍被渲染，内部把格式操作禁用。专注模式另有界面隐藏和过渡状态，宿主又有自己的窗口与侧栏状态。[S2][S4]

这里有三个不同问题：

- 阅读的人需要轻松浏览，不需要面对大量灰色编辑按钮；
- 专注写作的人仍然需要编辑能力，只是不希望被导航与管理界面打断；
- 窗口变大只改变空间，并不自动成为专注。

**建议建立两个独立轴：**

```text
内容交互：编辑 / 阅读
界面密度：普通 / 专注
```

窗口最大化单独存在，不再与上述模式混称。外部只读权限也单独表达，不伪装成用户可随时切回的阅读偏好。

- 阅读：隐藏格式工具，保留查找、链接、目录、引用和“编辑”。
- 专注编辑：隐藏文库导航与默认上下文栏，仍可通过选区、块菜单与快捷键操作。
- 专注阅读：优先正文与章节导航。
- 切换模式保留文档、滚动锚点、选区及历史；有未保存内容时仍可阅读，但保存异常必须继续可达。

目前没有看到与 Obsidian 对等的一套显式 Source mode 产品入口。若增加源码模式，应定位为 Markdown 的直接控制与兼容兜底，不能借此要求普通用户承担三种编辑模式的选择成本。

### 3.6 “已保存”首先是一种信任关系

用户不关心内部有多少队列、重试与乐观锁；他们关心现在能否放心离开。

当前编辑器内有五类状态：已保存、保存中、未保存、失败、冲突；向宿主输出的类型却只有 `saved / saving / dirty`。Workbench 状态栏按三态展示，不能准确表达编辑器中的失败与冲突。[S2][S3]

**建议：**

- 正文标题区、标签条、状态栏共用同一个保存状态，而不是分别猜测。
- 正常保存保持安静，不频繁弹成功通知，不让标签宽度随“保存中”文本抖动。
- 保存失败给出持续、就地、可操作的提示：“尚未写入磁盘，内容仍保留”，并提供重试、复制、另存为副本。
- 冲突先保留用户可见的草稿及两个版本，再让用户选择；不把“已刷新到其他版本”当作普通成功。
- “自动保存完成”必须对应当前文档版本，不能仅对应一个较早请求成功。
- 明确区分本地持久化与其他同步动作；不能因为本地保存成功就暗示全部远端工作也完成。

### 3.7 AI 应在人的意图明确时介入

当前制卡位于格式工具栏，选区又有引用到聊天，AI 修改有独立 diff 与检查点，属性页另有“AI 摘要”。能力丰富，但不同 AI 入口与对象边界需要重新安排。[S2][S4][S6]

我的建议是：

- 选中文本：默认操作这段，并能看到选区范围。
- 页面菜单：操作整篇，明确“根据全文生成卡片”“总结全文”等。
- AI 建议：在正文附近呈现差异，但不持续吞掉主要阅读空间。
- 接受建议后继续允许写作，检查点入口可回看，不要求用户一直面对操作横幅。
- 等待期间用户又编辑了正文，原建议必须重新校验或重算，不能悄悄覆盖新内容。
- 长文未完全加载时，文档级 AI 仍必须取得全文，不能把当前渲染片段当作整篇。

不要让每一篇普通笔记都像一个 AI 功能启动页。**AI 的价值应由它帮用户完成了什么体现，而不是由常驻入口的数量体现。**

---

## 四、桌面端应该如何组织

### 4.1 默认结构

以下是目标结构示意，不是当前界面截图：

```text
应用导航 / 窗口管理
┌────────────────┬──────────────────────────────┬──────────────┐
│ 文库导航       │ 文档标签 / 返回与前进       │ 上下文入口   │
│ 搜索、最近     ├──────────────────────────────┤ 大纲         │
│ 文件夹、收藏   │ 标题                 页面菜单│ 关联         │
│                │                              │ 属性         │
│                │            正文              │              │
│                │                              │ 默认按需打开 │
└────────────────┴──────────────────────────────┴──────────────┘
```

三条硬规则：

1. **文档只有一套稳定的页面操作。** 不让宿主标题栏和正文标题区各自再长出一套同义按钮。
2. **导航可退出视野。** 文件树用于找文件；找到后，用户应能舒服地读写。
3. **辅助区打开后仍需保障正文的有效宽度。** 空间不足时切换为辅助页面，而不是挤出一列勉强能输入的正文。

### 4.2 不同桌面状态的安排

| 状态 | 推荐行为 | 应避免 |
|---|---|---|
| 宽窗口、单篇 | 文库导航可见，正文有合理最大行宽，上下文按需开启 | 默认三栏全部铺开、正文随屏幕无限拉长 |
| 中等窗口 | 优先保正文；一侧管理区可折叠 | 左侧全局导航、Finder、右侧属性共同挤压正文 |
| 窄窗口 | 保留桌面交互语义，收起导航、减少按钮；必要时辅助页替换正文 | 只因 pane 窄就挂一个全窗口底部手机工具条 |
| 双栏对照 | 两个正文区域、一个明确活动 pane；上下文跟随活动篇 | 两套全局工具、相同快捷键同时影响两篇 |
| 同篇多视图 | 文档版本共享，滚动与选区独立 | 用 noteId 代替视图身份，导致两边一起滚动或覆盖 |
| 文件树关闭 | 返回/前进、快速切换仍可达 | 把文档导航只放在文件树工具栏里 |
| 多标签溢出 | 明确的标签列表入口，能识别脏状态、固定状态和同名路径 | 只隐藏滚动条，让用户猜有多少篇未关闭 |
| 专注 | 恢复正文优先，退出入口稳定，辅助命令仍可调用 | 仅把窗口放大；退出时丢失原侧栏状态 |

### 4.3 空间分配与视觉品味

这里不建议先做一轮全局换色、加阴影或改圆角。

更有收益的是：

- 用排版区分正文与管理界面，少用一层层卡片来区分每块信息。
- 标题是文档内容的起点，不是仪表盘抬头；长标题自然换行，辅助信息让位。
- 元数据没有内容时，不常驻“暂无标签”等提醒；空白本身可以是正常状态。
- 标签很多时压缩为前几个及数量入口，避免正文首屏被 chip 推走。
- 正文行宽使用可配置上限；表格、代码和大图可有局部宽展示，不强迫整个页面横向滚动。
- 选区浮条、块句柄、链接提示避免同时抢占光标附近的狭小区域。
- 更高层级的 UI 不应该比用户的文字更亮、更密或更有动效。
- 已有 reduced-motion 支持应保留；动效用于解释空间关系，不用于庆祝每次统计变化。

“看起来干净”的实质是信息有优先级。把按钮缩小、文字变灰、滚动条隐藏，并不会真正降低复杂度。

---

## 五、移动端应从四种人的状态重新设计

### 5.1 浏览与阅读

```text
返回      笔记标题                  更多
───────────────────────────────────────
正文
正文
正文
───────────────────────────────────────
笔记切换 / 搜索          编辑          关联入口
```

- 不显示桌面标签条、文件树与格式工具栏的完整组合。
- 标题区保持紧凑；完整标题在正文起点可读，不复制多份。
- 阅读时点文字不应意外弹键盘；进入编辑有明确入口。
- 普通链接点击打开，长按提供上下文操作；阅读与编辑中的链接手势需可解释。
- 图谱、属性、文件列表都是单独任务页，返回恢复原阅读位置。

### 5.2 输入与编辑

```text
返回/完成      当前笔记             更多
───────────────────────────────────────
正文
当前光标附近的内容
───────────────────────────────────────
+       常用格式       撤销/重做       收键盘
───────────────────────────────────────
系统键盘
```

- 键盘上方只出现一条编辑工具条，取代当前不需要的底部导航。
- 默认保留少量动作，`+` 稳定可见，低频命令进入有文字的完整菜单。
- 常用动作允许自定义排序；默认配置服务多数人的输入，而不是展示全部能力。
- 工具条横向滚动可以保留。Obsidian 也采用这种方式；问题在于没有明确入口、动作不可发现、滚动与点击互相干扰，而不是横向滚动本身。
- 工具条展开不能让光标消失在键盘或菜单下；选择完操作后回到刚才的输入位置。
- 当前代码有 pointer/mouse 防抢焦点、VisualViewport 偏移和 44px 控件设计，应保留这些已有基础。

### 5.3 选中内容

- 避免系统复制/粘贴菜单、Crepe 格式浮条和“引用到聊天”同时在同一位置竞争。
- 原生选择手柄应优先可用；扩展动作靠一组明确入口提供。
- 引用到聊天、生成卡片、AI 修改必须标出“选中内容”还是“整篇”。
- 图片选择器和链接输入结束后，恢复原文档、原选区；取消不生成空节点。
- 手机的块操作不能依赖 hover 小手柄，必须从当前块的菜单到达。

### 5.4 辅助页面

当前代码已将窄屏属性/文件等区域做成全屏内联子页，这个方向合理。要继续把它做完整：

- 顶栏只保留一个返回。
- 点大纲章节属于“完成选择”，应返回正文并定位，不让用户额外退出一层。
- 改属性属于“编辑辅助信息”，可留在该页；返回保存页面自身滚动位置。
- 返回优先关闭最上层临时交互；键盘关闭遵循平台原生行为；只有没有待处理层级时才离开笔记。
- 普通返回不丢文本，也不自动变成每次都弹确认；失败或明确丢弃时才需要决策。

### 5.5 将“宽度”和“输入方式”拆开

当前 `isTouchEditingSurface = isSmallScreen || isCoarsePointer` 将窄屏与触摸一起用于切换编辑工具界面；底部工具条又 portal 到 body。[S2][S9]

这混合了三个不同维度：

1. **可用空间**：决定并排还是替换、多少按钮可见。
2. **输入能力**：决定是否需要触摸块菜单、更大命中区。
3. **键盘/编辑状态**：决定工具条放在哪里、是否替换导航。

窄桌面 pane 仍然可能用鼠标和硬件键盘；平板宽屏也可能用触摸；外接键盘时手机不应制造一块“软键盘空位”。建议将这些维度分别表达，再派生布局。

这比继续增加 `max-width` 补丁更接近长期解法。

---

## 六、功能与微交互：每个动作都应有自然的下一步

| 场景 | 建议的具体行为 |
|---|---|
| 新建笔记 | 从文件夹发起则继承位置；从全局发起用稳定的默认位置。创建后直接输入，标题允许稍后补充 |
| 空笔记 | 简短输入提示即可；模板是可选帮助，不用大卡片占据第一屏 |
| 标题编辑 | Enter 完成，Esc 取消；中文候选确认不等于提交；保存失败留在就地错误与可恢复输入 |
| 快速切换 | 名称、路径和最近使用足以区分同名笔记；方向键选择、Enter 打开；无结果可直接新建 |
| 全文搜索 | 与按文件名查找清楚区分；结果显示命中上下文；打开后定位，返回保留查询和滚动 |
| 搜索语法 | `tag:`、`path:`、属性过滤可提供可视提示；不要强迫普通用户学语法 |
| 文内查找 | 只作用于当前活动文档；关闭回到合理焦点，不让后台面板抢快捷键 |
| 替换 | 当前/全部范围清晰；替换计入撤销；无匹配、非法正则和只读状态有就地解释 |
| 撤销 | 用户能理解“一次撤销”是什么：一次格式动作、一次替换、一段 AI 接受；不要悄悄跨篇操作 |
| 大纲 | 直接导航、支持折叠；同名标题能区分；点选后进入正确章节，不能只高亮目录行 |
| 双链 | 标题与路径消歧；未解析链接可创建；完成后原引用与新文档形成闭环 |
| 背链 | 默认显示少量有意义的上下文；点击到具体引用处；明确区分真实链接、未链接提及与扫描范围 |
| 图谱 | 默认围绕当前笔记展示局部关系；过滤、返回来源和列表替代入口比动画更重要 |
| 标签 | 本篇关联与全库标签维护分开；移除关联不等于删除标签或修改其他笔记 |
| 属性 | 常用字段优先、空字段按需添加；大纲不埋在字段后；失败保留用户输入 |
| 链接插入 | 选中文本作为链接文字；确认后恢复选区；编辑已有链接优先于再插入新链接 |
| 图片与附件 | 展示处理中/失败/重试；取消选择不改正文；缺失资源给可修复状态，不能只留破图 |
| 列表与任务 | Enter、空项退出、缩进、退格行为一致；点击勾选不能把阅读滚动变成编辑跳动 |
| 表格 | Tab 增行与退出行为清楚；手机横向滚动限定在表格；删除整表与清空单元格不能混淆 |
| 公式、代码 | 编辑入口与渲染状态明确；离开/取消可预期；错误源码保留，不静默吞掉 |
| Callout / Toggle | 用块语义做转换与移动；展开状态不应让内容在导出或往返中丢失 |
| 选区引用与资源 ID | 保存、重开、重命名后仍指向原资源；可见文案与稳定标识分开 |
| 导入 | 先说明冲突策略和范围；进度区分笔记/附件；部分失败有结果清单，避免一句“导入失败” |
| 导出 | “当前篇”“全文库”明确；成功说明实际包含的资源范围；导出文件可再次导入 |
| 删除与回收站 | 明确删除的是当前文档还是一个视图；外部删除后草稿能另存；恢复后正常回到文档 |

这些不是新增功能清单。很多能力已经存在，真正需要调整的是默认值、作用范围、完成后的落点和异常的出路。

---

## 七、从源码能直接指出的实现问题与风险

本节区分两种证据：

- **代码可确定**：调用、状态或参数契约本身已经不符合预期。
- **条件风险**：代码存在明确时序或多实例缺口，影响在给定条件下出现；不把推演写成现场复现。

### C1．“未保存”检测晚于真实输入，留下离开与外部更新的判断窗口

**优先级：P0；证据：明确的时序风险。**

Crepe 的 Markdown 变更通知采用约 250ms 防抖，连续输入会继续推迟回调。`isCurrentNoteDirty` 读取的是 `draftByNoteRef`、保存队列和在途请求，没有读取即时文档变化标志。`closeTabGate` 在查询结果为 clean 时直接允许关闭；外部更新也依赖该 dirty registry。[S2][S10][S18]

因此，在输入已进入 ProseMirror、草稿回调尚未执行期间，可能仍被判断为 clean。已有 `flushNoteDraft` 会在生命周期边界读取 live Markdown，这是正确补救，但不能替代**在决定是否关闭或应用外部内容之前**作出正确判断。

**改法：**事务产生时立即更新轻量文档版本/dirty 标志，序列化与持久化继续延迟；dirty 判断按文档版本与保存版本比较。不要为了同步 dirty 而每按一个键序列化全文。

**行为标准：**最后一次输入刚发生就关闭、切换或收到外部更新，也不能被当作无改动；失败时保留当前草稿。

### C2．手动保存的入口没有全部进入同一条队列

**优先级：P0；证据：调用路径可确定，并发影响为条件风险。**

编辑器的快捷键保存走 `handleManualSave → flushNoteDraft → queueSave`；`NoteContentView` 对 `NOTES_FORCE_SAVE` 的处理却直接调用 `handleSave(editor.getMarkdown())`。该命令已有 `isActive/readOnly` 守卫，缺口是持久化入口不统一，而不是毫无目标保护。[S1][S2]

这意味着用户认为同义的“保存”，具有不同的串行化、快照更新与反馈路径。自动保存恰在进行时，额外直达请求会增加乐观锁冲突和状态不同步的可能。

**第二轮校正：**AI 主路径已经优先使用 `replaceFullMarkdown(expectedMarkdown)`；`NoteContentView` 的包装实现会更新全文窗口、调用 `flushPendingSave`，并核验持久化结果。只有不提供该能力的兼容分支才执行 `setMarkdown → onSave`。因此不能把所有 AI 接受都归类为绕过队列；应消除不同宿主能力组合的差异。[S1][S30][S42]

**改法：**所有保存入口统一调用文档级持久化协调器；提交时绑定文档 ID 和版本，成功后统一更新状态。保留现有队列/OCC，而不是新增另一套保存机制。

**行为标准：**自动保存、快捷键、命令面板、保存并关闭和 AI 接受之间，不能出现旧版本成功覆盖新状态或重复成功提示。

### C3．Workbench 的两标签挂载上限没有学习桌面的脏标签豁免

**优先级：P0；证据：保活策略差异可确定。**

Workbench pane 的 `mountedKeys` 使用 `.slice(0, 2)`；学习桌面 `TabPanelContainer` 则在五个保活名额之外额外保留 dirty 标签。[S3][S11]

在 A 未保存、切 B、再切 C 的路径中，Workbench 的 A 可能被卸载，保存依赖卸载时的异步 flush。若写入持续失败，草稿不应仅靠即将销毁的组件与内存闭包维持。

**改法：**先统一脏标签淘汰策略；随后将草稿与保存生命周期移出 editor view。文档可以继续保存和恢复，视图可以被释放。

**行为标准：**打开再多笔记也不能以丢弃未持久化草稿换取内存下降；恢复 A 时有正确内容与明确异常状态。

### C4．强制外部更新按 noteId 广播，缺少草稿实例的保护边界

**优先级：P0；证据：多实例条件风险。**

冲突路径会发出 `notes:external-updated`，携带 `noteId` 和 `force:true`。编辑器收到 force 时清除对应待保存项并替换内容；事件没有限定目标 view/session。恢复本地版本的请求也主要按 noteId 分发。[S1][S2]

同一 JavaScript/DOM 事件域内，同一篇笔记有多个编辑实例、各有草稿时，一个实例解决冲突可能影响其他实例。仅按文档 ID 过滤可以区分不同笔记，不能区分同篇的不同编辑会话。这里的风险包括同一桌面里的多个内部窗口；不能据此声称 DOM `CustomEvent` 会自动跨独立 Tauri WebView 传播。

**已有保护：**`workspaceRegistry.getNoteEditor(resourceId, windowId)` 已严格限制带窗口归属的编辑器回退；真实正文冲突已有用户版本恢复、外部版本读取，元数据单独推进版本时也会重试。问题应限定为 force 事件、草稿身份和冲突操作的所有权，不能否认这些保护。[S1][S25]

**改法：**文档版本更新统一传播，但每个未合并草稿保留；“应用到某个草稿实例”的动作包含 view/session 标识。保留本地、保留外部、另存副本等操作都要有明确对象。

### C5．查找面板的全局快捷键可以由隐藏或另一 pane 的实例响应

**优先级：P1；证据：监听范围可确定。**

`FindReplacePanel` 在 document 捕获阶段监听 F3 和 Ctrl/Cmd+F，这两条分支没有当前 pane/可见性判断。父级只按 `isFindReplaceOpen` 挂载；标签隐藏保活时组件不一定卸载。Ctrl/Cmd+Z 的分支反而已有 `inPanel` 检查。[S12][S2]

**影响：**后台查找框或另一 pane 可以争抢焦点；多个面板可能响应同一次导航。专注模式还在 window 捕获阶段处理 Esc；应检查它与上层菜单的消费顺序，而不能只检查每个组件“是否有 Esc”。

**已有保护：**Android 已有 overlay/view/navigation 三层优先级和可见性守卫；移动工具条也有可见性与单 owner 管理。第二轮建议复用这些机制并补齐具体接线，不另建一套相互竞争的返回管理器。[S2][S28]

**改法：**共享活动视图和顶层交互所有权；隐藏 view 不监听用户命令；Esc 一次只结束当前最上层交互。查找字段内的撤销也应区分“撤销输入查询”与“撤销替换”。

### C6．重复标题的大纲定位存在实质性歧义

**优先级：P1；证据：代码与 ProseMirror 遍历语义可确定。**

大纲内部记录了同名标题的 `occurrence`，但点击事件只传文字、级别和 noteId，没有传 occurrence。`CrepeEditor.scrollToHeading` 遍历匹配时写入 `targetPos`，并把 `return false` 当作结束遍历。[S5][S10]

ProseMirror 的 `descendants` 回调返回 false 是不再遍历该节点的子节点，不是终止后续兄弟节点。因此，后面同名同级标题会继续覆盖 `targetPos`。

使用当前依赖的三个“总结”标题进行纯模型推演，访问位置为 `[0, 4, 8]`，该写法最终选中 `8`。这是算法语义验证，不是 UI 实测。

**改法：**优先使用稳定标题标识；最少把 occurrence 贯穿解析、点击、定位与高亮。模糊匹配必须是显式回退，不应在精确目标存在时偷偷跳到相似章节。

### C7．长文的“全文”和“已加载片段”没有贯穿所有能力

**优先级：P1；涉及修改全文时按 P0 处理。**

笔记默认可按约 600 行窗口加载。保存有 `composeWindowedSave` 保留未加载后缀，但文内查找读取当前 ProseMirror doc，嵌入属性页的大纲读取 `visibleContent`。Workspace 属性页先读取全文，又接收编辑器的片段级实时事件。[S1][S2][S5][S12][S13]

制卡 helper 已优先使用 `getFullMarkdown`，但全文 API 是在宿主为 ACR 注册时包装的；编辑器内部传给桌面/移动工具栏的仍是自己的 `editorApi`，因此不能仅因为 helper 支持全文就认为接线已完整。[S1][S2][S14]

**复核范围：**`getFullMarkdown`、`replaceFullMarkdown` 以及 expectedMarkdown 校验确实存在，应保留。查找插件还正确处理了跨格式 text node 的匹配、非文本原子的边界、非重叠替换和正则捕获组。剩余问题是全文来源、渲染位置与各调用方的连接，不是从零实现搜索算法。[S42][S43]

**人的体验：**“搜索不到明明存在的内容”“目录后半段不见”“整篇制卡只覆盖前半篇”，都会被理解为文档不可靠。

**改法：**

- 文档级功能统一从 document session 取全文。
- 查找/大纲到达未加载区域时，先扩展到目标，再定位。
- 界面显示的是全文统计还是片段统计必须明确。
- 只对片段执行的替换不能叫“全部替换”；全文写入必须校验原始版本和未加载部分。

### C8．窄屏大纲点击没有完成“回到正文”的任务

**优先级：P1；证据：接线缺口可确定。**

窄屏属性打开时正文 main 被隐藏。大纲点击只广播滚动事件，没有请求宿主关闭属性子页；`NotesContextPanel` 也没有完成章节选择的宿主回调。[S1][S5]

**改法：**大纲选择作为导航动作交给宿主：记录目标 → 返回正文 → 视图恢复后定位。宽屏可留侧栏，窄屏应完成返回；不能要求用户猜刚才点了有没有生效。

### C9．属性页有“新身份配旧正文”的中间状态

**优先级：P1；证据：状态逻辑可确定。**

`NotesPropertiesTab` 的 `content` 没有与 noteId 配对；从 A 切到 B 时，新的 note 元数据会进入渲染，但旧 content 要等 B 的异步读取完成才替换。读取失败则直接 return，没有对应错误态。[S15]

**已有保护：**该页使用 `dstu.watch` 刷新当前元数据，切篇时清空 `liveNode`，内容请求也有 cancelled 守卫；`NotesContextPanel` 切篇会重置 liveContent。因此不是“任何慢请求都会回写错误篇”，而是等待新请求期间仍可把旧正文交给新身份，以及失败没有独立状态。

此外，大纲解析优先取 `liveContent`，而文档概览取 `effectiveActive.content_md`，同一上下文面板内部也有不同的新鲜度来源。[S5]

**改法：**身份与内容快照一起管理；切篇后不把旧内容作为新篇可操作的大纲；加载、失败与重试就地表达。实时内容应带版本，不能仅凭非 null 就永远优先。

### C10．中文输入法的提交语义没有一致处理

**优先级：P1；证据：处理方式差异可确定。**

标题编辑对 `isComposing/keyCode 229` 做了保护；自定义属性也有组合输入守卫。快速切换、上下文标签添加/改名，以及查找/替换输入框的 Enter 分支没有同样的统一保护。[S16][S5][S12][S31]

**改法：**候选词确认与业务提交分开，统一 composition 守卫。不能在用户确认中文候选时打开搜索结果、提交标签，尤其不能触发全库改名。

### C11．保存状态在宿主边界降级

**优先级：P1；证据：类型与渲染可确定。**

内部五态经三态回调输出，失败和冲突不能准确呈现在标签与状态栏。应输出完整状态、版本和可恢复动作，而不是由各层组合几个布尔值推断。[S2][S3]

第二轮进一步确认：Workbench 的 `getTabSaveState` 对 note 直接把 dirty registry 映射为 `dirty/saved`，仅 mindmap 分支单独识别 saving。需要统一的是正文、标题、标签、属性及宿主共同消费的事实；不必把所有状态词塞进每个标签，只需使异常有准确且持续的入口。

### C12．正常阅读仍背负编辑工具界面

**优先级：P1；证据：渲染路径可确定。**

阅读状态继续渲染桌面格式栏，仅以 `readOnly` 禁用其格式按钮。移动工具条则有更明确的隐藏条件，造成两端阅读语义不够一致。[S2][S4]

**改法：**阅读模式给阅读任务需要的控件；不能把“不可编辑”简单呈现为“编辑界面里所有按钮变灰”。

### C13．属性和标签写回没有使用已有的元数据乐观锁

**优先级：P0（并发覆盖）；证据：调用缺口确定，覆盖发生依赖并发。**

`dstu.setMetadata(path, metadata, expectedUpdatedAt?)` 已接受版本参数，Rust `update_note_metadata_with_conn` 已在同一 UPDATE 内执行可选 CAS。`NotesPropertiesTab` 写 tags/props、`NoteContentView` 写标题和 tags 时没有传入第三个参数。[S1][S15][S17][S32]

props 是整对象替换。两个视图从同一旧对象开始，先后增加不同键时，后一次可能覆盖前一次；watch 的作用是更新显示，不能代替写入前的版本校验。正文 OCC 能识别元数据推进版本，也不能反向保护这些元数据写入。

**改法：**先接入现成 CAS；再按字段记录 `base/local/latest`，不同键可重放 patch，同键不同值必须提示冲突。保留现有键名、数量、类型校验。版本字符串与前端毫秒时间的转换应集中在 adapter，不能各处自行猜测格式。

### C14．改标题的入口不同，双链维护与失败反馈也不同

**优先级：P1；大批量写回保护按 P0；证据：调用链与结果字段可确定。**

文件树 `renameTreeItem` 调用 `dstu.rename` 后执行 `syncWikiLinksAfterNoteRename`；正文标题使用 `dstu.setMetadata({title})`，不经过该 helper。后端标题更新本身不等同于改写所有来源笔记中的 `[[旧标题]]` 字符串。[S1][S3][S33]

现有 helper 已保留 alias/heading，使用 OCC，跳过 dirty 来源，结合后端背链与客户端搜索发现来源。这些是应保留的正确工作。第二轮确认两个剩余缺口：

1. helper 返回 `scanFailed`，宿主目前只处理成功数、dirty 跳过数和失败数，没有处理“根本未能扫描”。
2. 来源最终 `.slice(0, 256)`，summary 没有截断标记或继续游标。不能把“本批成功”表达为“全库引用均已同步”。

**改法：**统一 renameNote 服务；将标题成功和引用同步状态分开。补齐扫描失败、部分完成、待处理来源与重试；跨篇更新继续逐篇 OCC，不能为了完成批次绕过脏草稿。

### C15．已有“保存并关闭”协议，没有完整接入 Workbench 内部标签

**优先级：P0；证据：协议与调用差异可确定。**

`ContentCloseConfirmation` 已提供 save/discard/cancel 三态；Learning Hub 的 `closeTabGate` 会根据 save handler 提供保存选项。Workbench `NotesWorkspaceApp.closeTab` 仍调用布尔兼容接口，只确认是否丢弃关闭。[S3][S18][S23]

另外，dirty registry 已支持同资源多个 checker 与多个 save handler，绝不存在“后注册覆盖前注册”的单槽问题；但保存 handler 使用 `Promise.all`。标题组件注册了 dirty checker，却没有对应的标题 save handler；不能认为“注册了正文保存”就能保证该资源所有编辑字段都提交成功。[S22][S44]

`saveContentNow` 在所有 handler resolve 后直接返回 true，`confirmTabClose` 直接以该结果放行，没有再次核对 dirty 或当前 revision。因此，即使使用了三态 gate，异步标题仍未完成、或等待保存期间又发生输入时，仍缺少最后的关闭屏障。这里是可确定的协议缺口，不能描述成已有 gate 就完全解决了关闭安全。[S18][S22]

**改法：**让全部关闭入口复用三态协议和文档级保存屏障；为未提交标题/属性提供明确提交契约。关闭视图、丢弃草稿、删除文档分别处理。保存失败保留窗口，取消不改变草稿。

### C16．文库导出成功与“当前正在看的内容已备份”之间有缺口

**优先级：P1；涉及丢失备份预期时优先处理；证据：入口可确定。**

Workbench 文库导出直接调用 `NotesAPI.exportNotes`；API 直接 invoke 后端，从持久化数据生成归档，该入口没有先等待打开笔记的草稿保存完成。用户刚输入或仍有失败草稿时，导出可能成功，但并不包含屏幕上的最新内容。[S38][S39][S40]

已有 ZIP 导入策略、进度事件和后端导出 staging，应保留。`includeVersions` 参数在 API 中明确已废弃，不能因为调用传了 true 就承诺备份历史版本。

**改法：**导出先确定范围与一致性边界，等待对应文档的保存屏障；失败则让用户重试，或明确选择“仅导出已保存版本”。导入覆盖前处理受影响的打开草稿。批量 Markdown 导入已有逐项 imported/failed 返回，ZIP 文库导入需要单独评估，不能混为一种任务协议。

---

## 八、对标 Obsidian：借鉴稳定的行为模型

以下竞品事实来自官方文档；设计建议是针对 DeepStudent 的判断，不把 Obsidian 描述为没有缺点。

| 维度 | Obsidian 官方行为 | DeepStudent 应吸收的原则 |
|---|---|---|
| 编辑/阅读 | 明确区分 Reading 与 Editing；Editing 下有 Live Preview 和 Source mode | 模式有清楚语义；阅读、界面密度、写权限分别表达 |
| 标签与分组 | 支持标签组、分屏、拖拽到其他组/窗口、固定标签、恢复布局 | 打开位置与文档状态稳定；复杂布局不改变基础操作 |
| 关联视图 | Outline、Backlinks、局部 Graph 可成为 linked views | 上下文绑定文档，不应被全局活动状态随意带走 |
| 快速切换 | 名称搜索、最近笔记、方向键/Enter、新标签打开、无结果创建 | 将“找到”和“开始写”接起来，避免多余导航 |
| 命令面板 | 可搜索操作并执行，支持快捷键 | 长尾能力有统一发现入口，不必全部常驻 |
| 手机导航 | 有返回/前进、快速切换、标签数量、菜单入口 | 保持少量稳定入口，让正文获得空间 |
| 手机编辑条 | 编辑时显示，可横向滚动并自定义；收键盘后回到导航 | 按任务切换工具；自定义比无限追加默认按钮更可持续 |

官方资料：

- https://help.obsidian.md/edit-and-read
- https://help.obsidian.md/tabs
- https://help.obsidian.md/plugins/quick-switcher
- https://help.obsidian.md/plugins/command-palette
- https://help.obsidian.md/mobile
- https://help.obsidian.md/plugins/outline
- https://help.obsidian.md/plugins/backlinks
- https://help.obsidian.md/plugins/graph

**不建议照搬的部分：**

- 不为形式上的对标立即引入更多视图、更多模式或更复杂的标签玩法。
- 不把 Markdown 标点知识变成初学者的使用门槛。
- 不把所有低频功能塞入命令面板后就宣称解决了可发现性；常用动作仍需可见入口。
- 不复制插件生态的全部扩展自由度，先稳定应用自己的行为。
- DeepStudent 的学习引用、AI 修改和制卡有自身价值，应围绕学习任务组织，不必假装自己只是纯笔记库。

还有一个具体冲突：Obsidian 用 Ctrl/Cmd+E 切阅读/编辑，当前 Milkdown 工具栏将 Mod-E 表达为行内代码。不能为了“像 Obsidian”直接覆盖现有快捷键；需要统一快捷键定义、冲突提示和迁移方式。[S4]

---

## 九、状态设计：让异常也符合人的理解

### 9.1 状态应分层，而非堆叠布尔值

```text
文档状态：加载中 → 可用 → 已删除/不可访问
保存状态：clean → dirty → saving → saved
                              └→ failed / conflict
视图状态：active / inactive，编辑 / 阅读，普通 / 专注
导航状态：正文 / 文件列表 / 大纲 / 属性 / 关联
临时交互：查找、块菜单、链接输入、AI 差异、原生选择器
```

这些状态可以组合，但应规定合法组合。例如：隐藏标签不能拥有全局查找焦点；属性替换正文时，正文不能继续占有移动工具条；保存失败不能被阅读模式切换隐藏。

### 9.2 各状态的产品标准

| 状态 | 用户应该看到和能做什么 |
|---|---|
| 首次加载 | 与正文结构接近的轻量占位；不能出现可编辑的上篇内容 |
| 切篇加载 | 标题与内容身份一致；保持空间稳定；旧请求完成不覆盖新篇 |
| 空笔记 | 直接写；没有元数据也正常 |
| 加载失败 | 就地原因、重试、返回；不把失败当空文档 |
| 已保存 | 安静的确认，可放心离开 |
| 未保存 / 保存中 | 不阻断输入；当前版本的进度可理解 |
| 保存失败 | 内容仍在、可以重试/复制/另存；不能只弹一次 toast |
| 冲突 | 两版都在，明确来源与差异；选择后落盘结果可确认 |
| 外部更新、当前 clean | 原位更新，尽量保持阅读位置，避免闪回顶部 |
| 外部更新、当前 dirty | 不静默覆盖；保留草稿，提供解决入口 |
| 只读 | 告知原因；仍能阅读、查找与引用；不提供看似可用的写操作 |
| 长文加载更多 | 正文仍可用；加载错误只影响该步骤；全篇命令语义不缩水 |
| 外部删除 | 无草稿时显示已删除；有草稿时可另存，不让用户在幽灵文档上反复保存 |
| 离线 / 网络不可用 | 本地笔记继续工作；依赖网络的 AI/资源分别解释，不能把整个编辑器判为不可用 |
| 标题或属性提交失败 | 保留输入、就地纠错；不要假显示为已成功的新值 |
| AI 执行中 | 可取消或离开；范围与版本明确；完成后不强抢焦点 |

### 9.3 必须统一的生命周期约束

1. 能否关闭由文档事实决定，不由某个按钮的局部布尔值决定。
2. 视图卸载不等于草稿销毁。
3. 同篇多视图不能共享一个可变选区或滚动位置。
4. 页面命令、菜单、快捷键、AI 接受都走同一文档命令与保存边界。
5. 任何异步结果都携带文档身份和基准版本；回调前再次确认目标。
6. 渲染窗口只影响显示性能，不改变“整篇笔记”的业务含义。

---

## 十、改造路线：先改变默认体验，再做长期抽象

### P0：保存与作用范围的底线

| 工作包 | 具体内容 | 完成标准 |
|---|---|---|
| P0-A 文档写入一致性 | C1、C2、C11：即时 dirty、统一保存入口、完整状态向上输出 | 刚输入就离开也能识别；所有保存方式指向同一版本；错误不被包装成已保存 |
| P0-B 草稿与视图隔离 | C3、C4：脏标签保活、保存不依赖视图、冲突保护每个草稿实例 | 切第三篇、关辅助窗、同篇多开都不隐式丢弃内容 |
| P0-C 命令目标正确 | C5 及全文写操作：活动 pane、临时交互所有权、文档/片段范围 | 一次命令只影响预期对象；隐藏 view 无权抢焦点或改内容 |
| P0-D 标签改名范围 | 局部替换与全库改名分离，批次结果明确 | 本篇操作不隐式修改其他笔记 |

### P1：最能改变日常手感的一轮设计收敛

| 工作包 | 具体内容 | 完成标准 |
|---|---|---|
| P1-A 正文优先 | 精简默认格式条，移除重复概览卡，标签/统计后置 | 打开普通笔记首先进入阅读/写作，而不是管理面板 |
| P1-B 上下文重组 | 大纲、关联、属性各自有直接入口；窄屏选择后返回正文 | 找章节不经过元数据；同名标题与长文目标正确 |
| P1-C 手机四状态 | 浏览、输入、选区、辅助页分别定义 chrome | 无双层底栏、无重复返回、无整套桌面工具长期挤占正文 |
| P1-D 导航与搜索闭环 | 快速切换、新建、全文命中、返回原结果、同名消歧 | 找到内容后直接继续任务，不反复重新搜索 |
| P1-E 模式与焦点 | 阅读隐藏编辑工具；专注保留能力；Esc 按层级处理 | 模式切换不丢位置，操作对象始终清楚 |
| P1-F 内容与语言边界 | C6–C10：标题锚点、全文 API、属性身份、IME | 正确中文输入、可靠目录、全文能力无片段缩水 |

### P2：稳定之后再投入的能力

- 手机常用命令配置、快捷键个性化。
- 更好的局部图谱、背链分组与关系探索。
- 文档级与选区级 AI 工作流优化。
- 真正的内容摘要与摘要版本管理。
- 长文的增量解析、索引、视图恢复和性能观测。
- 编辑器 adapter 与候选底座 PoC。

**可以先交付的一组明显改善：**移除默认重复摘要卡；让大纲直接到达且点后回正文；阅读模式隐藏格式条；标签局部编辑不做全局传播；把桌面常驻格式工具缩为少量入口。这些改动能改变日常体验，不需要等待大规模架构重写。

与此同时，保存与命令范围问题应在同一轮优先处理，不能只完成视觉减法。

---

## 十一、工程组织建议

不建议再把更多独立状态堆进 `NotesCrepeEditor`。也不建议第一步就重写一个完整编辑器框架。

可以沿现有边界逐步收敛：

### 11.1 文档会话

集中管理文档 ID、全文、当前版本、持久化版本、草稿、保存队列、外部更新与冲突。首先复用既有 DSTU/VFS 和 OCC 逻辑，把事实来源收拢。

Rust 侧已存在内容/元数据更新的乐观锁与事务/保存点，正文和链接索引有原子更新路径。这些是可靠性基础，应继续使用；本轮问题主要是上层入口和生命周期没有完全共享这些边界。[S17]

### 11.2 文档视图会话

独立保存 viewId、pane/window、选区、滚动锚点、查找状态、阅读方式与当前辅助页。视图可以卸载，文档草稿不能因此失去主人。

### 11.3 命令定义

一条命令至少回答：

```text
作用对象是什么？
需要哪个文档和哪个视图？
现在能否执行，不能执行的原因是什么？
是否修改内容，是否进入撤销历史？
执行后焦点在哪里？
是否需要全文，成功如何确认？
```

先收口保存、查找、插入链接、图片、格式与 AI 接受等高价值动作，不必一次迁移所有命令。

### 11.4 交互层所有权

继承已有活动实例、移动工具条 owner、Android back coordinator 和 focus scope 的思路，统一管理谁有权消费全局按键、显示 body portal、捕获返回。

一个 view 被隐藏时，应释放临时交互所有权；恢复时可以恢复查找文本等状态，但不能自动抢焦点。

### 11.5 编辑器适配层

建议逐步明确的能力包括：全文与可视片段读取、版本、选区捕获/恢复、格式操作、块操作、定位、历史、焦点、只读、持久化确认。

若未来做 BlockNote/Tiptap PoC，至少比较以下语料往返：

- 标题与重复标题、列表与任务、表格；
- 行内/块级公式、代码、callout、toggle；
- 图片、附件、wikilink、标题锚点；
- AI 引用、学习资源 ID、缺失资源；
- 中文输入、撤销重做、选区与滚动；
- 保存、关闭重开、导出再导入、外部修改。

只有在明确的体验瓶颈无法通过现有底座合理解决，且候选方案确实保持这些语义时，再决定迁移。单凭组件代码长或 UI 难看，不足以支持换底座。

---

## 十二、改造后的行为验收清单

这是一组未来实施的完成标准，不是本次调研的实测要求。

| 编号 | 使用路径 | 通过标准 |
|---|---|---|
| A01 | 从资源库/Workbench/搜索分别打开同篇 | 文档操作语义一致，返回各自来源上下文 |
| A02 | 新建普通笔记 | 直接输入，不必先补元数据或选模板 |
| A03 | 刚输入就切篇/关页 | 当前输入被识别，持久化失败时可恢复 |
| A04 | 自动保存同时触发手动保存 | 版本顺序正确，无多条竞争保存路径 |
| A05 | 保存失败后切过多篇 | 草稿仍可找回，标签与正文状态一致 |
| A06 | 同篇开两视图并发生冲突 | 不静默覆盖另一视图草稿，各自位置保留 |
| A07 | 双栏分别打开查找 | Ctrl/Cmd+F、F3、撤销只影响当前目标 |
| A08 | 隐藏标签保留查找/菜单状态 | 不消费当前页面快捷键，不残留 body 浮层 |
| A09 | 同名同级标题、大纲、双链锚点 | 跳到明确目标，不落到最后一个同名标题 |
| A10 | 超过窗口行数的长文 | 查找、大纲、制卡等全文能力覆盖后半篇 |
| A11 | 窄屏打开大纲并选章节 | 回正文并准确定位，无多余返回操作 |
| A12 | 切篇时属性读取慢/失败 | 不显示属于上篇的可操作目录，能重试 |
| A13 | 中文输入搜索/标签/标题 | 候选确认不触发业务提交 |
| A14 | 阅读/编辑/专注来回切换 | 对应工具正确，位置与内容历史保留 |
| A15 | 当前篇替换标签 | 只影响当前篇；全库操作另有范围说明 |
| A16 | 插入链接/图片后取消或切篇 | 不误插到其他笔记、不插空节点；已上传未引用资源有后续处理 |
| A17 | AI 建议等待期间继续编辑 | 接受时校验基线，可撤销，不覆盖新输入 |
| A18 | 搜索打开命中再返回 | 查询、结果位置和过滤条件恢复 |
| A19 | 从 320/375/390/768 到宽桌面容器 | 布局有明确优先级，无控件盖住正文；窄桌面不误用手机键盘逻辑 |
| A20 | 手机键盘、选区、菜单、返回组合 | 一次返回结束一层，无重复工具条，光标可见 |
| A21 | 图片/双链/公式/表格/引用往返 | 保存、重开、导出导入后语义与资源身份保留 |
| A22 | 外部删除、只读、离线、加载失败 | 每种状态都有符合任务的恢复路径 |
| A23 | 文库导入部分失败 | 已完成、未完成、冲突项清楚，可继续处理 |
| A24 | 键盘与辅助技术使用 | 菜单可导航、焦点可见、按钮有名称、状态不只依赖颜色 |

性能目标应先建立测量基线。可以把“热切篇反馈约 100ms 内”“普通输入不出现明显长任务”“打开辅助区不改变当前阅读锚点”作为初始设计预算，但这些不是当前产品的已测数据，也不应以牺牲全文正确性实现。

---

## 十三、证据边界与历史修复的处理

### 本轮源码基线

- 静态仓库：`helixnow/deep-student`。
- Commit：`69fbed572187b2d4470d3df61e9abdc4b74da181`。
- 用户本机桥接读取的 `NoteContentView.tsx`、`NotesCrepeEditor.tsx` 快照与本轮静态仓库对应文件一致。
- 本机 `NotesContext.tsx` 快照与静态仓库不同，存在额外并发/刷新修复；没有把静态版本的该文件问题直接当作用户最新工作区的问题。
- 第二轮又读取了本机该文件的 diff，确认已有按 noteId 串行的 pending saves、内容读取 generation、保留已加载正文及晚到响应守卫。不能建议用另一个同类队列覆盖这些在途工作。
- 除已逐文件核对的路径外，用户工作区仍可能与固定 commit 不同。本文 AI 等调用链细节以固定 commit 为准；涉及它们的剩余风险必须在实施前重比本机 diff，不能把静态结论直接等同于最新工作区结论。
- 其他文件主要以该 commit 为依据；进入实施时应对照届时工作区，不覆盖已有改动。

本报告按用户补充要求，以源码和设计分析为交付，不要求实测作为判断前提，也不声称本轮完成当前版本 UI 复现。前期已运行的八个现有前端测试文件共 81 项通过，只说明这些已覆盖逻辑通过，不证明整体手感或本文新指出的边界已经正确。

### 证据分级与复核结果

为避免与源码索引 `[S1]` 等混淆，以下证据等级使用 `E-` 前缀：

| 等级 | 含义 | 本文使用范围 |
|---|---|---|
| E-S1 | 当前固定 commit 的源码确定事实 | 类型、调用、条件分支、缺失参数、事件载荷 |
| E-I1 | 由源码推导的条件性影响 | 快速切换、多实例、并发、失败和长文本边界 |
| E-D1 | 产品与设计判断 | 默认信息密度、命令层级、流程落点；明确作为建议 |
| E-C1 | 竞品官方资料 | 第八章；第二轮再次读取所列官方页面 |
| E-R2 | 历史真实环境记录 | 仅用于说明历史修复，不证明当前 UI |
| E-U1 | 本次未验证 | 当前像素、真机软键盘、选择手柄、VoiceOver/TalkBack、系统进程终止 |

本次没有新增 E-R1（当前版本真实 UI 复现）。C1–C4、C7、C13、C16 的“源码入口成立”与“用户损失一定发生”是两件事：前者可确定，后者需要满足各条所述条件。

| 首轮项 | 二轮裁定 | 应落实的剩余工作 |
|---|---|---|
| C1 | 保留，限定为序列化通知前的判断窗口 | 事务版本先标脏；flush 仍保留 |
| C2 | 保留命令保存分叉，收窄 AI 归因 | AI 主路径已有全文持久化验证；消除兼容分支差异 |
| C3 | 保留 | 两标签淘汰缺少 dirty 豁免；不能据此称每次切第三篇都会丢稿 |
| C4 | 保留事件粒度风险，补充窗口保护 | 强制更新区分草稿；独立 WebView 不假定共享 DOM 事件 |
| C5 | 保留查找监听问题 | 复用已有所有权与返回设施；补齐隐藏实例接线 |
| C6 | 保留 | occurrence 贯穿目标；修正遍历终止语义 |
| C7 | 保留调用方边界，确认全文 API 已存在 | 把工具条、查找、大纲接到相同全文来源 |
| C8 | 保留，限定内嵌窄屏属性路径 | 大纲选择后由宿主返回正文并定位 |
| C9 | 保留过渡态与错误态问题 | 认可 watch/cancelled/liveContent 重置，补内容身份绑定 |
| C10 | 保留，补充查找替换输入框 | 复用标题/自定义属性的 IME 守卫 |
| C11 | 保留并细化 | note 标签实际还会降为 dirty/saved；补多字段聚合 |
| C12 | 保留 | 阅读工具集独立，不依赖全栏 disabled |

第二轮还确认：全文搜索已有请求 sequence，旧请求会被丢弃；查找与标题跳转桥接已有冷挂载暂存；未解析双链已有创建确认与同标题请求去重；图片持久化失败不会在有 noteId 时偷偷返回临时 blob；图谱已有加载错误、降级来源和截断状态。后续规格均以保留这些基础为前提。

### 已有正确工作应保留

2026-09-12 的审计与后续验收记录提到：窄窗工具栏、属性遮挡、重复移动 chrome、非活动浮层、响应式切换 flush、链接与图片选区等已有修复。当前源码也能看到相应保护。

因此本报告没有把那些旧问题全部重新宣布为现存 bug。需要继续推进的是：

- 从“避免重叠”走到“默认信息层级正确”；
- 从“按钮有接线”走到“动作范围和完成路径符合预期”；
- 从“卸载时尽量保存”走到“草稿有独立生命周期”；
- 从“按 noteId 过滤”走到“文档、草稿与视图各自有身份”；
- 从“可以缩到手机宽度”走到“手机阅读与输入有独立设计”。

历史窄窗记录只能说明当时的桌面响应式行为，不等于手机原生交互事实。本轮也不借历史截图推断当前像素表现。

---

## 十四、第二轮施工规格：把建议落实为完整的人机交互

本章的 R 编号是改造规格；B 编号是下一章的实施批次。规格中的目标状态、接口字段和动作顺序是建议，不能反过来当作现有实现的描述。

### R01．写下的内容应立即有归属，保存入口只表达同一件事

**路径与成本：**用户输入一句话，接着改标题、按保存、切篇。如果标题、正文与标签分别“看起来成功”，用户仍不知道整篇是否安全。C1、C2、C11、C13、C15 都落在这个问题上。

**当前基础：**正文已有 draft、队列、在途请求、OCC、重试和 flush；dirty/save registry 已支持 Set 聚合；元数据后端已有可选 CAS；正文与 note_links 在同一保存点更新。[S1][S2][S17][S22][S32]

**目标契约：**

| 对象 | 至少保存的信息 | 不该承担的职责 |
|---|---|---|
| 文档快照 | noteId、全文、元数据、存储版本 | 当前光标、哪个菜单打开 |
| 本地草稿 | draftId、基准存储版本、本地 revision、未提交字段 | 用某个 React 组件的存活代表草稿存在 |
| 保存请求 | noteId、draftId、要确认的 revision、正文/元数据 patch、原因 | 在 await 后重新读取“此刻活动篇”决定写谁 |
| 保存结果 | 已确认 revision、新存储版本、失败/冲突信息 | 仅用“Promise 成功”把所有后续输入标为 saved |

**具体规则：**

1. ProseMirror `docChanged` 先递增本地 revision；选区移动不标脏。Markdown 序列化仍防抖，不能为了 dirty 正确每键扫描全文。撤销恰好回到已保存内容时，可在后续快照比较中清除 dirty。
2. 自动保存、快捷键、命令面板、关闭、AI 接受和导出调用同一 `flushThrough(targetRevision)` 语义；这是建议接口名。普通保存确认点击时的版本，不因用户一直打字而永久等不到“系统完全空闲”。
3. 同一草稿只有一个持久化协调入口。成功确认 revision 10 时，若用户已写到 12，状态仍是未保存/保存中，不能退成 saved。
4. 正文与元数据先复用现有接口串行协调，不要求第一步改成一个巨大的后端接口。不同字段更新导致 OCC 冲突时读取一致快照；仅 metadata 变化可安全重试正文，正文真实变化进入冲突。
5. 属性先补版本参数，再做字段级 patch 重放。两边修改不同键可以合并；同一键从不同基线修改不能直接 last-write-wins。
6. 标题编辑保留 Enter 提交、Esc 取消、blur 提交；`flush` 必须显式处理尚未 blur 的有效标题。校验失败保留输入和错误，不让正文保存成功掩盖标题失败。
7. 状态对外提供 dirty、saving、saved、failed、conflict 及失败字段。只读权限、离线和维护模式另外表达；本地笔记不能因网络离线被整体标成保存失败。

**失败处理：**自动重试保留退避，只重试可重试错误；冲突、权限、超限不能无限重试。失败横幅持续可达，提供重试、复制、另存副本。错误状态是文档事实，不能因为切到阅读模式消失。

**实施：**B01 先收入口和版本，B02 接生命周期，B07 接标题/标签/属性。文件重点为 `NotesCrepeEditor`、`NoteContentView`、`contentDirtyRegistry`、DSTU API 和元数据调用方；保留后端 CAS 与保存点。

**验收：**在序列化回调前关闭仍能识别修改；v10 保存期间写 v11，v10 完成后仍 dirty；正文成功但标题失败不能显示整篇已保存；两个视图各加一个属性键均保留；同键冲突不静默覆盖。

### R02．切篇、关闭、恢复与同篇多视图共用一套生命周期

**当前事实：**Workbench 每 pane 只保留当前与最近一个完整编辑器；Learning Hub 有 dirty 额外保活。Workbench 已保存 tabs、活动 tab、分屏、焦点 pane、侧栏和折叠目录，但没有统一保存阅读锚点、选区、查找上下文。[S3][S11][S24]

**先确定三个不同动作：**

- **关闭视图**：结束这个 pane/tab 的展示；同篇其他视图可继续存在。
- **放弃草稿**：明确撤销这份未提交版本；不能顺手抹掉另一个独立草稿。
- **删除笔记**：修改资源的存在状态，经过独立删除与恢复流程。

**实施顺序：**

1. B02 的短期措施给 dirty、保存中、冲突中、尚未提交元数据的标签淘汰豁免；不增加所有干净编辑器常驻数量。
2. 把保存队列和可恢复草稿移到 view 外，再恢复有界 LRU。离开编辑器先交接 live 内容与版本；卸载 cleanup 不承担“用户数据最后的主人”。
3. 所有关钮、中键、快捷键、关闭其他、关内部窗口接三态 gate。保存并关闭须确认目标 revision；期间出现新输入则重新检查。批量关闭用一份清单处理失败和取消，避免逐篇连续弹窗。
4. 关闭 clean 视图不弹确认。关闭 dirty 视图且该草稿已由其他可见视图可靠承接时，可仅关闭视图；否则保留保存/放弃/取消选择。
5. 恢复会话分两层：文档恢复最后可靠草稿；视图恢复阅读锚点、模式与查找。没有可靠内容恢复时，不能仅还原旧 tab 就声称“恢复了工作区”。
6. 崩溃恢复另需本地 journal。先保证正常切换/关闭；再设计原子写入、容量与清理。进程被系统杀死时只能承诺恢复到最后 durable checkpoint，不能把异步 flush 宣称为每个按键零损失。

**同篇多视图的推荐路线：**同一运行时优先共享文档事实，view 各自保留滚动、选区、模式。过渡阶段已有多个独立草稿时，把它们明确标识并用版本冲突处理，不能靠一次 force 广播合并。跨独立 Tauri 窗口继续以存储版本/OCC 为底线；不预设已有跨窗口共享内存。

**尚需明确的产品选择：**是否允许同篇双视图同时写入。建议先保证独立阅读、单一活动编辑目标，再评估双向 transaction 映射；普通撤销应撤销本人的事务，不撤销别的 pane 刚写的内容。不能用“重建另一边全文”冒充完整的多视图编辑支持。

**验收：**A 写入失败后依次打开 B/C/D，返回 A 可恢复；关闭右 pane 不销毁左 pane 草稿；取消批量关闭保留未获准项；重开后回到原章节；遇到外部删除可另存本地草稿。

### R03．命令在执行前就确定对象，执行后回到合适位置

**人的成本：**用户按同一个快捷键，有时作用于正文，有时被隐藏查找框吞掉；点一个菜单后不知道光标在哪。这不是按钮样式能解决的问题。

**基础与接线：**保留 command palette、workspaceRegistry、移动 toolbar owner 和 Android back coordinator；把 `FindReplacePanel`、页面菜单、选区工具条和块工具接到相同的 active-view/overlay 规则。[S2][S12][S25][S28][S29]

建议每条命令声明：

```text
id、label、scope(document / selection / block / library)
目标 noteId、draftId、viewId、windowId、paneId
可执行条件与不可执行原因
是否写入、是否需要全文、undo 行为
需要保留的 selection/version、完成后的焦点位置
```

| 命令类型 | 确定目标的时点 | 完成后的落点 |
|---|---|---|
| 全篇保存、导出、制卡 | 用户触发时固定文档与版本 | 不因后台完成抢回正文焦点 |
| 文本格式、引用所选 | 打开菜单前捕获选区与文档 revision | 仍是原 view 时恢复映射后的选区 |
| 移动/转换当前块 | 固定块身份或可映射位置 | 移动后的块，取消回原块 |
| 搜索打开、大纲跳转 | 固定目标 view 与 requestId | 文本可见；阅读状态不自动弹软键盘 |
| 全库标签改名 | 固定范围与批次 | 结果清单，不能假装是一个当前篇动作 |

**键盘与 overlay 规则：**

1. 浏览器或 native 层先处理其拥有的输入。Web 内依次是顶层 modal/popover、字段编辑、文内面板、view、导航。只消费自己真正处理的事件。
2. F3 与 Mod-F 只发给 active view；隐藏/保活实例不注册全局输入所有权。
3. 输入查询字段中的 Mod-Z 默认撤销字段输入。正文替换使用明确的“撤销替换”动作，或回正文后进入正文 undo；不要悄悄把字段撤销改成文档撤销。
4. 菜单打开时 Enter、方向键归菜单；IME 候选确认不执行业务动作。Esc 关闭菜单后，不能继续退出专注。
5. 窗口失活或 view 卸载时释放 body portal、拖拽和 focus owner；不能只把根元素 hidden。
6. 命令执行失败给出原因；例如“先选中一个列表项才能缩进”，比点击后无反应更可理解。纯位置不适用可以禁用，文档错误应可重试。

**实施：**B03 先覆盖保存、查找、Esc/back、链接、图片六类高频命令；B09/B11 再覆盖块与 AI。无需一次重写整个应用命令系统。

**验收：**两个 pane 各开查找，F3 仅作用于活动者；隐藏 pane 不抢焦点；菜单嵌套时一次 Esc 只结束一层；原生选图返回期间切篇，不插入当前新篇。

### R04．桌面正文优先：先删默认噪声，再调整容器结构

**当前事实：**格式工具、页面动作、宿主 chrome 和上下文各有自己的结构；阅读态仍渲染格式条。宽度变化与窗口/pane/全局导航的叠加，是产生拥挤的重要条件。[S2][S3][S4][S5]

**第一步可直接交付的设计减法（B04）：**

- 阅读态只保留阅读/编辑切换、查找、上下文与页面菜单；没有必要显示一排灰色编辑按钮。
- 默认编辑态常驻“插入”“文字样式”“列表”“更多”；选中文本后展示选区格式，不再重复长期摆放全部 H1–H6、公式、图表和制卡。
- 页面菜单明确分组：文档信息/位置、全文操作、导出、删除；制卡属于全文操作。
- 大纲、关联、属性各自可直达；移除位于章节导航之前的重复统计与确定性摘要。统计放在轻量底部或折叠信息区。
- 无标签、无属性是正常状态。需要分类时可见“添加属性”，不要让空卡片成为正文上方固定装饰。

**容器布局规则（B04→B05）：**

1. 用 pane 的可用尺寸决定辅助区形态，用平台/指针能力决定输入交互，二者不得互代。
2. 单 pane 建立可配置正文行宽；中文可先评估约 32–40 个全角字的行宽作为设计起点。此数值是排版建议，不是已测最佳值。代码/表格允许块内横向滚动。
3. 空间不足时先收低频上下文，再将文库导航改为抽屉；仍不足才以辅助页替换正文。禁止同时挤出左右两列极窄正文。
4. 手动开启的侧栏偏好与因宽度暂时折叠分开保存；窗口放回宽屏后恢复偏好，而不是永久改掉用户布局。
5. compact 双屏当前按上下各半展示。手机默认呈现活动篇与切换入口；窄桌面可保留用户显式选择的上下对照。宽度变化不能销毁另一篇或改动其保存状态。
6. 长标题自然换行；标签条使用省略与路径 tooltip；保存状态占位固定，不让字宽变化推动标题。

**专注模式：**进入时记录本 view 的布局快照；退出仅恢复自己的变更，不覆盖用户期间在其他窗口做的布局调整。仍有搜索、大纲、保存异常和稳定退出入口。阅读/编辑切换不重建文档历史。

**验收：**无元数据的空篇直接写；阅读界面无格式禁用墙；关闭文件树仍能前后导航；双屏右侧操作不会改变左侧专注偏好；窄窗往返不丢失原分屏资源。

### R05．打开笔记是一段导航，不只是把 ID 塞进 tab

**当前事实：**Workbench 已有内存 back/forward 历史，最多 100 条，能避免导航再入；布局可持久化。搜索有 sequence 防晚到结果，但全文查询和标题目标仍按 noteId 暂存。[S3][S16][S24][S26][S27]

**统一打开协议建议：**

```text
openNote(noteId, {
  destination: current / newTab / otherPane / window,
  intent: read / edit / reveal,
  target: searchMatch / heading / block,
  origin: sourceView + returnContext,
  requestId
})
```

目标不是让所有宿主长得一样，而是让每个宿主正确接收打开意图。

**具体路径：**

1. 新建：先按明确的当前目录规则建立空篇，立即允许正文输入；默认标题可稍后改。手机“新建”是主动编辑意图，可准备键盘；搜索/双链“打开”默认保留阅读意图，不强 focus。
2. Quick Switcher：空查询显示最近；有查询优先名称与路径；同名条目同时显示文件夹和简短上下文。Enter 打开选中项，候选输入期间不打开。无结果创建保留输入标题，但必须明确创建位置。
3. 全文搜索：保留查询、过滤器、列表滚动、选中命中和分页；打开后带命中目标。返回搜索恢复原位置，而不是重置成空的 quick-open。
4. 点击链接默认当前 pane；修饰键/菜单明确提供新 tab、另一 pane、窗口。若改键位，应统一配置并提示冲突，不直接抢占当前 Mod-E 行内代码命令。
5. 文档内大纲/搜索跳转应能回到此前阅读位置，但不要把每次滚动记入历史。tab 列表与“本 pane 走过哪些位置”的历史分开。
6. 重复打开同篇的既有 tab 时恢复其视图；显式“另一 pane 查看”应创建独立 view，而不是让同一个 tab key 同时承担两边状态。
7. 关闭 tab 的焦点优先回到同 pane 最近使用的存活 tab；中键和批量关闭采用相同规则。新 tab 溢出时有可搜索列表和 dirty 标识。

**异步跳转协议：**先发布 requestId/目标 view，再等文档加载与 view ready；根据锚点加载所需区域，最后定位并确认消费。后来的同 view 请求使先前请求失效；其他 view 的请求不能被同 noteId 消费掉。一次 requestAnimationFrame 不能作为文档就绪保证。

**实施：**B05 在现有 nav history 和 bridge 上扩展，不更换路由框架；依赖 B02 的视图恢复与 B03 的目标所有权，B06 提供全文锚点。

**验收：**从搜索进入 A、由双链进入 B、返回两次时原命中与结果列表均可恢复；连续两次打开同篇不同命中，最终到达最后目标；不同 pane 同篇互不消费对方跳转。

### R06．全文能力、标题锚点和加载窗口必须有同一坐标系

**当前事实：**Markdown window 保存会拼回未加载后缀；全文替换 API 已存在。FindReplacePanel 操作 PM 当前 doc；大纲点击没有传 occurrence；标题定位遍历可能覆盖前面同名匹配。[S1][S5][S10][S12][S13][S42][S43]

**分三步改，而不是直接去掉长文窗口化：**

1. **先公开能力与范围。** editor adapter 区分 `getVisibleMarkdown` 与 `getDocumentSnapshot`；为工具条、AI、制卡、统计统一注入全文 provider。还不能覆盖全文的查找/替换必须写明“已加载部分”，不能返回 0 就让用户认为全篇没有。
2. **建立全文索引和映射。** 全文解析产生标题、文本匹配及 Markdown offset；渲染层维护片段与 PM 位置映射。加载目标后再换算成当前 view 的位置。增量加载应复用已有选择与滚动保护。
3. **扩展全文替换。** 对全文 snapshot 计算修改，校验 revision；尽量以结构化事务应用，避免每次 setMarkdown 重建全文并破坏 undo。阶段过渡可先要求加载全文后替换，超过预算明确提示，不静默裁剪。

**标题锚点：**最小修复把 `(level, normalizedText, occurrence)` 贯穿解析、点击、桥接和滚动。长期使用块/标题稳定 ID 或内容锚点；Markdown 纯文本未存稳定 ID 时可保存前后文摘要与相对序号，失效时回退最近可识别章节并说明，不能声称永远精确。

**语义细则：**

- wikilink `#标题` 没有 occurrence 时，重复目标需候选选择或稳定且公开的默认策略；不要依赖“遍历最后一个碰巧覆盖”。
- 查找区分“可见文本”和“Markdown 源码”两种语义；搜索格式标记不是普通正文搜索的隐含承诺。
- 保留现有 regex 错误提示、捕获组替换、非重叠匹配、inline atom 边界保护。全部替换作为一个可撤销动作，并显示影响数。
- 目录、全文统计和摘要绑定同一 revision；统计暂时落后应显示更新中，不能拼出当前标题配旧正文的可操作目录。

**实施：**B06 最小锚点修复可早交付；全文 provider 先于 B11 制卡/AI 扩展。不要先做全量 DOM 渲染，再靠调大阈值遮蔽性能问题。

**验收：**第 601 行之后的章节、同名“总结”、代码块里的伪标题、带格式的标题、Unicode/中文标点、加载更多期间继续输入均有明确结果；全文替换不丢后缀，撤销恢复同一完整版本。

### R07．属性与标签属于本篇管理，全库操作要另立边界

**当前事实：**属性已有合法键、保留键、长度、数量、值类型约束；watch 会刷新元数据。标签局部改名路径会继续全库传播；props 写回为整个对象；正文读取失败没有独立状态。[S5][S15][S17][S31]

**本篇属性页：**

1. 默认只显示标题之外真正有值的字段、标签和“添加属性”；大纲独立，不在属性卡片底部寻找。
2. 字段保留 `baseValue/inputValue/submitting/error`；外部 watch 更新不能覆盖正在输入的字段。未编辑字段可以同步，正在编辑字段出现冲突提示。
3. Enter 提交当前字段、Esc 取消当前字段；候选输入 Enter 只确认候选。提交失败保留输入，焦点停留在可修正位置。
4. 字符串/数字/布尔保持类型；未显式切类型时，显示为文本不能顺手把布尔转成字符串。对已有不支持的历史值给出可读呈现和受控处理，不能自动删掉。
5. 删除属性可撤销，但 undo 也要校验字段最新版本；不能用旧整个 props 对象覆盖期间新增的字段。
6. 内容加载状态使用 `{noteId, revision, status, content}`。切篇立即移除上篇的可操作内容；失败显示重试，不把空串当成功加载。

**标签动作：**

| 用户入口 | 默认影响范围 | 完成反馈 |
|---|---|---|
| 删除当前标签 chip | 当前篇 | 移除并可撤销 |
| 替换当前标签 | 当前篇 | 保留其他标签，失败保留输入 |
| 点击标签名筛选 | 文库查询 | 打开含明确标签过滤的结果 |
| 标签管理的“全库重命名” | 明确筛选出的文库笔记 | 预览数量、批次进度、成功/跳过/失败清单 |

全库操作必须去重合并标签，记录原值和修改结果；对 dirty 文档转交会话协调器或列为待处理，不直接修改磁盘与草稿竞争。跨篇任务不宣称天然原子，恢复逐篇校验版本。

**实施：**B07，依赖 B01 版本写入与 B03 命令范围。标签局部默认范围可先修，字段级 merge 和批次恢复后补。

**验收：**用户只替换 A 的标签时 B 不变；全库操作中途失败能看到具体未完成项；A 的属性请求晚到不会污染 B；两个 pane 编辑不同字段可合并，同字段冲突可解释。

### R08．手机四状态的转换要完整，窄桌面不进入手机假设

**已有能力：**visualViewport 键盘偏移、safe-area、触摸热区、preventDefault 保焦点、横向滚动、body portal、能力门控、壳可见性与单 toolbar owner；Android 有返回协调器。[S2][S9][S28][S29]

**建议状态表：**

| 状态 | 顶部 | 底部 | 正文点击/退出 |
|---|---|---|---|
| M1 阅读 | 返回、紧凑标题、更多 | 少量导航与编辑入口 | 点文字不弹键盘；链接正常打开 |
| M2 输入 | 完成/返回、标题、更多 | 一条键盘工具条 | 保持光标可见；收键盘不等于关文档 |
| M3 选区 | 保持文档上下文 | 原生菜单优先，扩展命令按需 | 格式/引用绑定这段；取消只收选区工具 |
| M4 辅助页 | 返回正文、页名 | 无正文编辑条 | 选章节后返回；属性/图谱返回恢复原位置 |

**动作分配：**

- 一级固定 `+`、撤销/重做、文字样式、列表/任务、收键盘；先验证频率，再开放自定义。
- `+` 打开有名称的插入面板：链接、图片、表格、代码、公式、callout、toggle；不能在正文先插入 `/`、取消后又留下无意的字符。现有 openSlashMenu 是字面插入 `/`，应作为需要替换的命令适配行为。[S29]
- 列表输入时缩进/反缩进就近出现；普通段落不长期占两格无意义按钮。
- 整篇制卡、导出、属性在页面菜单；选区动作明确写“所选内容”，不与全文入口混淆。

**键盘和空间规则：**

1. 键盘占位、safe-area 和工具条高度分开计算；避免把同一 inset 加两遍。浮动/分离键盘不一定表现为 viewport 高度减少，必须允许保守回退。
2. visualViewport 改变后只滚动必要距离让 caret 可见；不要每次 resize 把整篇 scrollTop 重设。
3. 触控滚动开始后不触发被划过的工具按钮；收起动画不在同一次 pointer 手势中改变点击对象。
4. 属性页打开前保存 view 上下文并释放正文浮条；返回时恢复上下文。除非用户明确继续编辑，否则不强制调用 focus 弹键盘。
5. 原生选词菜单、Crepe 浮条和引用到聊天不能同时占同一选区上方。优先使用一个扩展入口，不以隐藏系统菜单换取自家功能可见。

**返回层级：**native 键盘已消费的 back 不再执行 web 导航；Web 层顶层 overlay 优先，再到辅助页、文档导航，最后应用级返回。现有 Android coordinator 是接入点，不能让每个笔记组件另注册 native 返回。iOS 没有 Android 硬件 back，不假定两端事件相同。

**实施：**B08，依赖 B03 所有权、B04 模式、B05 导航、B06 章节跳转。源码阶段先完成状态表和 handler 接线；系统选择手柄、键盘动画、输入法与 back 消费顺序留作实施后的真实设备验收。

**验收：**编辑→选区→插入→取消→辅助页→返回全过程只有一个工具 owner；窄桌面配鼠标不显示依赖软键盘的底栏；Android 一次 back 不同时关菜单并退文档。

### R09．文本与块操作要有共同的选区、撤销和失败语义

本节是编辑交互规格，不声称表内每项目前均有 bug。当前编辑器能力已较丰富，应先用适配层校准行为，再考虑底座替换。

| 对象 | 应实现的细节 | 取消/撤销与边界 |
|---|---|---|
| 普通文本格式 | 有选区作用于选区；无选区明确影响后续输入；混合格式显示 mixed | 保留选择方向与光标；不把整段重建成纯文本 |
| H1–H6 | 当前块转换；选多块逐块按合法结构处理 | 从标题转段落不丢内容；列表内标题行为明确 |
| 列表/任务 | Enter 同级新增，空项退出，Tab/Shift-Tab 缩进与反缩进 | 不让缩进默默跳网页焦点；退格能退结构层级 |
| 任务勾选 | 编辑模式单事务更新；阅读模式先做产品选择 | 推荐阅读下只允许显式勾选这一特定写动作，并继续显示保存状态；若坚持严格只读则禁用，不能伪装无副作用 |
| 块选择/移动 | 鼠标句柄、键盘命令和手机菜单等价；拖动显示插入线 | Esc 取消不移动；移动后选中原块；整次移动一次 undo |
| 表格 | Tab 在单元格间走，末格增行规则固定；工具区区分删行/列/整表 | 块内横向滚动；多单元格粘贴保结构；不能把删表当清空内容 |
| 代码块 | 语言、复制、编辑入口明确；代码内容按文本处理 | 保留缩进/空行/反引号；Tab 在代码与导航之间有明确退出方式 |
| 公式 | 源码和预览并排或可切换；解析失败保留源串 | Esc 回原公式；提交整次可撤销；IME 不误提交 |
| Callout | 类型与标题是块属性；正文仍可正常编辑 | 转换保留嵌套内容；不因未知类型删正文 |
| Toggle | 折叠是视图状态；内容结构持久化 | 折叠不从搜索/导出消失；移入移出保持顺序 |
| 链接输入 | 选区打开前固定；显示文字和目标分别编辑 | 取消无正文改变；异步返回时验证 revision/实例 |

**关键工程规则：**原始 `from/to` 只是某一 revision 的位置；打开菜单后还有别的 transaction，必须映射位置或拒绝过期写入，不能盲目 restoreSelection。异步图片流程已有实例守卫，应进一步接入此契约。[S29][S42]

**实现顺序：**B09 先做 selection/transaction/undo 适配，再按列表→链接→图片→表格→特殊块逐项接入。正常格式操作直接调用 PM 命令，不用“读取 Markdown→拼字符串→重建全文”。

**验收语料：**中文与英文混排、多段选区、跨 mark 文本、嵌套任务、合并粘贴、带反引号的代码、未完成公式、折叠长块。每个操作按“输入→操作→撤销→重做→保存→重开”验证语义；渲染看起来相同不足以证明内容未变。

### R10．双链、背链和图谱围绕“读到出处”组织

**当前事实：**wikilink 已有未解析确认、同名候选、hover 预览、按标题 in-flight 去重；背链有后端/客户端来源和有界扫描；图谱有加载/错误、1–2 度选择与客户端降级提示。标题改名同步有 C14 的入口与完整性缺口。[S33][S34][S35][S36][S37]

**具体改进：**

1. 普通链接与 wikilink 共用打开协议。阅读时直接打开，编辑时保证可进入链接文本编辑；新 tab/另一 pane 通过修饰键或菜单，不强制每次弹确认。
2. 同名候选显示文件夹、最近修改和摘要，不只显示重复标题。内部稳定资源 ID 与可读文案分开；旧标题链接继续兼容，不要求用户手工理解 ID。
3. 创建未解析链接沿用现有确认与防重复；显式说明创建目录。当前去重键只是 trimmed title、创建上下文是 activeCreateContext；多窗口同时在不同目录创建同名时应把文库/目录/意图纳入键，而不能无条件合并。
4. renameNote 服务统一接正文标题、文件树和其他入口；保存标题后生成可追踪的引用更新任务。保留 alias、heading、ID 链接；dirty 来源进入待处理，不被覆盖。
5. 背链结果首先给来源标题和命中上下文；点击到准确位置。区分已解析反向链接、提及和未解析链接，不把它们统称“引用”。
6. 图谱默认作为相关内容探索入口，不自动占用右栏。保留布局、缩放和深度偏好；选中节点先标记，再按明确动作打开，避免拖拽结束误导航。
7. 索引未覆盖全库、扫描截断、后端不可用降级为本篇出链时，持续显示范围。空结果必须区分“没有引用”和“未完成查询”。

**工程路线：**B10 先统一改名和导航目标，补 scanFailed/truncated/pending；随后分页恢复和 dirty 来源协调。索引更新继续复用 note_repo 的原子出链更新。标题更新、历史标题兼容和来源重写是不同任务，不要求一次跨全库大事务。

**验收：**正文改名和树改名得到一致结果；超过 256 个来源可继续处理；扫描两路都失败有错误入口；两个目录同名创建不误复用；背链点击返回后保持原展开与滚动；图谱降级不显示完整关系的假象。

### R11．图片、附件和学习引用应有稳定身份与可修复状态

**已有基础：**有 noteId 的图片先保存到 notes_assets，再返回稳定相对引用；保存失败返回空并提示。只有缺少笔记上下文的临时使用才降级 blob 并警告。移动原生选图已捕获选区，并在返回后检查编辑器实例与只读状态。[S29][S41]

**目标过程：**选择/粘贴→校验→保存资源→插入确认→正文保存。每阶段可区分失败，不用一条“插入失败”掩盖资源已写入但正文未写入的情况。

1. 取消选择不插空图；上传中有就地占位与进度/取消入口，失败能重试或删除占位。
2. 资源保存成功后，用户切篇/取消插入，资源转为待清理；不能立刻删除可能已被其他草稿引用的资源。GC 应考虑已保存文档、可恢复草稿、导入 staging 与历史备份策略。
3. 选图返回时固定原 noteId/viewId/revision。当前实例相同仍不代表选区坐标有效，需 transaction 映射或让用户重新选择插入位置。
4. 缺失文件显示原名称、缺失原因、重新关联和删除引用；网络图加载失败与本地附件缺失不同，不把两者都画成破图。
5. 正文持久化只存稳定引用；渲染用的 data/blob/asset URL 不反写正文。大图读取采用缩略图/按需加载，避免把 50MB 上传上限当作每张图都适合全文加载的目标。
6. PDF/卡片/资源选区引用携带稳定 resourceId、来源定位、引用文本快照和版本。原资源删除/改名后仍能显示当时引用内容，并清楚标记“来源不可用/已变化”。
7. 导出用可移植相对路径及引用清单；导入分配新 ID 时重映射链接，不能只复制 Markdown 字符串。

**实施：**B09 收插入事务，B10 接资源/链接身份，B12 验证导入导出。附件实际清理策略和底层来源类型需沿对应 VFS 资源实现继续确认，本轮不宣称所有引用类型均已完成往返验证。

**验收：**多图中一张失败仍可知道哪张未完成；选图返回期间切篇不误写；资源写入成功但正文保存失败可恢复；重命名与导出导入后引用仍指向正确内容；不能把“取消不产生空资源”误解为删除任何已上传但尚未插入的资源都安全。

### R12．AI 的范围、版本、接受与撤销要和手工编辑一致

**当前事实：**AI handler 已在接受时重算过期建议，校验 expectedMarkdown，使用 generation 防切篇晚到结果；有最多五个 checkpoint，内容偏离时标 stale；全文替换主路径有持久化验证。制卡 helper 也优先使用全文 API，移动端有在途防重复。[S1][S14][S29][S30]

**因此改进重点不是重做一遍“接受前校验”，而是统一产品与数据边界：**

1. 页面菜单明确“基于全文”，选区菜单明确“基于所选”；执行前显示来源标题、范围和是否包含尚未保存的内容。
2. 建议生命周期使用 proposed→applying→persisting→applied，另有 stale/failed/cancelled；只有持久化达到目标版本才能说已应用并保存。
3. 等待期间用户继续写时保留现有重算；重算结果与原预览明显不同，应重新展示，不直接把新差异套上旧“接受”意图。
4. 选区级修改记录选区锚点与基准版本；锚点失效明确要求重新选择。全文操作使用 R06 的 provider，不依赖 toolbar 恰好拿到哪种 editorApi。
5. 普通格式和手动替换走正文 undo；AI 接受尽量形成一个明确事务。checkpoint 是跨普通编辑后的受控恢复入口，不是第二套无条件覆盖整个文档的撤销。
6. checkpoint stale 时可以看差异、复制旧内容或另存；不要允许一键覆盖期间所有人工修改。标题/属性变化与正文变化的失效规则分别定义。
7. 制卡任务固定文档版本与范围，显示任务已启动及结果入口；成功不强制导航走当前笔记，失败不丢刚才选区。防重复以任务/文档标识为准，不只依赖按钮短暂 disabled。
8. 离线时普通编辑和本地保存照常工作，网络 AI 单独说明不可用。

**实施：**B11，依赖 B01/B03/B06。先补全文工具条接线、范围文案与完成状态，再调整 checkpoint/undo 关系。用户本机有未提交改动，实施前重新核对相关 diff，不能以固定 commit 覆盖。

**验收：**长篇制卡包含未加载部分；AI 等待时继续编辑不被覆盖；应用失败仍可恢复两版；接受期间切到 B，结果不写 B、不抢焦点；stale checkpoint 不覆盖后续人工编辑。

### R13．导入、导出、删除与恢复是一套可说明结果的文库任务

**当前事实：**ZIP 文库导出/导入已有独立面板、冲突策略、进度事件、后端 staging；批量 Markdown 导入已有逐项成功/失败返回。导出没有前置草稿屏障，ZIP 进度对象没有任务 ID，`includeVersions` 已废弃。[S38][S39][S40]

**导出：**

1. 在入口区分“当前篇 Markdown”“当前篇及附件”“全文库备份”；本次首要修正是明确现有文库 ZIP 的范围。
2. 确定快照范围后等待目标版本保存。若某篇失败，提供重试或明确“仅导出已保存版本”，不能一面显示成功一面遗漏用户刚写的正文。
3. 归档应包含可解释的清单：笔记、附件、元数据、缺失资源、版本格式。历史版本已无数据时不能显示“包含全部历史”。
4. 大文库导出需一致快照/版本清单，避免遍历时前后篇来自不同时间却被称为同一时刻备份。具体数据库读事务与文件快照成本需在实施时核对 exporter，当前不能声称已有全库一致快照。

**导入：**

1. 先预检格式、目标目录、同 ID/同名冲突、附件范围和覆盖数量，再执行；skip/overwrite/keep-newer 的比较对象与时间规则可见。
2. 覆盖打开的笔记先处理 dirty 草稿；后台任务不能以 force 事件代替用户对草稿的选择。
3. 任务具备 operationId 和逐项状态。不同窗口任务的进度不混流；订阅建立后再开始，避免首段进度丢失。
4. 提供明确“后台继续”或“安全取消”。没有后端取消能力时不放一个暗示已停止的取消按钮。取消只在可靠边界生效，并展示已完成项。
5. 失败结果保留可重试清单；重试基于同一任务/资源映射幂等处理，不重复创建此前成功的笔记与附件。
6. ZIP 和普通 Markdown 是两条入口，不以其中一条的测试推定另一条正确。

**删除与回收站：**删除对象显示标题与范围；打开的 clean view 转为已删除提示，dirty view保留“另存副本”。恢复时校验 ID/目录冲突，原来的 view 可重新绑定，不强行把用户带走。批量删除不顺手删除仍被其他资源或草稿引用的附件。

**实施：**B12，依赖 B01/B02/B10。先修导出保存屏障和范围反馈，再做统一任务/清单；后端事务与取消能力另行按实际 importer/exporter 拆小包。

**验收：**最后一字输入后立即导出能说明是否包含；中途失败可继续未完成项；覆盖 dirty 文档不静默丢稿；归档重导入后正文、属性、附件和链接语义一致；时区差异不把旧内容误判成较新。

### R14．手感、辅助技术和性能需要可操作的质量约束

本节提出设计预算与未来验收，不提供未经测量的性能结论。

**交互质量：**

- 文本输入不被统计、背链、摘要和图谱重算阻塞；这些数据按 revision 异步更新，丢弃过期结果。
- 模式切换、展开上下文、加载更多不无故改变阅读锚点；明确导航可以移动，后台更新不抢滚动。
- 键盘焦点始终可见，图标有可读名称；混合格式、选中、保存失败不能只靠颜色区分。
- 辅助技术读取完整保存状态，正常自动保存不反复播报；错误用持续状态区域，避免重复 toast 播报淹没正文。
- 桌面键盘可完成新建、切篇、开查找、换 pane、开菜单、返回、关页；触屏低频动作有具名菜单，不依赖 hover 或拖拽。
- 尊重系统字体缩放、reduced-motion、高对比需求。44px 可作为触控热区基准，视觉图标可以更小，但不要用重叠热区制造误触。

**性能观察点：**

| 过程 | 应记录的边界 | 不应采用的“优化” |
|---|---|---|
| 输入 | transaction→dirty；序列化耗时；排队/持久化耗时 | 延迟标脏换取输入分数 |
| 热切篇 | 命令→标题确认→正文可用→锚点恢复 | 丢掉失败草稿换取卸载 |
| 长文搜索 | 索引版本、范围、查询耗时、目标加载耗时 | 只查前缀却标全文完成 |
| 辅助页 | 数据来源、解析版本、错误、正文是否保持位置 | 总是重建 editor |
| 图片 | 解码/代理读取、缓存占用、对象 URL 释放 | 把临时展示 URL 持久化 |

普通热切篇反馈约 100ms 可作为初始设计预算；冷加载、设备差异与大文档另立预算。设置日志只记录 ID、版本、阶段、耗时和错误类型，不写正文、选区内容或凭据。禁用对无序 HashMap/对象直接序列化后作哈希或字符串判等；文档变化优先比较版本，备份校验需要确定性 canonical 数据。

**实施：**贯穿 B01–B12。先复用现有日志/测试位置，把可观测性放在边界；不为调研新增常驻调试 UI。

---

## 十五、分阶段实施路线与依赖

### 15.1 施工原则

第十章保留 P0/P1/P2 的产品优先级；这里给出落地顺序。**优先级不是要求一次实现整套框架。** B01–B03 先修真实断点；B04 的视觉与信息减法可同步准备；全文、关系与文库任务在对应边界稳定后接入。

| 批次 | 可独立评审的交付 | 主要落点 | 依赖 | 通过门槛 |
|---|---|---|---|---|
| B01 / P0 | 事务即时 dirty、统一 flush、带 revision 保存结果；命令保存接队列 | NotesCrepeEditor、NoteContentView、contentDirtyRegistry | 无；先对比用户本机在途保存修复 | 最后输入、重入保存、旧 ack、新 revision 全部正确 |
| B02 / P0 | dirty 淘汰豁免、三态关闭接线、view 外保存归属；再加恢复存储 | WorkspacePane、TabPanelContainer、close gate、document session | B01；豁免与关闭接线可先做 | 切第三篇/保存失败/批量关闭不丢草稿 |
| B03 / P0–P1 | 活动命令目标、查找快捷键、Esc/back 层级、overlay owner | FindReplacePanel、workspaceRegistry、command palette、Android coordinator | B01 的保存入口；不必等恢复 journal | 隐藏实例零消费，单次命令单目标 |
| B04 / P1 | 阅读工具集、格式减法、大纲直达、去重复概览、密度策略 | EditorToolbar、NotesContextPanel、workspace chrome | 设计可先做；交互依赖 B03 | 正文优先，能力仍可发现，模式不毁历史 |
| B05 / P1 | 打开协议、pane 历史与返回上下文、tab 关闭落点、恢复锚点 | NotesWorkspaceApp、nav history、search/heading bridge | B02、B03 | 搜索→笔记→链接→返回链完整 |
| B06 / P0–P1 | 全文 provider 接所有调用方、occurrence 定位、范围提示；再扩全文索引 | markdownWindow、editor adapter、FindReplacePanel、outline | B01；目标定位接 B05 | 后缀不丢，未加载目标可达，全文命令无缩水 |
| B07 / P0–P1 | 元数据 CAS、多字段保存、属性加载错误态、本篇标签范围 | NotesPropertiesTab、NotesEditorHeader、NoteCustomPropsEditor、DSTU | B01、B03 | 不同键不互盖，同键冲突可处理，局部不改全库 |
| B08 / P1 | 手机四状态、工具条默认动作、辅助页返回、输入法与键盘接线 | MobileEditorToolbar、mobileEditorCommands、NoteContentView | B03–B06；真实设备验证独立安排 | 单 owner、一次返回一层、光标与选区稳定 |
| B09 / P1 | 文本/块/附件命令的选区映射、transaction、undo 契约 | Crepe API、块菜单、selection、imageUpload | B03、B06 | 取消不写，过期目标不误写，块往返正确 |
| B10 / P1 | 统一改名服务、链接同步完整性、背链准确跳转、图谱范围 | rename helper、wikilink、backlinks、graph、VFS links | B01、B05、B07；附件身份衔接 B09 | 各改名入口一致，截断/失败有出路 |
| B11 / P1–P2 | 全文/选区 AI 范围、完成状态、制卡接线、checkpoint 语义 | AI handler、generateCards、页面/选区菜单 | B01、B03、B06、B09 | 版本确认、全文范围、过期建议处理正确 |
| B12 / P1–P2 | 导出保存屏障、导入预检/任务清单、删除恢复、往返语料 | library dialog、NotesAPI、cmd/notes、importer/exporter | B01、B02、B10 | 结果范围诚实，覆盖安全，重试不重复 |

### 15.2 每批再拆“止住问题”和“长期完成”

| 主题 | 先交付的最小闭环 | 后续完成 | 不应提前做 |
|---|---|---|---|
| 保存 | 即时 dirty、命令统一、完整失败状态 | view 外会话、持久草稿、跨窗口协调 | 更换数据库/编辑器解决入口分叉 |
| 关闭 | 三态协议、脏标签豁免 | 批量关闭清单、资源引用计数、恢复 journal | 放大所有 editor 保活数量 |
| 大纲 | occurrence、窄屏选后返回 | 全文锚点、跨 revision 映射 | 重复追加标题模糊匹配规则 |
| 搜索 | 所有权、IME、范围说明 | 命中位置、返回上下文、全文索引 | 全库每键同步扫描 |
| 属性 | 内容身份/错误态、CAS、本篇标签范围 | 字段合并、受控全库批次 | 在 UI 回调里再加一套独立写队列 |
| 移动 | 使用现有 owner/back，减少默认动作 | 真机选区/键盘细化、常用动作配置 | 把所有桌面按钮复制到底栏 |
| 链接 | 统一标题改名、显示 scanFailed/truncated | 可继续同步、历史别名/ID 策略 | 强制覆盖 dirty 来源 |
| AI | 全文接线、范围与完成语义 | 事务级 undo、checkpoint 管理 | 第二套持久化成功标准 |

**必须遵守的依赖：**

1. 没有正确 dirty/版本，不能让新的布局淘汰策略扩大编辑器卸载。
2. 没有全文 provider，不能增加更多以“全文”为名的 AI/替换按钮。
3. 没有明确 view 目标，不能扩展全局快捷键或 noteId 广播。
4. 没有保存屏障和快照范围，不能承诺导出包含正在看的最新内容。
5. 没有内容兼容语料与适配层，不进入编辑器迁移。

### 15.3 评审和回退方式

- 每批先写行为契约，再做对应最小改动；从现有职责抽边界，不一次替换全部 notes 模块。
- 同一路径切到新 coordinator 后，旧路径必须停止写入；可以保留读侧比对，不能同时双写“观察效果”。
- 错误时回退 UI 表现，不回退为不带版本的写入。数据已迁入可恢复草稿后，回滚 UI 也必须能读出这些草稿。
- 无需预先做大规模数据迁移；journal/schema 等确需迁移时单独评审、版本化，并提供旧数据兼容读取。
- 变更与验收证据跟随对应批次；批次失败只阻塞真正依赖它的后续工作，不把“做完全部架构”当作每个视觉改善的前提。

---

## 十六、验收细化：验证转移过程与异常，不只看最终截图

沿用 A01–A24；以下增加应在实施时使用的可重复场景。本次只制定验收，第二轮没有执行这些测试或设备操作。

### 16.1 逻辑与故障注入矩阵

| 编号 | 受控条件 | 必须观察到的事实 | 对应批次 |
|---|---|---|---|
| V01 | 输入 transaction 后、Markdown debounce 前立即关闭 | dirty 已成立；保存门控读到该 revision | B01 |
| V02 | v1 保存延迟，输入 v2，再按保存 | v1 完成不清 v2；flush 确认正确目标版本 | B01 |
| V03 | 标题未 blur；正文 save handler 成功 | 标题也提交或显示阻塞原因，不错误判整篇成功 | B01/B07 |
| V04 | 连续写入失败，A→B→C→D | A 草稿有主人、错误可见，返回后可重试 | B02 |
| V05 | 关闭多个标签，中途取消/失败 | 不再弹后续脏项；保留未获准项和焦点上下文 | B02 |
| V06 | 同 note 两 view，一个外部冲突 | 另一个草稿不被 noteId force 清掉 | B02/B03 |
| V07 | 同篇两视图同时改不同属性键/同一键 | 不同键合并，同键冲突；watch 不替换正在输入值 | B07 |
| V08 | 隐藏 pane 保留查找，活动 pane 按 F3 | 仅活动目标导航；后台输入无焦点 | B03 |
| V09 | 菜单覆盖查找，且在专注模式中按 Esc | 只关菜单，第二次才到下一层 | B03 |
| V10 | 中文候选中按 Enter；输入法报告 keyCode 229 | 搜索/替换/标签/属性不执行业务动作 | B03/B07/B08 |
| V11 | 搜索慢响应乱序，连续打开同篇两个目标 | 旧结果不回写；最后一个目标精确消费 | B05 |
| V12 | 第 900 行命中，同名标题三处 | 加载后准确定位；不默认落到最后一个同名标题 | B06 |
| V13 | 长文前缀编辑、加载更多、全文替换、撤销 | 后缀与全文版本均保留；范围反馈准确 | B06/B09 |
| V14 | A 属性加载中切 B，A 晚到/B 失败 | B 不出现 A 可操作正文；有失败重试 | B07 |
| V15 | 原生选图/上传期间编辑正文、切篇或设只读 | 过期坐标映射或拒绝；不误写新篇 | B09 |
| V16 | 300 个改名来源，含 dirty、OCC conflict、扫描故障 | 完整性状态真实，失败/待处理可继续 | B10 |
| V17 | AI 预览后继续编辑、接受时再次变化、保存失败 | 重算/拒绝目标明确，两版仍可恢复 | B11 |
| V18 | 导出前存在未序列化/失败草稿 | 等待或明确仅已保存版本，不暗示最新已备份 | B12 |
| V19 | 导入部分成功后重试，同 ID/同名/跨时区 | 不重复创建；覆盖策略和结果可解释 | B12 |
| V20 | native 进程终止后重开 | 恢复到可验证 durable checkpoint；不虚报零损失 | B02 后续 |

优先在现有对应测试中增加真正跨入口、跨版本的断言。mock 必须保留 Promise 时序与多个实例，不能把 save/close 全部 mock 成立即成功。现有 81 项通过不能替代上述新增边界。

### 16.2 宿主、设备与内容组合

| 维度 | 至少覆盖 | 当前边界 |
|---|---|---|
| 宿主 | Workbench、Learning Hub、独立内部窗口、资源预览入口 | 同一组件不代表相同生命周期 |
| 视图 | 单 pane、左右 pane、隐藏保活、同篇多 view、冷恢复 | 单实例 happy path 不够 |
| 尺寸 | 320/375/390 手机级容器、768 附近、窄桌面、宽桌面 | 容器测试仅验证响应式结构 |
| 输入 | 鼠标键盘、触控、触屏加外接键盘、中文 IME | coarse pointer 不等于软键盘一定出现 |
| 系统 | macOS/Windows/Linux Tauri、iOS、Android | 当前没有新增真实 Tauri/手机验证 |
| 特殊状态 | 系统字体放大、深浅主题、reduced-motion、辅助技术 | 需要实际渲染与读屏确认 |
| 文本语料 | 空篇、重复标题、超窗口长篇、Unicode、全部特殊块、缺失附件 | 验证语义往返，不仅是截图相似 |

不必一次做所有维度的笛卡尔积。数据一致性场景先跑跨宿主逻辑测试；焦点/可见性在真实 Tauri 验证；键盘/手柄/back 在真实 iOS/Android 验证。后两者是未来实现验收，**不是当前报告完成的前置条件**。

---

## 十七、推荐的下一步与编辑器决策门槛

第一批最值得直接进入实现的是 **B01 保存、B02 关闭与淘汰、B03 命令所有权**；同时评审 B04 的阅读工具集、默认格式减法、大纲直达与去重复概览。这样用户能较早感到界面更安静，底层也开始变得可信。

随后接 B05/B06/B07，优先完成“找到内容→定位→修改→离开→返回”这个闭环，再扩展移动和 AI 的复杂操作。C13 的元数据 CAS、C14 的改名入口与失败反馈、C15 的关闭协议都是有明确接线位置的小切口，不必等待完整新框架。

**保留 Crepe/Milkdown 的判断在第二轮没有改变。** 迁移前必须满足：

1. 保存、全文、选区、定位、只读、命令能力已通过 adapter 隔离；
2. Markdown 与特殊块、wikilink、附件、资源引用具备稳定兼容语料；
3. 同一份语料在候选底座中完成导入→编辑→撤销→保存→重开→导出，无静默语义丢失；
4. 候选底座在实际问题上给出可验证收益，例如移动选区或特定结构编辑，而不是只证明能展示一篇简单 Markdown；
5. 草稿、存储与导出格式可继续使用，且保留按文档回退路径。

满足这些条件后，再判断哪些问题来自底座限制，哪些来自宿主。当前最有把握的收益，仍是统一文档事实、稳定导航与焦点、缩减默认界面，并让每个动作都有符合人预期的结束位置。

---

## 源码索引

以下链接固定到本轮审阅 commit。正文中的 S 编号可直接打开对应文件。

- [S1 — NoteContentView：加载、窗口化、OCC、保存命令、上下文子页][S1]
- [S2 — NotesCrepeEditor：保存队列、dirty、模式、工具条、事件与生命周期][S2]
- [S3 — NotesWorkspaceApp：工作区导航、多标签、分屏、两标签挂载与状态栏][S3]
- [S4 — NotesEditorToolbar：默认格式条、只读处理、制卡与快捷键][S4]
- [S5 — NotesContextPanel：信息顺序、实时大纲、标签全局改名、标题点击][S5]
- [S6 — NotesGenerativeSummary：确定性概览的展示][S6]
- [S7 — buildNoteSummaryIntent：统计、概览与标题列表的重复组织][S7]
- [S8 — 中文文案：标签改名语义][S8]
- [S9 — MobileEditorToolbar：工具、portal、键盘偏移和动作可用性][S9]
- [S10 — CrepeEditor：Markdown 变更通知与标题遍历定位][S10]
- [S11 — TabPanelContainer：五标签保活与 dirty 豁免][S11]
- [S12 — FindReplacePanel：全局快捷键、当前文档查找与替换][S12]
- [S13 — markdownWindow：默认窗口、加载边界与全文保存组合][S13]
- [S14 — generateCardsFromNote：全文优先读取契约][S14]
- [S15 — NotesPropertiesTab：元数据、正文异步加载与 watch][S15]
- [S16 — NotesSearchOverlay：快速切换、全文搜索与键盘提交][S16]
- [S17 — note_repo.rs：持久化、OCC、事务与链接索引][S17]
- [S18 — closeTabGate：dirty 查询、保存并关闭与取消][S18]
- [S19 — 2026-09-12 历史验收记录][S19]
- [S20 — 2026-09-12 历史 UX 审计][S20]
- [S21 — “AI 笔记摘要”中文文案][S21]
- [S22 — contentDirtyRegistry：多 checker/save handler 聚合与保存确认][S22]
- [S23 — ContentCloseConfirmation：三态关闭、兼容入口与请求队列][S23]
- [S24 — useNotesNavHistory：回退、前进、裁剪与导航再入守卫][S24]
- [S25 — workspaceRegistry：宿主与编辑器归属、窗口匹配][S25]
- [S26 — findQueryBridge：按 noteId 暂存查找请求][S26]
- [S27 — headingTargetBridge：按 noteId 暂存标题目标][S27]
- [S28 — androidBackCoordinator：返回优先级、可见性与 native 回退][S28]
- [S29 — mobileEditorCommands：图片、slash、缩进、制卡与底层命令][S29]
- [S30 — useCanvasAIEditHandler：建议重算、全文替换、checkpoint][S30]
- [S31 — NoteCustomPropsEditor：属性字段校验、类型和提交][S31]
- [S32 — DSTU API：setMetadata 的可选版本参数][S32]
- [S33 — wikilinkRenameSync：有界来源发现、OCC、dirty 跳过与 summary][S33]
- [S34 — createFromWikilink：创建上下文、去重、创建后导航][S34]
- [S35 — wikilink view：创建确认、同名候选、预览与点击][S35]
- [S36 — NotesBacklinksPanel：来源发现、范围、加载与关系展示][S36]
- [S37 — NotesGraphTab：图谱状态、深度、后端与客户端降级][S37]
- [S38 — WorkbenchNotesLibraryDialog：导入/导出触发与进度订阅][S38]
- [S39 — NotesAPI：导入导出调用与 includeVersions 废弃说明][S39]
- [S40 — cmd/notes.rs：导出 staging、ZIP 与 Markdown 导入协议][S40]
- [S41 — imageUpload：稳定资源引用、失败处理与临时 URL][S41]
- [S42 — Crepe API types：全文、选区、持久化与标题定位契约][S42]
- [S43 — searchHighlight：文本投影、原子边界、正则与非重叠替换][S43]
- [S44 — NotesEditorHeader：标题输入、pending title 与 dirty 注册][S44]
- [S45 — notes.commands：命令面板入口与保存事件][S45]
- [S46 — NotesLibraryManager：文库面板、冲突策略与进度字段][S46]

关键复核入口：C13 先看 `NotesPropertiesTab.handleCustomPropsChange → dstu.setMetadata → VfsNoteRepo.update_note_metadata_with_conn`；C14 看 `NotesWorkspaceApp.renameTreeItem → syncWikiLinksAfterNoteRename` 并对比 `NoteContentView.handleTitleChange`；C15 对比两种 close gate；C16 看 `confirmExport → NotesAPI.exportNotes → notes_export`。这些符号比易随修订移动的行号更适合实施时重新定位。

[S1]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/learning-hub/apps/views/NoteContentView.tsx
[S2]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/NotesCrepeEditor.tsx
[S3]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/notes/NotesWorkspaceApp.tsx
[S4]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/components/NotesEditorToolbar.tsx
[S5]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/NotesContextPanel.tsx
[S6]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/components/NotesGenerativeSummary.tsx
[S7]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/generative-ui/utils/buildNoteSummaryIntent.ts
[S8]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/locales/zh-CN/notes.json
[S9]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/components/MobileEditorToolbar.tsx
[S10]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/components/crepe/CrepeEditor.tsx
[S11]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/learning-hub/apps/TabPanelContainer.tsx
[S12]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/components/FindReplacePanel.tsx
[S13]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/markdownWindow.ts
[S14]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/generateCardsFromNote.ts
[S15]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/notes/NotesPropertiesTab.tsx
[S16]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/notes/NotesSearchOverlay.tsx
[S17]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src-tauri/src/vfs/repos/note_repo.rs
[S18]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/learning-hub/closeTabGate.ts
[S19]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/docs/research/notes-ux-verification-20260912.md
[S20]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/docs/audits/2026-09-12-notes-ux-audit.md
[S21]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/locales/zh-CN/generativeUi.json
[S22]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/content/contentDirtyRegistry.ts
[S23]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/content/ContentCloseConfirmation.tsx
[S24]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/notes/hooks/useNotesNavHistory.ts
[S25]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/notes/workspaceRegistry.ts
[S26]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/findQueryBridge.ts
[S27]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/headingTargetBridge.ts
[S28]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/app/navigation/androidBackCoordinator.ts
[S29]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/mobileEditorCommands.ts
[S30]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/hooks/useCanvasAIEditHandler.ts
[S31]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/notes/NoteCustomPropsEditor.tsx
[S32]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/dstu/api.ts
[S33]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/notes/wikilinkRenameSync.ts
[S34]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/createFromWikilink.ts
[S35]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/components/crepe/plugins/wikilink/view.ts
[S36]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/notes/NotesBacklinksPanel.tsx
[S37]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/notes/graph/NotesGraphTab.tsx
[S38]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/workbench/apps/notes/WorkbenchNotesLibraryDialog.tsx
[S39]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/utils/notesApi.ts
[S40]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src-tauri/src/cmd/notes.rs
[S41]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/components/crepe/features/imageUpload.ts
[S42]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/components/crepe/types.ts
[S43]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/components/crepe/plugins/searchHighlight.ts
[S44]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/components/NotesEditorHeader.tsx
[S45]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/command-palette/modules/notes.commands.ts
[S46]: https://github.com/helixnow/deep-student/blob/69fbed572187b2d4470d3df61e9abdc4b74da181/src/features/notes/NotesLibraryManager.tsx
